<view class="page-shell builds-shell">
  <navigation-bar title="{{navTitle}}" back="{{false}}" color="#FFFFFF" background="#111111"></navigation-bar>
  <scroll-view class="page-scroll" scroll-y type="list" scroll-into-view="{{scrollTarget}}" scroll-with-animation="{{true}}">
    <view class="page-content builds-content">
      <view class="hero builds-hero">
        <text class="hero-kicker">{{kicker}}</text>
        <text class="hero-title">{{title}}</text>
        <text class="hero-desc">{{desc}}</text>
      </view>

      <view class="section query-section">
        <view class="section-header">
          <text class="section-title">查询入口</text>
          <text class="section-action">选择模块</text>
        </view>
        <view class="query-grid">
          <view
            class="query-card"
            wx:for="{{quickActions}}"
            wx:key="key"
            data-key="{{item.key}}"
            bindtap="openQueryPage"
          >
            <view class="query-icon">{{index + 1}}</view>
            <text class="query-title">{{item.title}}</text>
            <text class="query-desc">{{item.desc}}</text>
            <text class="query-link">进入查询</text>
          </view>
        </view>
      </view>

      <view id="task-section" class="section task-section">
        <view class="section-header">
          <text class="section-title">任务列表</text>
          <text class="section-action">最近记录</text>
        </view>
        <view class="task-list" wx:if="{{tasks.length}}">
          <view
            class="task-card"
            wx:for="{{tasks}}"
            wx:key="taskId"
            data-task-id="{{item.taskId}}"
            bindtap="openTaskDetail"
          >
              <view class="task-row">
                <text class="task-title">{{item.title}}</text>
                <text class="task-status">{{item.status}}</text>
              </view>
              <text class="task-desc" user-select="{{true}}">{{item.desc}}</text>
          </view>
        </view>
        <view class="task-empty" wx:else>
          <text>暂无任务记录</text>
        </view>
      </view>
    </view>
  </scroll-view>

</view>
