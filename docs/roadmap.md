# WOW Mini Program Roadmap

> 这份文档是项目的长期推进控制台：先统一目标，再拆行动，再把讨论中的想法持续收敛到可执行路线中。

## 近期落地证据

- 2026-06-27：修复 SimC 确认摘要右侧全显示“不可用”的装备回放问题。根因是装备页从 `equippedSet` 生成选择状态时，同槽同 itemId 的 SimC-ready 候选证据会被 `sourceType=crafted / simcReady=false / missingFields=['ilevel']` 的展示对象覆盖，导致保存后的结构化装备模板在 `/api/websim/gear/stats` 回放时丢失腰带、鞋子等核心槽位。现在前端会在匹配到 SimC-ready 候选时保留其可执行证据；后端 stats 也接受 `id + bonus_id` 的确定性 SimC 装备行，不再强制要求额外 `ilevel`；SimC 页若仍收到 blocked snapshot，会在右侧属性面板展示首个 blocker 的中文摘要，避免只出现五个“不可用”。证据：`pages/builds/detail.js`、`pages/simulator/simc.*`、`server/websim_payload.py`、`tests/builds-page.test.js`、`tests/simulator-page.test.js`、`tests/websim_payload_test.py`。
- 2026-06-27：SimC 首屏交互继续收敛为模板选择器工作流。天赋模板和装备模板现在共享同一套紧凑选择行；当某类模板为空时，页面显示禁用态选择框而不是展开大块虚线空态，避免空天赋模板把首屏挤高。确认摘要右侧的属性面板增加状态分层：缺装备/缺天赋/计算中/不可用/verified 快照；结构化装备模板缺少 verified `statSnapshot` 时，页面继续通过 `/api/websim/gear/stats` 后台计算，但成功后会把 SimulationCraft JSON 产出的 verified `statSnapshot` 连同职业/专精/种族/天赋/装备签名写回构筑模板 metadata，后续签名命中才直接读取缓存，换种族等上下文变化会重新计算，不在前端按 rating 猜百分比。证据：`pages/simulator/simc.*`、`tests/simulator-page.test.js`。
- 2026-06-27：SimC 确认摘要改为左右两栏：左侧保留职业、种族、天赋、装备、场景信息，并对过长天赋/装备模板名单行省略；右侧消费装备模板 metadata 中的 verified `statSnapshot`，展示主属性以及暴击、急速、精通、全能四个副属性百分比。装备模板保存时只把轻量 SimC JSON 属性快照写入 metadata，不写入 `rawString`，避免 `checkedAt` 等快照字段造成同一装备重复模板；旧结构化装备模板缺少 verified 快照时，SimC 页会复用 `/api/websim/gear/stats` 后台补一次属性快照，失败则显示明确的缺口/不可用状态，不在前端猜 rating 公式。证据：`pages/simulator/simc.*`、`pages/builds/detail.js`、`tests/simulator-page.test.js`、`tests/builds-page.test.js`。
- 2026-06-26：天赋模拟与装备模拟的导入入口完成收口，按钮统一从“导入社区推荐”改为“导入”；点击后弹窗拆成两层，上层展示“我的保存”里的个人天赋/装备模板，下层保留社区推荐。天赋保存模板复用原有 WebSim 导入码应用链路，支持同专精直接写入与跨专精切换后应用；装备保存模板读取 `gearBySlot` 与 `enhancementBySlot` 快照，继续走武器规则、强化兼容性和属性刷新校验。证据：`pages/builds/talent-simulator.*`、`pages/builds/detail.*`、`pages/common/build-template-storage.js`、`tests/builds-page.test.js`。

- 2026-06-26：首版上线范围完成二次收口。职业专精 tab 首版保留“天赋构筑”“装备模拟”“模拟 SimC”“任务列表”四个入口，并承接最近模拟任务列表；暂下掉“热门专精”“属性权重”和“输出循环”。PVE 专区暂时从 tabBar 与小程序页面注册中下线；智能分析 tab 首版只保留“炸鸡队长”，下掉“分析 WCL”，SimC 与任务列表迁入职业专精。上述暂缓能力的既有代码与历史证据保留，统一标记为待规划，后续重新规划入口、数据源和验收标准后再接回。证据：`app.json`、`server/builds/home-payload.js`、`pages/builds/builds-api.js`、`pages/builds/builds.*`、`pages/simulator/simulator-api.js`、`pages/simulator/simulator.*`、`server/simulator_payload.py`、`tests/navigation-bar.test.js`、`tests/builds-home-payload.test.js`、`tests/builds-page.test.js`、`tests/simulator-page.test.js`、`tests/frontend-api-client.test.js`、`tests/pve-page.test.js`、`tests/news_backend_test.py`。

- 2026-06-26：装备强化机制已按装备模拟与天赋模拟同级 runbook 重新复核并补齐记录，覆盖宝石、美化、附魔、套装从上游证据到 UI 的边界。宝石对外仍显示 3 槽，但底层明确为后端 `SOCKET_OPTION_GEAR_SLOT_CAPACITY` 下发的项链、戒指 1、戒指 2 各 `socketCount=1`；附魔把腿部护甲片纳入 `enchant`，但 UI 上限按当前已选装备的 display-ready 行与受控兼容兜底统计，不直接等于 `ENCHANTABLE_GEAR_SLOTS` 全集；美化继续按自带 + 独立总数 2 兜底；套装只由已选装备 item-set metadata 自动统计，不进入手工强化配置。装备卡金色 tag 的来源也明确为已确认且仍匹配当前装备的 `enhancementBySlot` 或装备自带美化，不能从全局计数倒推。证据：[装备模拟全链路 Runbook](gear-simulation-full-chain-runbook.md)、[装备强化配置方案](plans/2026-06-24-gear-enhancement-config-design.md)、`server/websim_payload.py`、`pages/builds/detail.js`、`tests/builds-page.test.js`、`tests/websim_payload_test.py`。

- 2026-06-26：装备宝石槽位统计已改为后端能力证据链驱动，前端不再维护 `neck/finger1/finger2` 的本地容量表。当前后端 read model 仍按 `SOCKET_OPTION_GEAR_SLOT_CAPACITY` 输出项链、戒指 1、戒指 2 各 `socketCount=1`，所以对外展示仍为 `宝石 0/3` 或 `3/3`；前端只消费 `modCapabilities.socketCount` / socket 数组等显式容量证据，并在缺失容量字段但明确 `hasSocket=true` 的旧载荷下按 1 兼容。线上抽样 `shaman/elemental` compact payload 验证 `neck/finger1/finger2` 均为 `hasSocket=true/socketCount=1` 且 `socketTotal=3`，`head/wrist/waist/legs` 为 `hasSocket=false` 且无 socketCount；远端 `pages/builds/detail.js` 已无 `socketCapacityByGearSlot`。证据：`server/websim_payload.py`、`pages/builds/detail.js`、`tests/builds-page.test.js`、`tests/websim_payload_test.py`、生产 `/api/websim/gear?class=shaman&spec=elemental&compact=1`、生产 `/api/data/health`。

- 2026-06-26：装备模拟属性概览完成 UI 修正，并将早先的本地副属性换算方案废弃，改为 SimC JSON 角色快照口径。装备保存模板改为与天赋模拟一致的命名弹窗与默认命名规则；宝石/附魔 sheet 修正禁用选项偶发白底；属性概览把美化、宝石、附魔、套装固定为同一行，宝石/附魔基数改为当前已选装备能力口径，套装基数固定 `5`，装备卡片统一黄色边框。经 Elemental Shaman、Frost Mage 和 Retribution Paladin 线上角色样本验证，装备本地 rating 无法可靠推导角色面板百分比；`/api/websim/gear/stats` 现在运行 SimC 时写 `json=<tmp>`，优先解析 `collected_data.buffed_stats.attribute/stats`，返回 `statSource=simulationcraft_json` 的 verified 快照；前端只有在完整装备 + 天赋导入码 + 请求签名变化时请求一次 stats，并且只有 verified snapshot 才展示急速、暴击、精通、全能百分比。没有快照、字段不完整或 SimC item resolution warning 时 fail closed，只显示 raw rating，不输出百分比。证据：[装备属性百分比 SimC JSON 快照方案](plans/2026-06-26-gear-stat-simc-json-snapshot.md)、`server/websim_payload.py`、`pages/builds/detail.*`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/gear/stats`。

- 2026-06-26：社区模板导入链路已按独立 Runbook 治理并完成生产入库、热部署和 40 专精验收。新增 [社区模板导入全链路 Runbook](community-template-import-full-chain-runbook.md)，把 Raider.IO / WCL / manual fixture / `websim_baseline` 的来源边界、入库门禁、per-spec cap、公开身份去重、前端 consumer-only、装备导入 serializer gate、health 验收和回滚流程固化；`websim_baseline` 只作为后端 authority 生成的可编辑兜底模板，不冒充社区玩家样本。后端新增 baseline 生成器，按当前 `websim_talents` 贪心点满 class/spec/hero 三树并复用 `encode_websim_talents` 校验；同时修正 backend hero tree point gate，使 granted hero root 参与英雄树门槛，class/spec 仍保持 purchased gate 规则。生产写库前备份 `/opt/wow-mini-program/backups/wow_news-before-community-template-baseline-resync-20260626T073401Z.sqlite3`；最终 `websim_baseline=40 verified / 0 blocked`，社区模板 sync `70 verified / 1 blocked`，`scanCoverage=40/40`、`missingSpecs=[]`，Warcraft Logs 因缺凭据保持 `missing_credentials`，因此 `community_templates` 总体仍为 `partial`。线上 40 专精矩阵验证 `specsChecked=40`、`templatesValidated=71`、`issueCount=0`，每个专精返回 1-3 条、无跨专精、无重复公开身份且全部可由 `/api/talents/validate` 编码；公网抽样 `shaman/elemental/stormbringer` 返回 Raider.IO + WebSim baseline，`mage/fire/sunfury` 返回 WebSim baseline。证据：[社区模板导入全链路 Runbook](community-template-import-full-chain-runbook.md)、`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 SQLite 备份、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/talents`、生产 40 专精矩阵脚本。

- 2026-06-26：社区天赋推荐链路完成按当前职业专精收敛并已线上热部署。本轮根因是后端 `/api/websim/talents` 社区模板读模型只按 `class_key` 查询，前端 consumer 也只按职业过滤，导致元素萨会看到增强/恢复模板；同时仅按天赋编码 signature 去重，仍可能出现同一公开推荐身份的重复卡片。现在后端按 `class_key + spec_key` 取数，先按 talent signature 去重，再按公开展示身份去重，并限制每个专精最多 `3` 条；前端只镜像同一规则，不自行扩大推荐范围。线上公网抽样 `shaman/elemental/stormbringer` 返回 1 条且 `specs=["elemental"]`，`mage/frost/spellslinger` 返回 2 条且 `specs=["frost"]`；生产 SQLite 只读矩阵覆盖 40 个专精，18 个专精当前有推荐，`issueCount=0`，没有跨专精、超过 3 条或重复公开身份。证据：`server/websim_payload.py`、`pages/builds/talent-simulator-core.js`、`pages/builds/talent-simulator.js`、`tests/websim_payload_test.py`、`tests/talent-simulator-core.test.js`、`tests/builds-page.test.js`、生产 `/health`、生产 `/api/websim/talents?class=shaman&spec=elemental&hero=stormbringer`、生产 `/api/websim/talents?class=mage&spec=frost&hero=spellslinger`、生产只读 40 专精矩阵脚本。

- 2026-06-26：天赋详情法术说明展示改为后端 fail-closed，避免 Wago/SimC spell template 中的 `$?s...`、`${...}`、`$455089d` 等未解析公式泄漏到小程序详情 sheet。`/api/websim/talents` 现在为节点输出安全的 `description` 与 `descriptionStatus`，未解析数值统一显示“描述待补”，并在 `talentReadiness.spellCoverage.unresolvedDescriptionCount`、`spellReady=false` 和 blockers 中暴露真实缺口；编码链路仍按 `encodingReady/simcReady` 独立判断，不把可执行天赋树降级成 fallback。代码已热部署，未写生产 SQLite，未启动异步同步。线上抽样 `shaman/elemental/stormbringer` 返回 121 个节点、97 个 unresolved 描述，`风暴涌浪` / `超荷充能` 不再返回 `$` 公式；`/api/data/health` 中 `talent_catalog` 当前 `coveredSpellCount=499 / talentSpellCount=3160`、`unresolvedDescriptionCount=2660`、`missingDescriptionCount=1`。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 `/health`、生产 `/api/websim/talents?class=shaman&spec=elemental&hero=stormbringer`、生产 `/api/data/health`。

- 2026-06-26：全职业天赋模拟器治理补强完成后，已执行生产 SQLite 窄同步入库并保留回滚备份 `/opt/wow-mini-program/backups/wow_news-before-talent-narrow-sync-20260626T060945Z.sqlite3`。本轮只刷新天赋相关数据，不跑完整装备 catalog：SimC trait data 重新写入 `5246` 个天赋节点，SimC build 为 `12.0.5.67823`，Wago TraitEdge 依赖 `8465` 条，profile preset 解析到 `50` 个；社区天赋模板表从 `263` 增至 `267` 条，当前状态 `239 verified / 28 blocked`，同步来源为 manual fixture 与 Raider.IO，Warcraft Logs 因缺凭据保持 `missing_credentials`。线上 `/api/websim/talents?class=mage&spec=frost&hero=spellslinger` 返回 `talentStatus=simc`、110 个节点、12 个社区模板、`treeReady/ruleReady/spellReady/encodingReady/simcReady=true`；抽取 Raider.IO 冰法急咒师模板调用 `/api/talents/validate` 返回 `encoded` 且无 blocker，`/api/websim/profile` 可生成 `class_talents/spec_talents/hero_talents`，仅因未传装备保持 `profileReadiness.gearReady=false`。全局 `talent_catalog` 仍为 `partial`，spec 覆盖 `40/40`、hero tree 覆盖 `80/80`、`officialAuditStatus=pending_official_audit`，剩余 blocker 是 `455630 / 涌动图腾` 缺法术描述。证据：生产 SQLite 备份、生产窄同步 JSON 输出、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/talents`、生产 `/api/talents/validate`、生产 `/api/websim/profile`。

- 2026-06-26：全职业天赋模拟器完成与装备模拟同级的链路治理补强，并已按代码热部署路径上线；代码热部署阶段未做生产 SQLite 写入、未启动异步同步任务。新增 [全职业天赋模拟全链路 Runbook](talent-simulation-full-chain-runbook.md)，明确 SimC trait data、Wago trait edges、Blizzard spell/media、社区模板、SQLite catalog、后端 read model、前端 consumer-only、encoding/profile/simulate、health 和回滚边界；`GET /api/websim/talents` 调整为纯只读读模型，不再隐式触发社区模板同步；天赋 payload 新增 `talentReadiness/blockers`，`/api/talents/*` 复用同一 backend authority，`/api/websim/profile` 新增合并 gear/talent 的 `profileReadiness`，`talent_catalog.details` 补充 spec/hero coverage、rule/source/readiness 维度；前端在 fallback、authority blocked 或 `talentReadiness.simcReady=false` 时允许预览但禁止保存为可执行模板。线上验证：公网 `/health` 200，`/api/websim/talents?class=mage&spec=frost&hero=spellslinger` 返回 `talentStatus=simc`、110 个节点、`treeReady/ruleReady/spellReady/encodingReady/simcReady=true`，`/api/data/health` 中 `talent_catalog` 为 `partial` 且 `expectedSpecCount=40 / coveredExpectedSpecCount=40 / expectedHeroTreeCount=80 / coveredExpectedHeroTreeCount=80`，`officialAuditStatus=pending_official_audit`，仅剩 `talent spell descriptions missing for 1 talent spells` 的展示素材 blocker；`/api/websim/simulate` 对空天赋输入返回 `status=blocked` 且未启动 SimC。证据：[全职业天赋模拟全链路 Runbook](talent-simulation-full-chain-runbook.md)、`server/websim_payload.py`、`pages/builds/talent-simulator.js`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`tests/builds-page.test.js`、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/talents`、生产 `/api/websim/simulate`。

- 2026-06-26：装备模拟全链路 Runbook 已补齐，作为后续版本/赛季装备更新的主执行手册。文档从 Battle.net Game Data API、Raider.IO/WCL observed、SimulationCraft probes、Wago/curated seeds 等上游证据开始，串联只读审计、SQLite 备份、source/variant/mod option 入库、制造业和自带美化门禁、全职业专精武器/护甲/主属性适配、compact read model、前端 consumer-only 展示、`merge_websim_gear_enhancements` serializer 兜底、health 指标、生产 smoke、测试矩阵与 DB/代码回滚路径。治理标准顶部已增加 Runbook 入口，下一次版本更新应优先按该文档的快路径和清单执行，再把实际备份路径、health counts 和已知 blockers 回写 roadmap。证据：[装备模拟全链路 Runbook](gear-simulation-full-chain-runbook.md)、[装备自建数据库治理标准](gear-database-governance.md)、`server/websim_payload.py`、`server/crafted_gear_backfill.py`、`server/news_backend.py`、`pages/builds/detail.*`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`。

- 2026-06-26：装备强化配置的 Midnight 制造业美化映射完成整体验证、生产刷新和热部署。当前独立美化 catalog 收敛为 11 个 Midnight optional reagent：奥纹内衬、阳炎内衬、受祝福的磐皋、棱镜聚焦虹膜、稳定宝石弹药带、吞噬绑带、原始孢子缚带，以及鲜血/狩猎/腐朽/虚空四个暗月徽记；旧 TWW 默认美化不再进入当前赛季默认种子，只保留历史展示兼容。读模型按装备类型而不是单纯槽位过滤副手：盾牌可选护甲/武器护甲/通用装备类美化，不展示暗月徽记；Held In Off-hand 副手可选暗月、武器护甲和通用装备类美化，不展示护甲内衬；自带美化制造装备继续自动计入 `美化 1/2` 且禁止同槽再叠加独立美化。生产备份为 `/opt/wow-mini-program/backups/wow_news-before-midnight-embellishment-options-20260626T015048Z.sqlite3`，热部署后补跑 Wago 附魔中文名同步并重启服务；公网 `/health` 通过，`/api/data/health` 中 `details.sourceCoverage.crafted=84`、`modOptionCoverage.socket=20`、`enchant=21`、`embellishment=11`、`crafted_stats=6`，`shaman/elemental` compact 抽样验证制造项链/戒指/武器/副手按新规则返回美化选项。证据：[装备强化配置方案](plans/2026-06-24-gear-enhancement-config-design.md)、[Midnight 制造业装备整库审计与生产修正记录](plans/2026-06-25-midnight-crafted-gear-full-audit.md)、`server/websim_payload.py`、`pages/builds/detail.*`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`、生产 SQLite 查询、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/gear?class=shaman&spec=elemental&compact=1`。

- 2026-06-25：装备自建数据库实施方案升级到 v2，把后续周期性装备更新固定为“source registry -> 只读审计 -> verified variant 写库 -> backend read contract -> frontend consumer-only -> release gate/rollback”的执行链路。新方案明确普通制造业 allowlist、错误/unsupported 清单、verified 变体门禁、compact payload 必传字段、前端禁止硬编码、CodeRabbit/本地 CR、生产 smoke 与 roadmap 证据回写要求；`244774 / 以太流明践踏靴` 等工程单副属性制造装备被列为下一阶段独立扩展，不允许临时伪造双属性 option。证据：[装备自建数据库实施方案 v2](plans/2026-06-25-gear-database-implementation-plan-v2.md)、[装备自建数据库治理标准](gear-database-governance.md)。

- 2026-06-25：Midnight 制造业装备库完成整库审计、纠偏、生产写库和线上验收，本条覆盖 2026-06-24 阶段性制造业记录中的旧盾牌结论。当前制造业读模型按两条链路管理：54 件普通 PVE 可选属性制造装备来自 governed allowlist，30 件自带美化固定属性制造装备来自 Battle.net `装备唯一：美化（2）` metadata 与 fixed-stat SimC probe；生产 `source_type='crafted'` 来源为 `84` 条，crafted variants 为 `470/470 verified`、`0 partial`。已排除或阻断错误条目：`251105 / 破法者之盾` 仅保留副本掉落来源，不再作为制造业盾牌；真实制造盾牌是 `237831 / 破法者的责难`；`260370-260375`、`260377` 这批团本 BoE/random-stat observed 条目不再进入制造业；`244774 / 以太流明践踏靴` 因工程单副属性链路尚未支持，保持 unsupported，不伪造双属性 option。生产备份为 `/opt/wow-mini-program/backups/wow_news-before-crafted-audit-fix-20260625T093130Z.sqlite3`，热部署后 `/health` 通过，`/api/data/health` 中 `details.sourceCoverage.crafted=84`、`crafted_stats.optionCount=6`；线上 compact 抽样确认坏 ID 集合没有任何 `sourceTypes` 包含 `crafted` 的候选回流，腰部、武器、副手等制造业筛选均返回当前 allowlist 与自带美化清单。证据：[Midnight 制造业装备整库审计与生产修正记录](plans/2026-06-25-midnight-crafted-gear-full-audit.md)、[装备自建数据库治理标准](gear-database-governance.md)、`server/crafted_gear_backfill.py`、`tests/websim_payload_test.py`、生产 SQLite 查询、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/gear?compact=1` 抽样。

- 2026-06-25：装备库周期性更新实施文档补齐，明确后续装备筛选、校验、入库、读取、UI 联动、发布记录和回滚都按 DB-first / evidence-first 流程执行。`docs/gear-database-governance.md` 新增“周期性装备更新执行手册”，把 M+ / 团本 / 套装、普通可选属性制造业、自带美化固定属性制造业、宝石 / 附魔 / Optional Reagent 分开建模，要求 guide 只作为候选发现，Battle.net / SimC / Wago / curated allowlist 等结构化证据作为写库门禁，并把 compact 读链路、source filter 计数、自带美化计数、`craftedStatOptions=[]` 的固定属性例外和回滚边界写成验收项。`docs/plans/2026-06-25-midnight-preembellished-crafted-gear-ingestion.md` 补充 Midnight 自带美化制造装备的周期性复用模板，后续新增或修正同类装备时必须重新走筛选、只读审计、SimC probe、API smoke 和 UI 验收。证据：[装备自建数据库治理标准](gear-database-governance.md)、[Midnight 自带美化制造装备入库方案](plans/2026-06-25-midnight-preembellished-crafted-gear-ingestion.md)。

- 2026-06-25：Midnight Season 1 自带美化制造装备完成整批生产入库、后端读模型修正和热部署。按 Method/Wowhead 上游列表整理 30 件 pre-embellished crafted gear，并以 Battle.net Game Data API `preview_item.limit_category=装备唯一：美化（2）` 作为最终写库门禁；生产写库前备份 SQLite 到 `/opt/wow-mini-program/backups/wow_news-before-midnight-preembellished-crafted-20260625T082750Z.sqlite3`。本轮写入 30/30 件 `websim_items` metadata、30/30 条 `crafted-governed-*` source、32/32 条 `simulationcraft_preembellished_item_probe` fixed-stat crafted variant，武器/盾牌 `244472 / 骑士指挥官的雄关` 与 `244679 / 密谋小径鱼钩` 额外提供 `虚空晋升 295`，其余为 `神话 285`；`craftedDistinctTotal` 从 42 增至 71。后端读模型新增 fixed-stat crafted variant 支持，允许 verified `source_type=crafted` 且 `derivedVariantSource=simulationcraft_preembellished_item_probe` 的 `simcIlevelOnly` 变体进入 compact payload，但不放开其他来源的不完整候选。生产热部署后 `/health` 通过，`hunter/marksmanship`、`mage/frost`、`paladin/protection`、`rogue/assassination`、`shaman/elemental`、`priest/discipline` compact payload 抽样确认命中的自带美化制造候选均返回 `sourceTypes` 包含 `crafted`、`builtInEmbellishmentLabel=美化`、`simcReady=true` 和 verified 轨道。证据：[Midnight 自带美化制造装备入库方案](plans/2026-06-25-midnight-preembellished-crafted-gear-ingestion.md)、`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 SQLite 查询、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/gear?compact=1` 抽样。

- 2026-06-25：制造业自带美化装备的读模型、前端联动、生产写库和热部署完成。后端现在会从 Battle.net preview 的中英文 `limit_category` / unique-equipped embellishment 标记标准化出 `hasBuiltInEmbellishment`、`builtInEmbellishmentLabel=美化`、`embellishmentSource=built_in`，并在 compact gear candidate 中保留这些字段；前端装备候选行和已选装备卡片显示“美化”徽标，选择自带美化装备后会自动把属性概览计为 `美化 1/2`，并裁剪同槽位原有独立美化配置。生产只读审计确认截图目标为 `244611 / 世界照护者的树皮腰扣 / waist`，metadata 已有 `limit_category=装备唯一：美化（2）`，且 `256656 / 图样：世界照护者的树皮腰扣` 可作为制造业证据；写库前备份 SQLite 到 `/opt/wow-mini-program/backups/wow_news-before-built-in-embellished-crafted-20260625T075431Z.sqlite3`，随后写入 `crafted-governed-244611` source。热部署使用 `WOW_DEPLOY_SKIP_BOOTSTRAP=1 WOW_DEPLOY_START_ASYNC_SYNCS=0 ./server/deploy_lighthouse.sh`，公网 `/health` 通过，`/api/data/health` 可访问，公网 `hunter/beast_mastery`、`hunter/marksmanship`、`hunter/survival`、`shaman/elemental` 的 compact waist payload 均确认 `244611` 返回 `sourceType=crafted` 与 `builtInEmbellishmentLabel=美化`；`gear_catalog` crafted source coverage 从 `41` 增至 `42`。证据：`server/websim_payload.py`、`pages/builds/detail.*`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`、生产 SQLite 查询、生产 `/health`、生产 `/api/data/health`、生产 `/api/websim/gear?class=hunter&spec=marksmanship&compact=1`。

- 2026-06-25：装备强化配置的宝石属性展示修正为当前 live tooltip 证据优先。用户用英雄榜截图核对发现同一宝石 `240983 / 240898` 应展示 `+32主属性`、`+16精通 +7暴击`，而生产库中 Battle.net `preview_item.gem_properties.effect` 仍返回较低旧值；后端新增 20 个 PVE Quality 2/2 宝石 seed 的 server-owned `statSummary/displayLabel` 覆盖，`display_ready_socket_mod_option` 与 `gear_catalog_mod_option_coverage` 在单颗宝石且覆盖值 verified 时优先使用该 tooltip 证据，Battle.net item metadata 继续只作为名称、图标、宝石类型和缓存完整性证据。生产已热部署并在不抓取新网络数据的前提下刷新 20 个 socket seed：`metadataCounts.items=0 / skipped=20 / options=20`，公网 compact payload 的 `neck/finger1/finger2` 均返回 20 个宝石选项，包含 `+32主属性` 与 `+16精通 +7暴击`，不再出现旧的 `+14精通 +6暴击`；`/api/data/health` 中 `modOptionStatus=verified`。生产备份：`/opt/wow-mini-program/backups/wow_news-before-live-gem-tooltips-20260625T060131Z.sqlite3`。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`、生产 `/api/websim/gear?class=mage&spec=frost&compact=1`、生产 `/api/data/health`、Wowhead 当前 tooltip。

- 2026-06-25：装备强化配置从“ID/英文兜底展示”升级为后端 DB-first 的可读证据链。宝石、附魔、美化选项统一由 `websim_gear_mod_options` 输出 `displayLabel/displayKind/displayStatus/evidenceSource`，前端强化 sheet 只展示后端提供的可读证据，不再从 `gem_id/enchant_id/embellishment` 猜文案，也不再显示裸 ID、英文美化名或“待补”配置项。宝石必须有 Battle.net item metadata 与属性摘要才进入可选项，附魔通过 Wago DB2/SimC evidence 补齐中文名，美化 seed 由服务端维护中文 displayName；同一装备可同时保留宝石、附魔、美化三组配置，并继续只保存结构化 `enhancementBySlot` 可执行字段。本轮生产热部署后，socket catalog 已收敛为 20 个 PVE 二星单颗宝石 seed，覆盖项链与双戒指，过滤 observed 多宝石组合与 PVP `241144`；附魔过滤多 ID 组合并删除 Wago 未解析的 observed 行，`modOptionStatus=verified`，线上 compact payload 返回宝石 20、附魔 22、美化 3 类可读选项。生产备份：`/opt/wow-mini-program/backups/wow_news-before-rank-two-gems-20260625T045237Z.sqlite3`、`/opt/wow-mini-program/backups/wow_news-before-readable-mod-options-20260625T050309Z.sqlite3`。证据：`server/websim_payload.py`、`pages/builds/detail.*`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`、生产 `/api/data/health`、生产 `/api/websim/gear?class=mage&spec=frost&compact=1`。

- 2026-06-24：装备强化配置 v1 已完成本地实现并准备合入远端。装备模拟主流程继续保持 16 个 canonical 槽位，前端在装备栏上方新增属性概览，展示整体装等、按职业专精判断的主属性、耐力、急速、暴击、精通、全能，并把 `美化 0/2`、`宝石 0/x`、`附魔 0/x` 和“强化配置”入口收进同一概览区；生命值、法力值、护甲未进入首版展示。强化配置 sheet 按当前已选装备支持能力展示宝石、附魔、美化，达到 2 个美化后禁用其他独立美化，换装备时会重新裁剪不兼容的强化项。保存装备模板时，小程序不再直接拼 SimC profile 字符串，而是保存结构化 `gearBySlot/enhancementBySlot` 快照；后端 `build_websim_profile_response` / SimC 模板确认链路统一把宝石、附魔、美化回写到对应装备行。数据层保留 server-owned 二星美化 seed，`websim_gear_mod_options` 支持 `embellishment`，健康覆盖纳入 `crafted_stats/embellishment`，并增加二星过滤、美化上限、换装备 blocker 与 serializer 测试。证据：[装备强化配置方案](plans/2026-06-24-gear-enhancement-config-design.md)、`server/websim_payload.py`、`server/news_backend.py`、`pages/builds/detail.*`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`tests/builds-page.test.js`。本条为本地实现证据，线上部署和生产 SQLite 入库不在本轮“提交、合入远端”范围内。

- 2026-06-24：制造业装备从“profile 预览子集”扩展到 governed catalog source，并完成当日生产热部署、DB 备份和二次验收；该阶段的完整历史证据保留在实现记录和 git diff 中。后续 2026-06-25 整库审计已修正当时的盾牌误判：`251105 / 破法者之盾` 是副本掉落，不是制造业；当前准确制造业清单、source 数、variant 数和排除清单以本页 2026-06-25 “Midnight 制造业装备库完成整库审计”记录为准。

- 2026-06-24：制造业装备已按 governed catalog source 方案接回自建装备数据库与装备模拟 UI，并完成生产热部署和真实回填。新增 `server/crafted_gear_backfill.py` 从现有 SimC profile preset 的 `crafted_stats` 行构造受控预览导入项，再复用 `backfill_crafted_item_level_variants()` 对每个 `item + track + crafted_stats` 组合跑 SimC JSON 验证；旧 `WOW_WEBSIM_CRAFTED_GEAR_SEED` 仍被忽略。生产写库前已备份 SQLite 到 `/opt/wow-mini-program/backups/wow_news-before-crafted-preview-20260624T082057Z.sqlite3`；最终写入 `15` 个 `source_type='crafted'` source、`96/96` 个 verified crafted variant、`6` 类 `crafted_stats` option，覆盖 `back/feet/legs/neck/off_hand/waist/wrist`，普通制造神话为 `285`，仅 1 件有明确证据的副手生成虚空晋升 `295`。compact 装备 payload 会把内部多条 `crafted_stats` 变体折叠为“装等轨道 + 6 类属性搭配”；读模型修正了 crafted 285 被普通 289 floor 误过滤、以及 crafted variants 未按 `crafted_stats` 保留的问题。小程序槽位 sheet 展示“制造业”来源筛选和计数，选择制造候选后必须同时选中 verified 装等轨道和 verified 属性搭配才能应用/保存，保存模板 metadata 记录已选制造属性但 rawString 仍保持 canonical SimC gear line。生产 `/health` verified，`/api/data/health` 当前 `gear_catalog.partial` 的剩余原因仍为孢陨幽境 `48` 条 `SimC JSON did not include target item stats`；生产 `mage/frost` compact gear 可预览制造项：`neck/back/wrist` 为 `285`，`off_hand` 为 `295/285`，每个轨道都有 6 个 verified 属性搭配；全 `40` class/spec compact traversal 通过，无请求错误、无制造异常装等、无 verified 制造属性缺 `statSummary/statSource`。证据：`server/crafted_gear_backfill.py`、`server/websim_payload.py`、`pages/builds/detail.*`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`、生产 `/api/data/health`、生产 `/api/websim/gear?class=mage&spec=frost&compact=1`、全职业 compact traversal。

- 2026-06-24：装备自建数据库治理阶段性收口并固化 12.1 更新标准。生产 SQLite 只读审计确认官方装等变体不存在 `verified` 缺属性：M+ item-level probe `675` 条 verified，团本 `311` 条 verified / 孢陨幽境 `48` 条 partial blocker，当前职业套装 `206` 条 verified；当前套装覆盖 `65/65` 个部位且非 verified 为 `0`。审计同时发现旧 `needs-variant` 占位、复用旧副本 raw Journal source、旧“虚空强化”标签等残留会污染 health / source count 口径；本轮实现新增同步入口过滤 inactive source、装等回填后清理被新轨道替代的占位，并完成生产定向清理：删除已替代 `needs-variant` `246` 条、旧复用副本 source/variant `520` 条、旧 preview `11` 条、非当前套装残留 source/variant `25` 条，旧“虚空强化”标签 `113` 条归一为“虚空晋升”。清理后生产 official/observed `verified` 缺属性数为 `0`，`needs-variant` 残留、旧 preview、旧虚空标签均为 `0`，Journal loot coverage 为 `307/307` verified，当前 `WOW_CLASSES` `40` 个 compact gear 请求通过。已形成 [装备自建数据库治理标准](gear-database-governance.md)，作为 12.1 大量装备更新时的入库、审计、partial/blocker 上报和发布验收依据。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 SQLite 只读审计、生产 `/api/data/health`、生产 `/api/websim/gear?compact=1` smoke、[装备自建数据库治理标准](gear-database-governance.md)。

- 2026-06-24：当前赛季装备 item-level 入库继续推进到 M+ accepted source-reference 集。生产写库前备份到 `/var/lib/wow-backend/backups/wow_news-before-itemlevel-backfill-20260624-111442.sqlite3`；写入范围为 8 个 M+ 当前赛季实例的 accepted `source_reference/source_discrepancy` rows，以及孢陨幽境 partial blocker rows，显式跳过萨隆矿坑 / 执政团之座 / 通天峰的 `journal_candidate` 与 `excluded_legacy_bucket`。最终新增/重写 `215` 件 / `723` 个 item-level track：M+ `675` 个全部 `verified`，孢陨幽境 `12` 件 / `48` 个 track 全部 `partial`，blocker 为 `SimC JSON did not include target item stats`。随后部署前端 compact 读模型并修正旧的 statless verified 行：生产 `/api/data/health` 当前 variant readiness 为 `1701 verified / 579 partial / 2280 total`，剩余 partial 由旧 `missing deterministic SimC variant preset` `520` 条、孢陨幽境 SimC JSON 缺目标装备属性 `48` 条，以及 `11` 条旧 raid statless verified 修正组成；observed stat coverage 为 `898 / 1369`，剩余 `471` 条 observed 变体缺 SimulationCraft 属性 payload 并已作为 partial/blocker 暴露。生产后端已重启并通过 `/health`，全 `40` class/spec compact gear 请求通过且 `verified` 变体缺属性数为 `0`。证据：[逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md)、生产 `/api/data/health`、生产 SQLite 只读查询、生产 `/api/websim/gear?compact=1` smoke。

- 2026-06-23：虚影尖塔已接入同一条官方掉落 item-level 属性变体入库链路。`server/websim_payload.py` 的 WebSim SimC runner 现在会把服务器 `WOW_BLIZZARD_CLIENT_ID/SECRET` 转成 SimulationCraft 需要的临时 `~/.simc_apikey`，避免新物品触发 BCP item fetch 时 401；生产写库前已备份到 `/var/lib/wow-backend/backups/wow_news-before-voidspire-itemlevel-backfill-20260623-205548.sqlite3`。最终写入虚影尖塔 `60` 件 / `202` 个 verified 变体：`勇士 263`、`英雄 276`、`神话 289` 各 `60` 个，武器/饰品 `虚空强化 298` `22` 个，`0` partial。孢陨幽境 `12` 件已做 dry run，但当前生产 SimC/BCP 对 `268xxx` item document 返回空文档，SimC JSON 无目标装备属性，因此未写入 verified 属性变体，避免把不可计算结果包装成已验证数据。生产 `/api/data/health` 当前 variant readiness 为 `1026 verified / 521 partial / 1547 total`；`39` 个职业专精 compact gear traversal 通过，无 incompatible 候选，仅 `8` 个双手/无副手专精保留合理空副手。证据：[逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md)、`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 `/api/data/health`、生产 `/api/websim/loot?instanceId=1307`。

- 2026-06-23：梦境裂隙与进军奎尔丹纳斯已按逐实例装备入库方案完成属性计算与生产入库。新增 `simulationcraft_item_level_probe` 官方掉落变体写入入口，使用生产 SimC 对已确认 Battle.net raid loot 计算 `勇士 263 / 英雄 276 / 神话 289`，并仅对武器/饰品写入 `虚空强化 298` 特例轨道；变体 payload 保留 `derivedVariantSource`、probe class/spec、SimC gear line、`itemStats` 和 `statSummary`。生产库写入前已备份到 `/var/lib/wow-backend/backups/wow_news-before-raid-itemlevel-backfill-20260623-123445.sqlite3`；最终写入梦境裂隙 `8` 件 / `27` 个 verified 变体、进军奎尔丹纳斯 `24` 件 / `82` 个 verified 变体，合计 `109` 个 verified item-level 变体、`0` partial。生产 `/api/data/health` 当前仍为 partial，但 raid coverage `4/4`、journalLoot verified，剩余 blocker 是其他来源缺口与 socket gem metadata；全 13 职业 / 39 专精 compact gear traversal 通过，无 incompatible 候选，仅 `8` 个双手/无副手专精保留合理空副手。证据：[逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md)、`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 `/api/data/health`、生产 `/api/websim/gear?class=mage&spec=frost&compact=1`。

- 2026-06-23：本赛季 4 个团本装备入库已完成生产部署、受控同步和线上验收。根因是 Battle.net 在 `zh_CN` 下将 journal expansion `505` 命名为 `本赛季`，旧逻辑只匹配 `Current Season` / `Midnight` 后回退到 `地心之战`，导致错季 `Manaforge Omega` 残留；现已部署 selector 修复和中文团本名 canonical 化，并用 service credentials 受控同步 4 个团本。生产 `/api/game/season` 当前返回 verified refs：`1307 / The Voidspire / 虚影尖塔`、`1314 / The Dreamrift / 梦境裂隙`、`1308 / March on Quel'Danas / 进军奎尔丹纳斯`、`1305 / Sporefall / 孢陨幽境`；`/api/data/health` 显示 raid coverage `expected 4 / covered 4 / missing 0`、raid `sourceItemCount=104`、journalLoot verified，旧 `1302 / 法力熔炉：欧米伽` 缓存已删除。装备库整体仍为 partial，剩余 blocker 是 deterministic SimC variant preset、非本季/observed-only 可信来源缺口和 socket gem metadata，不是团本覆盖缺口。证据：[逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md)、`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 `/api/game/season`、生产 `/api/data/health`、生产 `/api/websim/loot`。

- 2026-06-23：当前工作树变更已完成提交前梳理，范围覆盖 Review Workflow、热部署流程、当前赛季池权威化、装备来源可信分层、复用旧副本三层校验、observed gear SimulationCraft 属性来源、Raider.IO 同步保护、职业装备页可信状态展示、health 状态选择和对应测试扩展。生产已完成复用旧副本 source-reference 写入与 `/api/data/health` stale gearCatalog 状态修正，当前 M+ 覆盖 `8/8`、M+ `sourceItemCount=203`，全 13 职业 / 39 专精 compact gear traversal 通过；团本入库仍不在本轮范围。证据：[当前变更收口清单](plans/2026-06-23-current-change-rollup.md)、[逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md)、`server/websim_payload.py`、`server/news_backend.py`、`pages/builds/detail.js`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`tests/builds-page.test.js`。

- 2026-06-23：复用旧副本当前赛季装备校验方案已补录并完成三本线上 source-reference 写入。后续 Pit of Saron、Seat of the Triumvirate、Skyreach 这类复用旧副本不能再把 raw Battle.net Journal 行数当作 accepted loot；必须同时记录 `raw_journal / journal_candidate` 上限、`observed_confirmed` 下限和 `source_reference` 当前赛季参考表中间口径。Pit of Saron 当前修正为 `74 raw -> 49 journal_candidate -> 24 source_reference`；Seat of the Triumvirate 当前修正为 `44 raw -> 44 journal_candidate -> 35 source_reference`，其中 `258523 / 奈扎尔的虚空爪` 以物品页交叉证据标记为 `source_discrepancy` 纳入；Skyreach 当前修正为 `230 raw -> 28 source_reference`，其余 202 行保留为 `excluded_legacy_bucket`。所有结果仍只是人工复核候选，缺 deterministic SimC variant preset 时保持 `partial`。证据：[逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md)、[复用旧副本候选表](plans/2026-06-23-reused-dungeon-current-season-candidates.md)。

- 2026-06-23：当前赛季装备升级轨道与 Windrunner Spire 首个实例切片口径已完成人工确认。普通满级轨道固定为 `Champion 6/6 = ilvl 263`、`Hero 6/6 = ilvl 276`、`Myth 6/6 = ilvl 289`；`ilvl 298` 不再作为普通 Myth 满级，而是仅作为 Ascendant Voidcore 武器/饰品或 Sporefall Sporefused 掉落等特例轨道单独建模。风行者之塔 26 件装备已用生产 SimC `1205-01 / 12.0.5.67823` 按 `itemId + ilevel` 计算勇士/英雄/神话三档属性，武器/饰品另算 298 特例候选；用户抽样人工核对确认属性正确。该计算结果只作为人工核对与后续补库输入，缺 deterministic SimC variant preset 的装备仍保持 `partial`，不能因此伪装为 `verified`。证据：[逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md)、生产 `/opt/wow-mini-program/server/data/wow_news.sqlite3` 只读查询、生产 `/opt/wow-simc/current/simc` 批量探针、用户人工核对反馈。

- 2026-06-23：装备模拟数据可信度完成新一轮生产收口。Review Workflow 已收敛为本地测试、smoke、diff review、运行日志和用户指定 review 工具；WebSim 装备来源展示不再把 Raider.IO observed profile 当作可信掉落来源，observed-only 候选统一显示为“来源待补充/来源待补”，并在 health 中以 `sourceStatus=partial` 暴露真实来源缺口；新增职业武器熟练度过滤后，法师、死亡骑士等专精不再出现 Crossbow / Fist Weapon 等不可装备主手候选。用户授权后完成生产同步与部署，`/api/data/health` 显示 Battle.net item metadata verified、socket 37 个 option 覆盖 16 槽、Raider.IO verified，剩余主要 blocker 收敛为 `missing deterministic SimC variant preset`。生产全职业验收遍历 13 职业 / 39 专精通过：16 槽 replacementCandidates 均可返回，空副手只出现在双手/无副手合理场景，无 incompatible 候选、无 observed 来源伪装、无已知错误披风回流。证据：`AGENTS.md`、`pages/builds/detail.js`、`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 `/api/data/health`、生产 `/api/websim/gear?class=mage&spec=frost&compact=1`、全职业 `/api/websim/gear` compact 遍历脚本输出。

- 2026-06-22：装备 observed variant 属性生产链路已改为 SimC JSON gear output。`sync_observed_gear_variants` 现在可从 profile/aggregate 携带的 SimC JSON 中解析每槽 itemId、装等、bonus/gem/enchant 和属性，将匹配到的 `itemStats/statSummary/statSource=simulationcraft` 写入 observed variant payload；Raider.IO/WCL 角色 API 只保留为观测配置、角色链接和校验引用，不再作为生产属性来源。生产已热部署并增量回填：`/api/data/health` 显示 observed stats coverage 为 271/271、非本季 observed 为 0、Battle.net item metadata 为 625/625 verified；线上 `/api/websim/gear?class=shaman&spec=elemental&compact=1` 对 `249979` 通过 promoted `tier_set` variant 返回装等 289、`verified_variant`、`statSource=simulationcraft` 和 `敏捷 or 智力 124；耐力 1768；急速 55；精通 109`。全职业 smoke 验证 13 职业 / 39 专精的 16 槽均非空、无 incompatible 候选，309 个 verified variant stats 均来自 SimulationCraft。证据：`server/websim_payload.py`、`server/gear_observed_backfill.py`、`tests/websim_payload_test.py`、`tests/gear_observed_backfill_test.py`、生产 `/api/data/health`、生产 `/api/websim/gear?class=shaman&spec=elemental&compact=1`。

- 2026-06-22：装备数据层长 goal 已补充独立交接文档 `GEAR-DATA-LAYER-HANDOFF-2026-06-22`，明确当前交付边界、生产健康快照、已验证能力和后续 ToDo。该 goal 的正确收尾状态是 blocked/handoff，而不是 complete：自有赛季装备数据层已达到 `dataReadiness=verified`，但官方来源仍有 382 个变体缺 `missing deterministic SimC variant preset`；下一步应以游标化 Raider.IO/WCL observed backfill 和独立后台 job 继续推进，避免再把低命中率扩扫绑在同一个长同步目标里。证据：[2026-06-22-gear-data-layer-goal-handoff.md](plans/2026-06-22-gear-data-layer-goal-handoff.md)。

- 2026-06-22：炸鸡队长后台架构 v1 骨架已落地。新增独立 `/api/chickenbro/*` 后端边界，承载会话、消息、任务轮询和专精打法画像读取；新增 `chickenbro_sessions`、`chickenbro_messages`、`agent_jobs`、`chickenbro_spec_profiles` 和 `chickenbro_user_profiles`，把用户可见对话、后台任务、Codex job 目录和可重建画像分开；CodexCli 只接收后端裁剪的 bounded context，输出必须通过证据引用、数字白名单和话题边界校验，失败时降级为后端确定性回答；`published` 画像可支撑结论，`partial` 只做背景，`stale/blocked/needs_review` 不进入结论。证据：`server/news_backend.py`、`tests/news_backend_test.py`、`docs/database-architecture.md`、`docs/roadmap/ideas.md`。

- 2026-06-22：装备数据层长任务收尾到可交接状态。`gear_catalog_refresh_reason` 已收窄为只在 dataReadiness/source/metadata/modOption 数据层不健康时触发 Blizzard 全刷新，避免仅因 `simulationReadiness.partial / missing deterministic SimC variant preset` 反复重拉 Battle.net Journal；临时库完整探针验证 WebSim 阶段可在约 115s 完成，Blizzard 阶段以 `fresh-season-cache` 跳过 Journal 后仅约 2.4s，剩余长尾明确落在 Raider.IO target profile 低命中率扩扫。Raider.IO profile 单请求 timeout 已降级为 batch error，不再让整轮同步异常穿透；生产曾被旧 service 低预算同步降级，已从 `wow_news.sqlite3.before-service-500-800-.bak` 恢复并重新部署 `server/websim_payload.py`、`server/raiderio_payload.py`、`server/websim_sync.py`、`server/news_backend.py` 与新版 `wow-websim-sync.service`。当前生产 `/api/data/health` 为 959 item / 1649 source / 1492 official variant / 1110 verified / 382 partial，observed 2425/2425，dataReadiness verified，唯一 blocker 仍是 `missing deterministic SimC variant preset`；下一步不再扩大同一 goal，而是拆成独立后台 job/cursor 化处理 Raider.IO/WCL observed 变体反哺。证据：`server/websim_payload.py`、`server/raiderio_payload.py`、`server/websim_sync.py`、`server/news_backend.py`、`server/wow-websim-sync.service`、`tests/websim_payload_test.py`、`tests/raiderio_payload_test.py`、生产 `/api/data/health`、生产 `/tmp/websim-full-probe.*`。

- 2026-06-22：WebSim 总同步新增阶段级进度日志与安全 smoke 开关，解决长任务运行时“只能等最终 JSON、无法知道卡在哪”的问题。`sync_websim_cache` 现在接受 `stage_callback` 并在 `simc`、`blizzard`、`gear_catalog`、`observed_item_metadata`、`gear_mod_option_metadata`、`websim_sync` 等阶段写入 start/complete 事件，`websim_sync.py` 会把阶段事件以 stderr JSON Lines 输出到 systemd journal，最终汇总仍保留在 stdout；同时新增 `WOW_WEBSIM_SKIP_RAIDERIO=1`，让生产 smoke 可只验证 WebSim 阶段链路而不触发 Raider.IO 网络扩扫。生产已部署 `server/websim_payload.py` 与 `server/websim_sync.py`，`wow-backend=active`，生产 `/api/data/health` 保持 959 item / 1649 source / 1492 official variant / 1110 verified / 382 partial，observed 2425/2425，dataReadiness verified。使用生产库拷贝 `/tmp/wow_websim_stage_smoke.sqlite3` 跑 `WOW_WEBSIM_SKIP_BLIZZARD=1 WOW_WEBSIM_SKIP_RAIDERIO=1 python3 server/websim_sync.py`，stderr 实时输出 `websim start`、`simc start/complete`、`gear_catalog start/complete`、`websim complete`、`raiderio skipped`；本轮临时库 smoke 总耗时 71.006s，其中 SimC 数据导入 42.483s、gear catalog 27.029s，输出仍为 959 item / 1492 official variant / 2425 observed variant。下一步用这套 journal 证据跑一次受控完整同步，定位包含 Blizzard 与 Raider.IO 的真实长尾阶段，再决定是否补 per-stage deadline 或拆成独立 systemd job。证据：`server/websim_payload.py`、`server/websim_sync.py`、`tests/websim_payload_test.py`、`tests/websim_sync_test.py`、生产 `/tmp/websim-stage-smoke.err` / `/tmp/websim-stage-smoke.out`、生产 `/api/data/health`。

- 2026-06-22：带 Raider.IO deadline 的生产真实同步验证显示，当前长耗时不只在 profile 扩扫层。部署 `WOW_RAIDERIO_PROFILE_WORKERS=8` 与 `WOW_RAIDERIO_SYNC_DEADLINE_SECONDS=900` 后再次触发 `wow-websim-sync.service`，服务运行约 19 分钟仍未自然退出，手动停止后 `wow-backend=active`、`wow-websim-sync.timer=active`、`wow-websim-sync.service=inactive`，生产 `/api/data/health` 仍保持 959 item / 1649 source / 1492 official variant / 1110 verified / 382 partial，observed 2425/2425，dataReadiness verified，说明 observed 降级保护已挡住半重建污染。下一步需要把 WebSim 总同步拆出阶段级 deadline 与进度日志，定位 Raider.IO deadline 后的长尾停留点，而不是继续盲目放大 profile 扩扫预算。证据：生产 `wow-websim-sync.service` journal、生产 `/api/data/health`、`server/raiderio_payload.py`、`server/websim_payload.py`。

- 2026-06-22：Raider.IO observed 反哺链路新增并发扩扫、全局 deadline 与防降级保护。`fetch_profiles_for_runs` 现在支持 `WOW_RAIDERIO_PROFILE_WORKERS` 并行抓取 profile，`sync_raiderio_cache` 新增 `WOW_RAIDERIO_SYNC_DEADLINE_SECONDS` 全局预算，超时会写入 `Raider.IO sync deadline exceeded` 并将 payload 标为 partial；`sync_observed_gear_variants` 现在在新 payload 的 verified observed 数量少于现有 verified cache 时默认保留旧 cache，避免真实扩扫被中断或只拿到小样本时把 2425 条 observed variant 降级成较小集合。生产验证中，8 worker 的 500/800 扩扫仍在 20 分钟后未自然收束，手动停止后暴露出 observed cache 与 socket option metadata 会被半重建污染；已将事故 DB 备份为 `/opt/wow-mini-program/server/data/wow_news.sqlite3.aborted-raiderio-workers-20260622111401.bak`，并从 `wow_news.sqlite3.before-service-500-800-.bak` 恢复生产。当前生产已部署 `WOW_RAIDERIO_PROFILE_WORKERS=8` 与 `WOW_RAIDERIO_SYNC_DEADLINE_SECONDS=900`，`/api/data/health` 恢复为 959 item / 1649 source / 1492 official variant / 1110 verified / 382 partial，observed 2425/2425，dataReadiness verified；剩余 blocker 仍是 `missing deterministic SimC variant preset`。证据：`server/raiderio_payload.py`、`server/websim_payload.py`、`server/wow-websim-sync.service`、`tests/raiderio_payload_test.py`、`tests/websim_payload_test.py`、生产 `/api/data/health`、生产事故 DB 备份。

- 2026-06-22：装备数据层新增 Blizzard Journal 缓存防中断保护。`sync_blizzard_journal` 现在先写入本轮 `updated_at` 数据，只有在本轮 journal 刷新没有 blocker 时才清理旧的 `websim_instances` / `websim_encounters` / `websim_loot` 行，避免长时间同步被手动终止或外部中断后把已验证的大秘境、团本、套装掉落缓存清空。新增回归用例 `test_sync_blizzard_journal_preserves_existing_cache_when_aborted_mid_refresh` 覆盖 `KeyboardInterrupt` 中断场景；本地 `python -m py_compile server/websim_payload.py`、`python -m unittest tests.websim_payload_test -v`、`git diff --check` 已通过（仅 Git CRLF 提示）。生产已热部署 `server/websim_payload.py` 并重启 `wow-backend`，复核 `wow-backend=active`、`wow-websim-sync.service=inactive`、`wow-websim-sync.timer=active`，生产 `/api/data/health` 仍为 `gear_catalog.partial` 且 dataReadiness verified：959 item / 1649 source / 1492 official variant / 1110 verified / 382 partial，journalLoot 783/783，Battle.net metadata 959/959 verified，属性、护甲、武器、槽位 mismatch 均为 0；剩余唯一 blocker 仍是 382 个官方来源变体缺 `missing deterministic SimC variant preset`。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 `/api/data/health`、生产 `wow-backend` / `wow-websim-sync.timer` 状态。

- 2026-06-22：装备数据层完成 Raider.IO target fair scan、observed-only PVP 套装过滤、官方/观测 readiness 分层与 partial 源保护收敛。`db_target_item_ids` 现在不再按低 itemId 截断，而是按副本来源桶与槽位 round-robin 选择 partial dungeon/raid/tier_set target；`sync_raiderio_cache` 在存在 target item 时不再因职业覆盖已满而提前停止翻页。生产定向补扫使用 `WOW_RAIDERIO_RUN_PAGES=20 / TARGET_ITEM_LIMIT=240 / TARGET_PROFILE_LIMIT=400 / PROFILE_LIMIT=160 / PROFILE_LIMIT_PER_SPEC=4`，实际读取 400 条 runs、442 个 profile，target 命中从上一轮 4 件推进到 21 件；其中 `49807 / Krick's Beetle Stabber` 通过同一单手武器 main_hand/off_hand 兼容规则完成 observed promotion，官方 partial 从 383 收敛到 382。生产 `/api/data/health` 当前把官方可模拟层与 Raider.IO observed 证据层分开计数：`gear_catalog` 为 959 item / 1649 source / 1492 official variant / 1110 official verified / 382 official partial，另有 2425 个 observed_profile verified variant 只作为证据与反哺来源，不再污染 official simulation readiness；若新一轮 Raider.IO payload 的 `sourceStatus=partial`，`sync_observed_gear_variants` 会保留既有 verified observed cache 而不是删除重建。生产 health 当前显示 dataReadiness verified：大秘境 8/8、团本 4/4、journalLoot 783/783、套装 expected 5 / discovered 18 / verified 18 / missing 0；`itemMetadata.status=verified`，959/959 来自 Battle.net Game Data API，missing verified/stat/armor/weapon/slot mismatch/stat mismatch 均为 0；socket 38 个 option 覆盖 16 槽，enchant 53 个 option 覆盖 8 槽。observed_profile 中的 `245752 / 角斗士的荣耀` 被确认是 Raider.IO 玩家身上的 PVP/角斗士套装，health 现在只把官方 dungeon/raid/tier_set 来源中的 discovered item set 纳入 PVE 赛季套装详情门禁，避免 observed-only PVP 装备误伤自有赛季库。剩余唯一官方 blocker 仍是 382 个官方来源变体缺 `missing deterministic SimC variant preset`；抽样 `133505/151303/251079/251206/49803` 证明 Battle.net dungeon preview 仍是旧资料片低等级 preview，不能硬升为当前赛季 SimC 变体，后续仍需 Raider.IO/WCL observed 样本或更权威的当前赛季 bonus/upgrade 来源。`wow-websim-sync.service` 已将 Raider.IO 扫描预算提升到 `TARGET_ITEM_LIMIT=500 / TARGET_PROFILE_LIMIT=800 / TIMEOUT_SECONDS=45`；本轮人工 500/800 扩采因 20 分钟无完成信号被停止，生产 DB 已从启动前备份恢复，因此不作为完成证据。证据：`server/raiderio_payload.py`、`server/websim_payload.py`、`server/wow-websim-sync.service`、`tests/raiderio_payload_test.py`、`tests/websim_payload_test.py`、`tests/deploy_lighthouse_test.py`、生产 `/api/data/health`、Raider.IO `mythic-plus/runs` 与 `characters/profile?fields=gear`。
- 2026-06-22：装备数据层新增 Battle.net official preview 变体 promotion。`sync_websim_gear_catalog` 现在会把官方 raid / tier_set source-only partial 中带可信 Battle.net `level` / `preview_item.bonus_list` 的条目提升为 `battle_net_preview` verified variant；无 bonus 的官方 preview 明确标记 `simcIlevelOnly=true`，并在候选 readiness 中允许 `id + ilevel` 作为可执行 SimC 最小变体，同时保留地下城低等级 preview 的保护阈值，避免把旧本静态 preview 误当作当前赛季 M+ 实例化装等。生产 full sync 2026-06-22 06:28:22-07:15:34 CST 成功退出后，`gear_catalog` 为 931 item / 1413 source / 2560 variant / 2150 verified / 410 partial；新增 `battle_net_preview` verified raid 294、tier_set 18；剩余 partial 分布为 dungeon 406、tier_set 3、raid 1，唯一 blocker 仍是 `missing deterministic SimC variant preset`。Battle.net metadata 审计保持 verified：missing verified/stat/armor/weapon/slot mismatch 均为 0；socket 30 个 option 覆盖 16 槽且无 gem metadata blocker。生产 `/api/websim/gear?class=mage&spec=frost&compact=1` 已出现 97 个 `simcIlevelOnly` 且 `simcReady=true` 的 preview 候选；生产 SimC `1205-01 / 12.0.5.67823` 对 `head=...,id=237548,ilevel=662` 退出码 0，仅返回名称不一致 trivial warning。Raider.IO public profile API 复核样本包括 `Kirikosuzu@rhonin` 的 `Weight of Command` itemId 249293 / ilvl 298 / bonus `6652,13335,13654` / enchant `8039,8052`，以及 `Mjtdknight@pandaren` 的 head/neck/chest/waist 真实装备样本，继续作为 observed promotion 的真实实例来源。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`tests/raiderio_payload_test.py`、`tests/deploy_lighthouse_test.py`、生产 `/api/data/health`、生产 `wow-websim-sync.service` journal、Raider.IO `characters/profile?fields=gear`。
- 2026-06-22：装备数据层完成 observed gear Battle.net metadata 回填、套装详情补齐与类型/stat evidence 收敛。新增 `sync_blizzard_observed_item_metadata` 后，生产定向同步先把 observed/source_reference 装备从 55 个 missing verified metadata 收敛到 0，并将 `WOW_WEBSIM_SYNC_OBSERVED_ITEM_LIMIT` 提升到 300，避免 120 预算截断真实 Raider.IO 样本；随后补齐 Battle.net item-set 1982，`itemSets=18 / setItems=78 / missingSetDetailCount=0`。类型审计补齐 Battle.net `Warglaive` subclass 与 `HOLDABLE` 副手物品，官方 trinket spell/effect 与 cosmetic/statless 装备不再被误判为缺 stats。生产 full sync 2026-06-22 05:08:57-05:57:58 CST 成功退出后，`gear_catalog` 为 939 item / 1482 source / 2725 variant / 2018 verified / 707 partial；`itemMetadata.status=verified`，`missingVerifiedItemCount=0`、`missingStatItemCount=0`、`missingArmorTypeItemCount=0`、`missingWeaponTypeItemCount=0`、`slotMismatchCount=0`；当前赛季大秘境覆盖 8/8、当前资料片团本覆盖 4/4、`journalLoot` 783/783 verified、套装 expected 5 / discovered 18 / verified 18 / missing 0；socket 30 个 option 覆盖 16 槽且无 gem metadata blocker。剩余唯一 blocker 是 707 个官方 dungeon/raid/tier_set source-only 变体仍缺真实可执行 `ilevel + bonus_id/gem_id/enchant_id` SimC preset，需要继续用 Raider.IO/WCL observed 样本或更权威的实例化变体来源反哺。证据：`server/websim_payload.py`、`server/wow-websim-sync.service`、`tests/websim_payload_test.py`、`tests/deploy_lighthouse_test.py`、生产 `/api/data/health`、生产 `wow-websim-sync.service` journal。
- 2026-06-22：装备数据层完成生产 Battle.net journal 全量预算修复。`wow-websim-sync.service` 现在通过 `ExecStart=/usr/bin/env ...` 在 Python 子进程运行时强制 `WOW_WEBSIM_SYNC_INSTANCE_LIMIT=20 / RAID_INSTANCE_LIMIT=8 / ENCOUNTER_LIMIT=200 / ITEM_LIMIT=1200 / ITEM_SET_LIMIT=40`，避免 `/etc/wow-backend.env` 里的旧低预算覆盖 systemd `Environment=`；生产同步 2026-06-22 02:32:08-03:19:08 CST 成功退出后，`websim_sync.blizzard.limits` 记录为 20/8/200/1200，`truncation` 全部为 0，当前赛季大秘境覆盖 8/8、当前资料片团本覆盖 4/4，`journalLoot` 达到 783/783 verified，`gear_catalog` 推进到 920 item / 1331 source / 2137 variant / 1436 verified / 701 partial，`slotMismatchCount=0`，socket 30 个 option 覆盖 16 槽，enchant 44 个 option 覆盖 8 槽。剩余 blocker 已收敛为 5 个当前赛季 item set 与 17 个 discovered item set 缺数字 Battle.net item-set id、52/55 个 metadata/stat 缺口、34/26 个护甲/武器类型缺口，以及 701 个仍缺 deterministic SimC variant preset；非数字套装名现在不再拼坏 URL，而是记录 `missing numeric Battle.net item-set id`。证据：`server/websim_payload.py`、`server/wow-websim-sync.service`、`tests/websim_payload_test.py`、`tests/deploy_lighthouse_test.py`、生产 `/api/data/health`。
- 2026-06-22：装备数据层修复 Battle.net 槽位审计被旧 `websim_items.slot` 污染的问题。生产真实同步后暴露出 `slotMismatchCount=119`，示例显示项链、胸甲、戒指、护腕等 Battle.net payload 明确带 `inventory_type.type=NECK/CHEST/FINGER/WRIST` 的物品，被旧行 slot 误判为 `trinket1`；`gear_catalog_item_metadata_audit` 现在以 Battle.net payload 推导官方 expected slot，并且 `build_gear_catalog_sync_state` 会按 payload 回写修正历史脏 slot。同步发现的另一个解析缺口是 observed-only `MAIN_HAND/OFF_HAND` 曾被文本 fallback 误判为 `hands`，现在已显式映射为 `main_hand/off_hand`，避免武器被计入护甲槽位缺类型。二次生产同步后旧异常行已回写为 `neck/chest/finger1/wrist`，`gear_catalog.details.itemMetadata.slotMismatchCount=0`，假的 slot mismatch blocker 消失。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、生产 `/api/data/health`。
- 2026-06-22：生产真实同步验证了 Raider.IO target coverage 与 observed promotion 链路。热部署后触发 `wow-websim-sync.service`，线上 `gear_catalog` 从 237 item / 643 variant / 603 verified / 40 partial 推进到 251 item / 1279 variant / 1248 verified / 31 partial；宝石/附魔 option 增至 socket 30、enchant 44，并且 16 个装备槽位都有 socket option 覆盖。`raiderio.details.targetItemCoverage` 现在线上可见，当前剩余 31 个 partial target 暂未被本轮 120 个额外 profile 命中；上一轮命中的 `251171 Enthralled Bonespines` 已被 observed promotion 消化，不再出现在剩余 target 中。剩余 blocker 已收敛为真实数据缺口：Battle.net journal 同步 limit 截断、6 个当前赛季地城缺掉落、4 个当前资料片 raid 缺掉落、13 个 item set 缺详情、44 个 journal loot item 缺本地缓存，以及 31 个仍缺 deterministic SimC variant preset。证据：`server/websim_payload.py`、`server/raiderio_payload.py`、`server/news_backend.py`、生产 `/api/data/health`。
- 2026-06-22：数据健康接口新增 Raider.IO target item coverage 透出。`/api/data/health` 的 `raiderio.details` 现在会保留缓存 payload 中的 `targetItemCoverage`，包括当前 partial 装备 target 总数、已命中的真实 profile itemId、仍缺失的 itemId、本轮 target profile 扫描预算，以及 `matchedTargetItemExamples` 中的角色、槽位、装等、bonus、宝石和附魔样本；这样后续同步不再只能看 `profileCount` 是否增长，而是可以直接判断 Raider.IO 真实 API 样本是否正在把自有赛季库里的 partial dungeon/raid/tier_set 装备推进到可校验状态。证据：`server/news_backend.py`、`server/raiderio_payload.py`、`tests/news_backend_test.py`、`tests/raiderio_payload_test.py`。
- 2026-06-22：装备数据层新增 Raider.IO partial item 定向扩扫。`sync_raiderio_cache` 现在会把环境变量 `WOW_RAIDERIO_TARGET_ITEM_IDS` 与当前 DB 中 `websim_gear_variants.status='partial'` 且来源为 dungeon/raid/tier_set 的 itemId 合并成 target 列表；在基础每专精 profile 采样之后，会按 `WOW_RAIDERIO_TARGET_PROFILE_LIMIT` 继续扫描榜单 roster 中尚未请求过的角色，直到命中目标 item 或达到上限，并在 payload 中输出 `targetItemCoverage.targetItemCount / matchedTargetItemIds / missingTargetItemIds / targetProfileRequestLimit`。该扩扫默认预算为 120 个额外 profile，`WOW_RAIDERIO_TARGET_PROFILE_LIMIT=0` 可显式关闭；这样线上 35 个 official dungeon partial item 不再只能等待随机 profile 命中，而是能被后续同步轮次定向寻找 Raider.IO 真实装备实例。证据：`server/raiderio_payload.py`、`tests/raiderio_payload_test.py`。
- 2026-06-21：装备数据层新增 Raider.IO public profile 样本反哺入口。对线上 40 个 partial variant 做全专精只读扫描后，收敛出 35 个唯一 official dungeon itemId，并用 Raider.IO 公开 M+ runs/profile API 扫描 160 条国服榜单记录、180 个角色 profile，命中 `251111 Splitshroud Stinger`、`251166 Falconer's Cinch`、`251171 Enthralled Bonespines` 等真实实例化样本；`raiderio_payload.api_get` 现在没有 `WOW_RAIDERIO_API_KEY` 时也会尝试公开 API，`get_raiderio_payload` 不再直接返回 missing_credentials，`sync_observed_gear_variants` 也会导入 Raider.IO payload 顶层 `profiles[].gear`，避免只依赖被每专精 aggregate 截断后的 `observedGearProfiles`。证据：`server/raiderio_payload.py`、`server/websim_payload.py`、`tests/raiderio_payload_test.py`、`tests/websim_payload_test.py`、Raider.IO `mythic-plus/runs` 与 `characters/profile?fields=gear` 公开 API 采样。
- 2026-06-21：装备数据层新增 simulation readiness 示例追踪。`gear_catalog.details.simulationReadiness` 现在除 verified/partial/blocked 计数和 blockers 外，还输出 `partialExamples` / `blockedExamples`，每条示例带 `variantId / itemId / slot / sourceType / sourceLabel / blockers`。这样线上仍剩的 40 个 `missing deterministic SimC variant preset` 不再只是一个汇总数字，后续可以直接按物品与副本来源去扩 Raider.IO/WCL observed 样本或接入更权威的实例化变体来源。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Raider.IO observed 附魔选项派生。用真实角色 `Supermono@isillien` 的 Raider.IO profile 对齐字段形状：戒指装备会同时提供裸数组 `gems: [240894]` 与 `enchants: [7967] / enchant: 7967`；`sync_observed_gear_variants` 归一化出 verified SimC 变体后，`sync_websim_gear_mod_options` 现在不只派生 `observed-socket-*` 宝石 option，也会派生 `observed-enchant-*` 附魔 option，并按真实槽位及等价戒指槽位写入可选项库。这样自有装备数据层对真实玩家样本里的插槽/附魔字段覆盖更完整，不再只能依赖人工 seed。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、Raider.IO `characters/profile?region=cn&realm=isillien&name=Supermono&fields=gear`。
- 2026-06-21：装备数据层新增 data readiness 与 simulation readiness 拆分。线上 `gear_catalog=partial` 的主要 blocker 是 40 个官方 dungeon loot 只有 Battle.net 来源 / metadata / stats，但没有确定 `ilevel + bonus_id/gem_id/enchant_id` 的可执行 SimC 变体；这不是同一种缺口。`gear_catalog.details.dataReadiness` 现在只看官方 metadata、属性、护甲/武器类型、赛季来源、套装来源和宝石/附魔选项数据完整性；`gear_catalog.details.simulationReadiness` 单独表达变体是否可执行，并保留 `missing deterministic SimC variant preset`。overall `gear_catalog.status` 仍保持严格 partial，避免把不能模拟的 source-only 掉落伪装成可执行装备。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、线上 `/api/data/health` 的 40 个 partial variant blocker。
- 2026-06-21：装备数据层新增职业武器类型兼容门槛。真实线上法师装备候选中暴露出 Battle.net 官方掉落盾牌 `破法者之盾` 进入 `off_hand` 候选的问题；现在 Battle.net Armor subclass `Shield` 会被规范化为 `weaponType=Shield`，并在 catalog 候选归一化时按职业过滤，只有圣骑士、萨满、战士可保留盾牌候选，其它职业会被判定为 incompatible 并从可替换装备列表移除。这样“保存正确武器类型”不仅进入 metadata，也会真正参与装备模拟候选可用性判断。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、线上 `/api/websim/gear?class=mage&spec=frost` 的盾牌 off-hand 案例。
- 2026-06-21：装备数据层新增候选可应用的官方校验门槛。线上真实 payload 中已经能看到 Raider.IO observed-only 装备会以 `Unknown/source_reference` 形式进入候选并带有可执行 SimC 变体，但这类数据缺 Battle.net 官方 metadata、preview stats 与护甲/武器类型校验，不能满足“自有赛季装备库可用”的要求；`enrich_catalog_item` 现在保留 observed variant 作为证据，但 catalog 候选只有在 `metadataStatus=verified`、来源为 Battle.net Game Data API、属性存在且护甲/武器槽位类型已校验时才允许 `simcReady=true`，否则输出 `verified Battle.net metadata / Battle.net item stats / Battle.net armor type / Battle.net weapon type` blockers。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、线上 `/api/websim/gear?class=mage&spec=frost` 的 Raider.IO observed-only `Unknown` 案例。
- 2026-06-21：装备数据层新增 health 对当前 SQLite 事实的强制重审计。`gear_catalog_health_payload` 现在不再让旧 `gearCatalog` sync state 中的 `status / itemMetadata / blockers` 覆盖当前数据库审计结果，只保留 checkedAt 与 revision 这类快照信息；`gear_catalog_item_metadata_audit` 的基准也从已有 `websim_items` 行扩展为 `websim_gear_sources / websim_gear_variants / websim_loot` 中出现的全部 itemId 并 left join metadata。这样 catalog source/variant 已经入库但 Battle.net metadata 行完全缺失的装备，会被计入 `missingVerifiedItemCount` 并阻断 verified，避免旧快照把当前自有库缺口遮住。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net item metadata 刷新失败时的可信缓存复用。`sync_blizzard_journal` 现在遇到单个 item metadata API 失败时，如果 `websim_items` 中已经有 verified 且可判定为装备的 Battle.net metadata，会复用本地缓存继续写入当前 `websim_loot`，并计入 `cachedItems`；同时仍记录 `fetchFailures` 和 `Battle.net journal item {id} fetch failed` blocker，表示本轮刷新不完整。这样外部 API 短暂抖动不会把已经校验过的赛季掉落来源清掉，也不会把刷新失败伪装成完整 verified。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal item metadata 拉取失败门禁。`sync_blizzard_journal` 现在在单个 `/data/wow/item/{id}` metadata 拉取失败时不再中断整轮副本 / 团本掉落同步，而是记录 `fetchFailures` 中的 item、encounterId、instanceId，并产出 `Battle.net journal item {id} fetch failed` blocker；失败 item 不写入 verified `websim_loot`，同一首领其它已验证装备仍可入库。这个门禁对应真实 Raider.IO observed 样本里可能出现的 `Unknown`/缺官方 metadata 装备：observed 只提供可执行变体线索，官方属性、护甲/武器类型和来源未校验前不能让装备库升为 verified。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal encounter 拉取失败门禁。`sync_blizzard_journal` 现在在 `/data/wow/journal-encounter/{id}` 失败时不再让整轮副本 / 团本同步直接抛出并中断，而是记录 `fetchFailures`，产出 `Battle.net journal encounter {id} fetch failed` blocker，并继续同步同一 instance 下其它首领掉落；这样单个首领详情 API 抖动时，已拉到的其它首领装备仍可入库，同时装备库不会误判为完整。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal raid 枚举失败门禁。`sync_blizzard_journal` 现在调用 `selected_journal_instance_refs` 拉取当前资料片 / 团本 refs 失败时，不再静默把 raid 列表当空集合继续同步，而是写入 `fetchFailures`，并产出 `Battle.net journal raid_selection journal-expansion fetch failed` blocker；`sync_websim_cache` 会继承该 blocker，使团本索引 API 失败时无法误判为“当前赛季无缺失 raid 掉落”。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层修复 Raider.IO 裸 gem id 污染 SimC 宝石字段。`observed_gear_simc_options` 现在只把 `gems: [240916]` 这类裸数组值写入 `gem_id`，不会再把同一个宝石物品 ID 误写成 `gem_bonus_id` 或 `gem_ilevel`；只有结构化 gem 对象明确带 `bonusId / itemLevel` 时才生成对应 SimC 字段。这样 observed 变体、socket option 派生和官方掉落反哺不会被错误宝石 bonus/装等污染，避免生成看似可执行但语义错误的装备行。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增官方掉落变体的真实 observed 反哺。`sync_websim_gear_catalog` 现在会先按 Battle.net journal 写入大秘境/团本/套装官方掉落来源，再用同一 itemId 且槽位兼容的 verified Raider.IO observed profile 变体补齐官方来源下的可执行 `itemLevel + bonus_id/gem_id/enchant_id` variant；被真实样本覆盖的 `loot-partial-* / set-partial-*` 不再保留 `missing deterministic SimC variant preset` blocker，payload 会保留 `observedVariantId / observedProfileRefs` 作为证据指针。这样不会猜测 SimC bonus，而是把“官方掉落来源”和“真实玩家可执行变体”合并成可运营装备库证据。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal instance 拉取失败门禁。`sync_blizzard_journal` 现在在 `/data/wow/journal-instance/{id}` 失败时不再静默写入空 encounter 的实例，而是记录 `fetchFailures / fetchFailureCount`，把错误写入实例 payload 的 `_syncError`，并加入 `Battle.net journal instance {id} fetch failed` blocker；`sync_websim_cache` 会继承该 blocker 使同步 `ok=false`。这样当前赛季副本详情没拉下来时，不会因为其他来源碰巧覆盖了同一副本而误判“该副本掉落已完整同步”。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 WebSim 同步成功状态的 gear catalog 约束。`sync_websim_cache` 现在不再只凭 active season `dataStatus=verified` 和 Battle.net API 阶段无 errors 判定 `ok=true`，还会检查最终 `gearCatalog.status`；只要装备目录仍是 partial/blocked/stale/missing_credentials，就会把 `gearCatalog.blockers` 合并进同步 `errors`，并保持 `ok=false`。这避免了“真实 API 同步没抛错，但当前赛季装备库仍缺大秘境/团本/套装/变体证据”时被上层误判为可用。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 socket option 的 Battle.net 宝石类型校验。`gear_catalog.details.modOptionCoverage.socket` 现在不再只接受 `gemItemId + iconUrl + metadataStatus=verified`，还会反查 `websim_items` 中对应 Battle.net item metadata，要求 `item_class` 必须是 Gem；如果 socket option 指向的是普通护甲、武器或缺失 class 的非宝石 item，会输出 `invalidGemMetadataCount / invalidGemMetadataExamples` 并加入 `socket mod options reference non-gem Battle.net item metadata` blocker，避免真实 Raider.IO / SimC 样本里的 `gem_id` 被错误 metadata 污染后仍进入 verified 装备库。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal 掉落来源上下文一致性门禁。`gear_catalog.details.seasonSourceCoverage.journalLoot` 现在不再只凭 `loot-*` source id 判定来源已覆盖，而是要求 `websim_gear_sources` 中的 `itemId / instanceId / encounterId` 与 `websim_loot` 及官方 journal encounter 期望上下文一致；如果同一 source id 存在但被写到错误副本或错误首领，会输出 `mismatchedCatalogSourceCount / mismatchedCatalogSourceExamples` 并加入 `Battle.net journal loot items have mismatched gear catalog source context` blocker，避免副本掉落来源错绑仍被当成可用赛季装备库。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net preview 属性权威与冲突审计。装备属性提取现在在存在官方 `preview_item.stats` 时只采用该字段作为展示与候选属性来源，不再把同一 payload 顶层残留的旧 `stats/itemStats` 混入玩家可见属性；`gear_catalog.details.itemMetadata` 会比较顶层属性与 Battle.net preview 属性，发现同一 item 属性数值冲突时输出 `statMismatchCount / statMismatchExamples` 并加入 `catalog items have stat mismatches with Battle.net preview stats` blocker，避免“属性存在但数值被旧缓存污染”的装备库进入 verified。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net slot 一致性门禁。`gear_catalog.details.itemMetadata` 现在会把 `websim_gear_variants` 与 `websim_loot` 中的本地目录槽位和 verified Battle.net item metadata 的装备槽位对账，发现同一 item 被本地 catalog 放到错误槽位时输出 `slotMismatchCount / slotMismatchExamples` 并加入 `catalog items have slot mismatches with Battle.net metadata` blocker；戒指、饰品和通用单手武器保留合法等价槽位，避免把 `finger2 / trinket2 / off_hand` 这类正常装备位置误判。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal 非装备掉落过滤。`sync_blizzard_journal` 现在会继续保存每个 journal item 的 Battle.net metadata 作为审计证据，但只有能解析到装备槽位且 metadata 未明确标成非护甲/非武器的条目才写入 `websim_loot`，非装备条目会计入 `skippedNonGearLoot` 与示例；`gear_catalog.details.seasonSourceCoverage.journalLoot` 也会复用已保存 metadata 跳过这些非装备 expected item，避免材料、货币、代币等 boss 掉落把“赛季装备库缺本地缓存/缺 catalog source”的 blocker 弄脏。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 observed socket option 的 Battle.net 宝石 metadata 回填。`sync_websim_cache` 现在会在 Raider.IO observed 装备样本生成 verified `gem_id / gem_ilevel` 变体后重新派生 `observed-socket-*`，再通过 `sync_blizzard_gear_mod_option_metadata` 拉取 `/data/wow/item/{gemId}` 与 media，把宝石名称、图标、品质、`gemItemId`、`metadataStatus` 写回 `websim_gear_mod_options` 和 `websim_items`；`websim_sync.gearModOptions` 暴露同步计数，`gear_catalog.details.modOptionCoverage.socket` 会报告缺 Battle.net 宝石 metadata 的 option，并加入 `socket mod options missing Battle.net gem metadata` blocker，避免“可插宝石但宝石本身不可校验”的数据进入 verified。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 observed gem 派生 socket option。`sync_websim_gear_mod_options` 现在不再只能依赖 `WOW_WEBSIM_GEAR_MOD_SEED`，会从 `websim_gear_variants.status='verified'` 且带真实 `gem_id / gem_bonus_id / gem_ilevel` 的 observed/SimC 变体中生成 `observed-socket-*` 宝石选项，写入 `websim_gear_mod_options`，来源 payload 标记为 `observed_variant`；前端仍只在物品 `modCapabilities.hasSocket=true` 时显示这些选项。这样真实 Raider.IO / SimC 装备样本里的宝石字段可以先补上服务端可运营宝石选项库，避免 socket-capable 装备长期卡在“无 socket mod options” blocker。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增宝石插槽可运营性门禁。`gear_catalog.details.itemMetadata.socketCapableItemCount` 已能识别 Battle.net metadata 或 observed SimC gem 字段证明的可插槽装备；现在当存在 socket-capable catalog items 但 `gear_catalog.details.modOptionCoverage.socket.optionCount=0` 时，`/api/data/health` 会加入 `socket-capable catalog items missing socket mod options` blocker，并阻止 `gear_catalog` 进入 verified，避免“装备支持插槽”与“服务端没有可用宝石选项库”之间的断点被隐藏。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增赛季 source revision 防串门门禁。`/api/data/health` 的 `gear_catalog.details.seasonSourceCoverage` 现在按 active season 的 `seasonRevision / revision / seasonLabel / label` 识别当前赛季来源；带有明确旧 `seasonRevision` 的 dungeon / raid gear source 不再计入当前赛季覆盖，并在 `mythicPlus` / `raid` 下输出 `staleSourceCount` 与 `staleSourceExamples`，避免旧赛季同副本或同名来源让本赛季大秘境、团本装备覆盖误判为完成。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增套装部件 catalog source 完整性门禁。`/api/data/health` 的 `gear_catalog.details.seasonSourceCoverage.sets` 现在会把 `websim_item_set_items` 中每个 Battle.net item-set 部件与 `websim_gear_sources.source_type='tier_set'` 对账，优先按 `setId + itemId` 校验；若套装详情已入库但任一部件缺少对应装备目录来源，会输出 `setItemCatalogSourceCount`、`missingSetItemCatalogSourceCount`、`missingSetItemCatalogSourceExamples` 并加入 `item set pieces missing gear catalog source` blocker，避免只因 item-set detail 存在就把套装装备库误判为可用。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增本赛季 item set 期望集合门禁。`sync_blizzard_journal` 会从真实 Battle.net item metadata 的 `item_set` 字段收集套装 refs，并写回 active season 的 `itemSets`；`sync_blizzard_item_sets` 现在即使没有任何已发现装备，也会主动拉取 active season `itemSets` 中的 `/data/wow/item-set/{id}`；`/api/data/health` 的 `gear_catalog.details.seasonSourceCoverage.sets` 新增 expected/known/verified/missing expected 计数与缺失套装名，并在本赛季期望套装缺 Battle.net item-set 详情时加入 `current season item sets missing Battle.net item-set detail` blocker。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增当前资料片 raid 期望覆盖门禁。`sync_blizzard_journal` 会把 Battle.net journal expansion 中选中的 raid refs 写回 active season 的 `raids` 字段；`/api/data/health` 的 `gear_catalog.details.seasonSourceCoverage.raid` 现在输出 expected/covered/missing raid instance 计数与缺失实例列表，并在期望 raid 没有对应 gear source 时加入 `current expansion raid missing gear loot` blocker，避免团本只按已入库来源汇总而漏掉整本未同步或未索引的情况。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal loot 本地缓存与 catalog source 完整性审计。`/api/data/health` 的 `gear_catalog.details.seasonSourceCoverage.journalLoot` 会从 `websim_encounters.payload_json` 解析官方 `items / loot` 期望集合，并逐项对账 `websim_loot` 与 `websim_gear_sources`；若 encounter 已同步但 loot 行缺失，或 loot 已入库但装备候选目录缺对应 source，会报告 expected/cached/catalogSource/missing 计数与 missingExamples，并把 `Battle.net journal loot items missing from local cache` / `Battle.net journal loot items missing gear catalog source` 并入 blockers，防止同步中断后把不完整副本掉落库误判为可用。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net journal 同步截断门禁。`sync_blizzard_journal` 现在会记录本轮 `instance / raid instance / encounter / item` 同步 limit，并输出 `truncation`、`truncated` 与明确 blockers；如果当前赛季地城、当前资料片 raid、首领或物品因为 limit 没有全量拉完，`sync_websim_cache` 会把 blocker 写入 `errors` 并使 `ok=false`，不再把部分 journal loot 当成完整赛季装备库。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增 Battle.net item-set 同步阶段。`sync_websim_cache` 在 journal loot 后会从已发现的官方 `item_set.id` 拉取 `/data/wow/item-set/{id}`，新增 `websim_item_sets` / `websim_item_set_items` 本地表，并把套装部件写入 `tier_set` gear source 与 partial variant；`gear_catalog.details.seasonSourceCoverage.sets` 现在区分 discovered/verified/missing item-set detail 和 verified set item count，发现套装但缺 Battle.net item-set 详情时会阻断 catalog verified。若某个套装部件的 item metadata 拉取失败，会写入 `battle_net_item_set` source_reference 占位，让 item metadata audit 明确报告缺官方 metadata / stats / armor type，而不是静默漏掉。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层新增赛季来源覆盖审计。`/api/data/health` 的 `gear_catalog.details.seasonSourceCoverage` 现在会按当前 active season 地城列表核对大秘境掉落来源覆盖，同时汇总团本来源物品数与套装件数；缺任一当前赛季地城掉落时 `gear_catalog` 会保持 partial/blocked 并报告缺口，不会因为 Raider.IO/WCL observed 样本存在就误判为完整赛季库。团本目前只汇总已验证来源，不虚构期望数量；套装已开始接入 Battle.net item-set 详情，后续还需要把当前赛季 raid / tier 的期望集合做成完整门禁。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`。
- 2026-06-21：装备数据层按真实 Raider.IO 装备案例完成校验收口。使用 `season-mn-1` 国服 +24 大秘境公开排行榜抓取 5 个真实角色装备样本（武僧织雾、死亡骑士邪恶、恶魔猎手、德鲁伊守护、唤魔师增辉）回放到 `sync_observed_gear_variants`；observed profile 只负责生成可执行 SimC 变体，不能单独把 `gear_catalog` 标成 verified。`/api/data/health` 的 `gear_catalog.details.itemMetadata` 现在会审计 Battle.net metadata 覆盖、属性覆盖、护甲类型、武器类型、套装数量、插槽能力和缺失示例；真实 observed-only 回放会保持 partial 并报告缺官方 metadata / stats / armor type / weapon type，官方 item payload 补齐后才可升为 verified。同步计数也改为按唯一 observed source / variant 去重，避免 aggregate 与 profile 重复装备把计数翻倍。证据：`server/websim_payload.py`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`tests/builds-page.test.js`。
- 2026-06-21：装备模拟属性与性能二次优化已落地。后端自有赛季数据层现在能识别 Battle.net 本地化中文 `护甲 / 布甲 / 皮甲 / 锁甲 / 板甲` 分类，并在 catalog 与 `verifiedLoot` 两条路径按职业护甲类型过滤候选；法师冰霜头部不再混入皮甲 `断法暗影面具` 或锁甲 `漩涡护面`，线上 `/api/websim/gear?class=mage&spec=frost&compact=1` 头部只返回布甲 `虚空粉碎者的面纱`，属性为 `智力 9；耐力 14；急速 12；精通 6`。compact 装备接口继续裁掉候选、sources、variants、社区装备模板和赛季 dungeon 的 raw `payload` 字段，公开入口从约 1,025,604 bytes 降到 433,810 bytes，`payloadIndex=-1`，远端本机接口约 0.21s。前端候选详情优先展示后端 `statSummary`，避免 `stats/itemStats/statSummary` 重复拼接。证据：`server/websim_payload.py`、`pages/builds/detail.js`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`、`http://124.223.51.33/api/websim/gear?class=mage&spec=frost&compact=1`。
- 2026-06-21：装备模拟页线上超时修复已落地。根因是读接口反复调用 `init_db()` 时会重写新闻源种子和清理 token，遇到 WebSim / 属性权重等同步任务持有 SQLite 写锁后，请求会等待到超时并触发小程序空槽位兜底；后端现在在 `schema_migrations` 完整记录后快速返回，避免已初始化数据库在读路径执行写入。新增回归测试锁定“初始化后不再重复写 seed”行为；线上部署后，`/api/websim/gear?class=mage&spec=frost&compact=1` 在同步服务仍运行时可稳定返回 16 个槽位组、113 个候选，公开入口 200，本机后端约 0.14s。证据：`server/news_backend.py`、`tests/news_backend_test.py`、`http://124.223.51.33/api/websim/gear?class=mage&spec=frost&compact=1`、`http://124.223.51.33/api/data/health`。
- 2026-06-21：装备替换 sheet 的 partial 候选应用保护已落地。静态 dungeon catalog / `needs-variant` 等缺装等或缺确定 SimC 选项的候选仍可在详情中查看掉落来源和装备属性，但“应用”按钮会进入待补禁用态，运行时也会阻止写入当前装备配置并提示缺少装等或缺少 bonus/宝石/附魔；可保存路径只接受应用后仍满足 `simcReady` 且能生成配置行的装备，避免把自有赛季数据层的 partial 证据误写成玩家可保存配置。证据：`pages/builds/detail.*`、`tests/builds-page.test.js`。
- 2026-06-21：赛季数据可信层方向已确认。后端需要维护自有 `Season Data Cache`，但它不是人工维护的“真理库”，而是围绕 Battle.net Game Data API、Raider.IO/WCL observed data、SimC preset 和本地可重建缓存建立同步、索引、对账、降级与稳定读取层；装备、宝石、附魔、变体和 SimC-ready 判断必须带 `sourceType/sourceStatus/seasonRevision/checkedAt/status/blockers` 等证据语义，缺字段时保持 partial/blocked，不把候选或 LLM 推理包装成已验证事实。证据：`docs/roadmap.md`、`docs/roadmap/ideas.md`、`server/websim_payload.py`、`server/news_backend.py`、`tests/news_backend_test.py`。
- 2026-06-21：装备模拟候选与详情优化已落地。职业专精装备模拟候选筛选收敛为 `全部 / 大秘境 / 团本`，移除推荐与制造来源；候选行右侧只保留紧凑详情按钮，展开后仅面向玩家展示掉落来源和装备属性，不暴露物品 ID、SimC 字段、缺失字段、blocker 或 observed refs 等内部元数据；装备属性从 Battle.net item `preview_item.stats` 提炼，难度/装等以中文玩家文案展示；默认假宝石/附魔 seed 不再进入候选，宝石/附魔选项必须经过物品插槽和可附魔部位过滤；制造来源不再进入 `/api/websim/gear` 装备库，但 SimC 标准字段 `crafted_stats` 继续兼容历史模板和真实输入；静态掉落缺确定 `bonus_id/gem_id/enchant_id` 等 SimC 字段时保持 partial，不伪装为可执行。证据：`pages/builds/detail.*`、`server/websim_payload.py`、`tests/builds-page.test.js`、`tests/ui-style-guide-implementation.test.js`、`tests/websim_payload_test.py`。
- 2026-06-21：小程序 UI 风格规范已沉淀为长期约束。新增 `docs/ui-style-guide.md`，把后续界面统一到“移动端艾泽拉斯分析控制台”：黑曜石底色、暗铁/深棕面板、魔兽金主强调、真实游戏图标、紧凑 hero、同排 header/action、固定状态色和移动端密度检查清单；后续新增小程序页面和模块应先对照该规范，避免每次局部微调造成色彩、圆角、按钮高度和状态语义继续分裂。证据：`docs/ui-style-guide.md`、`app.wxss`、`websim/app.css`、`pages/builds/detail.wxss`、`pages/simulator/simc.wxss`、`pages/pve/detail.wxss`。
- 2026-06-20：全职业社区模板扫描、去重与定时更新 v1 已落地。新增 `server/community_template_sync.py` 独立同步入口，按 `Raider.IO cache -> WCL source state/seed extraction -> talent authority validation -> observed gear catalog -> template dedupe -> sync run record` 记录扫描；Raider.IO profile 抓取从全局上限改为按专精采样，默认每专精最多 5 个 profile 并输出 13 职业 / 39 专精覆盖率；`websim_community_talent_templates` 补充 `signature/source_refs/scan_run_id`，新增 `websim_community_gear_templates` 与 `community_template_sync_runs`；`/api/websim/talents` 与 `/api/websim/gear` 均按稳定签名去重并合并 `sourceRefs`，`/api/data/health` 暴露 `templateRevision`、`scanCoverage`、`dedupedCount`、`hiddenDuplicateCount` 和 WCL 模板源状态；新增 `wow-community-template-sync.service/timer`，6 小时周期、boot 后 12 分钟启动并部署时 no-block 初始同步。证据：`server/community_template_sync.py`、`server/raiderio_payload.py`、`server/websim_payload.py`、`server/community_talent_sources/warcraftlogs.py`、`server/news_backend.py`、`server/wow-community-template-sync.*`、`server/deploy_lighthouse.sh`、`tests/raiderio_payload_test.py`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`tests/deploy-script.test.js`。
- 2026-06-20：全职业装备候选与社区装备模板 UI 收口已落地。`/api/websim/gear` 会过滤非通用槽位上的跨职业 / 跨专精 observed profile 候选，项链、披风、戒指、饰品按通用槽位复用，戒指 / 饰品自动补齐 1/2 槽；前端把原始 WoW icon 名归一成 render 图标 URL，并把社区装备模板 sheet 改成纵向卡片与全宽应用按钮，避免小屏溢出。线上 smoke 覆盖 13 职业 / 39 专精、624 个槽位，`failures=0`、无 incompatible 候选泄露。证据：`server/websim_payload.py`、`pages/common/game-asset.js`、`pages/builds/detail.wxss`、`tests/websim_payload_test.py`、`tests/frontend-api-client.test.js`、`tests/builds-page.test.js`、remote smoke: `/api/websim/gear?class=monk&spec=brewmaster&compact=1`。
- 2026-06-20：装备模拟社区模板导入与重置已落地。职业专精装备模拟移除顶部旧来源摘要条，保留底部“来源与样本窗口”证据；动作区与天赋模拟对齐为保存模板、导入社区推荐和重置；`/api/websim/gear` 新增 `communityTemplates` / `communityTemplateSync`，从 SimC preset 解析可执行装备模板，只导出带 canonical slot 与 item id 的 SimC gear item，display-only / source-reference 装备不进入可应用模板；前端导入 partial 模板时以 `equippedSet` baseline 为底只覆盖模板槽位，重置不重新请求 API 并恢复 baseline，保存仍要求 16 槽完整配置。证据：`pages/builds/detail.*`、`pages/builds/websim-api.js`、`server/websim_payload.py`、`tests/builds-page.test.js`、`tests/frontend-api-client.test.js`、`tests/websim_payload_test.py`。
- 2026-06-20：模拟 SimC 固定模板组合流程已落地。`pages/simulator/simc.*` 从聊天式确认改为全职业 picker 选择器流程：默认定位到最近保存过模板的职业，玩家可从 builds home 的完整职业列表选择任意职业，再只在该职业下筛选已保存天赋模板和装备模板，并默认选中各自最近保存的一条，缺模板时展示“尚未保存天赋模板 / 尚未保存装备模板”且不可提交；最终选择 1 个天赋模板、1 个装备模板和固定场景预设；新增 `mode=simcraft_template` 确定性请求路径，后端在 `/api/simulator/analyze` 入口把 WebSim 天赋 `rawString` 编码为 `class_talents/spec_talents/hero_talents`、把官方天赋码标准化为 `talents=<code>`、把装备 `rawString` 解析为 16 槽 canonical `simcItems`，并在职业/专精不匹配、装备缺槽或模板不完整时 deterministic blocked；confirmOnly 与最终提交复用同一 payload，final 只切换 `confirmOnly=false` 和 `saveTask=true` 后复用现有 SimC runner、任务保存与结构化报告。证据：`pages/simulator/simc.*`、`server/news_backend.py`、`server/simulator_payload.py`、`tests/news_backend_test.py`、`tests/simulator-page.test.js`。
- 2026-06-20：职业专精装备模拟首屏防 fallback 闪现已落地。`query=gear` 首次进入时新增 `gearInitialLoading` gate，在真实 `/api/websim/gear` 槽位结构返回前隐藏旧 `Mythicstats + Wowhead` 来源条、旧装备洞察文案和保存按钮，只展示装备库加载态，避免玩家误以为旧 seed 是当前装备模拟结果。证据：`pages/builds/detail.js`、`pages/builds/detail.wxml`、`pages/builds/detail.wxss`、`tests/builds-page.test.js`。
- 2026-06-20：装备数据库真实 API 补齐与 observed SimC 变体线上验收通过。Battle.net Game Data API 已用于官方赛季、journal loot、物品名称/图标和来源元数据；Raider.IO CN observed gear 已写入 `observed_profile` variants，只有真实实例具备 `itemId + slot + itemLevel + deterministic SimC option` 时才标为 verified。线上 `/api/data/health` 显示 `websim_sync=verified`、`gear_catalog=partial`、114 件 catalog item、133 个 variants、93 个 observed verified variants、16/16 canonical 槽位覆盖；静态 dungeon loot 仍保留 `missing deterministic SimC variant preset` blocker；`/api/websim/gear?class=mage&spec=frost` 不再全落 `trinket1`，`class=monk&spec=mistweaver` 有 16 个 SimC-ready observed variants。证据：`server/websim_payload.py`、`server/news_backend.py`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`http://124.223.51.33/api/data/health`、`http://124.223.51.33/api/websim/gear?class=monk&spec=mistweaver`。
- 2026-06-20：天赋模拟器移除可选态绿色圆点提示。可点状态仅保留绿色外框和轻量阴影，不再渲染右上角圆点或 `+` 字符，降低节点视觉噪声。证据：`pages/builds/talent-simulator.wxss`、`tests/builds-page.test.js`。
- 2026-06-20：天赋模拟器完成魔兽原生节点外框精修。所有节点边框统一加粗；choice 节点改为独立正八边形外框，图标内部裁剪，左右小箭头贴合外框边线，锁定态保留灰色语义。证据：`pages/builds/talent-simulator.wxml`、`pages/builds/talent-simulator.wxss`、`tests/builds-page.test.js`。
- 2026-06-20：天赋模拟器补齐魔兽原生节点形状语义。小程序原生天赋树尊重 WebSim `shape`：被动/圆形、主动/圆角矩形、choice/正八边形并用左右箭头提示可切换；核心 view model 对旧 payload 做 shape fallback。证据：`pages/builds/talent-simulator-core.js`、`pages/builds/talent-simulator.js`、`pages/builds/talent-simulator.wxss`、`tests/talent-simulator-core.test.js`、`tests/builds-page.test.js`。
- 2026-06-20：天赋构筑与装备模拟完成 SimC 解耦重构。天赋模拟器保存 WebSim/游戏天赋导出码为模板 `rawString`，不再调用 `/api/websim/profile` 生成 `simcLines`；职业专精内嵌装备模拟保存 16 槽 canonical 装备配置字符串，不再调用 `/api/websim/gear/stats` 或展示属性快照/SimC-ready 状态；`/api/me/build-templates` 保持 CRUD 与 `raw_string` 去重，新增 `saved` / `complete` 状态兼容旧模板。证据：`pages/builds/talent-simulator.js`、`pages/builds/detail.*`、`pages/common/build-template-storage.js`、`server/news_backend.py`、`tests/builds-page.test.js`、`tests/build-template-storage.test.js`、`tests/news_backend_test.py`。
- 2026-06-19：天赋模拟器社区模板去重与英雄树门槛修正已落地。小程序端按 WebSim export / selectedNodes / raw import 构造社区模板签名，优先保留可视化可应用、最高钥石层数、样本量和更新时间更优的模板；英雄天赋树点数门槛改为可计入 granted root，避免已赠送根节点仍锁住下一层；社区模板应用按钮在移动端固定宽度与居中，减少长文案挤压。证据：`pages/builds/talent-simulator-core.js`、`pages/builds/talent-simulator.wxml`、`pages/builds/talent-simulator.wxss`、`tests/talent-simulator-core.test.js`、`tests/builds-page.test.js`。
- 2026-06-19：装备模拟权威装备库 v1 已落地。`wow-websim-sync` 复用现有日更链路新增 `gearCatalog` 阶段，缓存当前赛季地下城、当前资料片团本、制造装备 seed、装备变体、宝石/附魔 server-owned seed 和 health 计数；`/api/websim/gear` 保持原 URL 并扩展 `catalogStatus`、`itemDatabaseRevision`、`variantRevision`、`sources[]`、`variants[]`、`socketOptions[]`、`enchantOptions[]`；`/api/websim/gear/stats` 支持 selected gear 的 `variantKey/ilevel/bonus_id/gem_id/enchant_id/crafted_stats` flatten，并在 SimC blocked 时也返回平均装备等级；小程序装备槽位 sheet 改为先选装备、再选难度/装等、宝石和附魔、最后应用到部位。证据：`server/websim_payload.py`、`server/news_backend.py`、`pages/builds/detail.*`、`pages/builds/websim-api.js`、`tests/websim_payload_test.py`、`tests/news_backend_test.py`、`tests/builds-page.test.js`、`tests/frontend-api-client.test.js`。
- 2026-06-19：高端玩家真实天赋导入进入社区推荐 v1。Raider.IO 社区模板从聚合流派升级为玩家级模板，保留玩家 ID、服务器、profile URL、原始导入码和结构化 loadout；社区模板校验必须先解析到 WebSim selectedNodes、英雄天赋和可视化导出码才 verified，raw-only 模板不进入导入列表；`/api/websim/talents` 的社区推荐改为按职业返回，前端可展示同职业所有专精/英雄天赋并在应用跨专精模板时先切换目标树再写入节点。证据：`server/raiderio_payload.py`、`server/websim_payload.py`、`pages/builds/talent-simulator.*`、`tests/raiderio_payload_test.py`、`tests/websim_payload_test.py`、`tests/builds-page.test.js`。
- 2026-06-19：Raider.IO 真实 API 云端预览验收通过。Lighthouse 线上缓存成功同步国服 `season-mn-1`：60 条大秘境记录、40 个角色 profile、26 个 Raider.IO 社区天赋模板；社区模板重解析后 `verified=26 / blocked=1`，`/api/websim/talents?class=druid&spec=guardian` 与 `class=monk&spec=mistweaver` 均能返回可视化应用的真实玩家模板，最高 +24，WCL 缺凭据仅使社区总状态保持 `partial`，不影响 Raider.IO 来源 `synced`。证据：`http://124.223.51.33/api/data/health`、`http://124.223.51.33/api/websim/talents`、`server/deploy_lighthouse.sh`、`server/raiderio_payload.py`、`server/websim_payload.py`。
- 2026-06-19：版本资讯采集队列机制 v1 已落地。新闻刷新从单段采集发布升级为 `discovery -> processing queue -> publication`，新增 `news_discovery_queue`、Blizzard 官方论坛 PTR/开发说明来源、动态 `versionEvent` 识别、每源至少 10 条发现、每轮 3-5 条处理、reference-only 审计不公开翻译，以及 `/api/news/refresh-runs/latest` / `/api/data/health` 的 backlog 与 sourceCoverage 指标；公开发布门禁仍保持 `official + approved + official_verified + source_translation`。证据：`server/news_collector.py`、`server/news_backend.py`、`docs/news-architecture.md`、`tests/news_collector_test.py`、`tests/news_backend_test.py`。
- 2026-06-18：Raider.IO 国服真实数据缓存与 payload 合并 v1 已落地。后端新增 Raider.IO client/sync/cache 聚合层，`/api/game/season`、`/api/pve/*`、`/api/builds/*`、`/api/websim/talents`、`/api/websim/gear` 保持原 URL 并扩展真实 M+ 样本、sourceStatus、赛季 slug、cutoffs/affixes、构筑样本、社区天赋模板和 observed gear 字段；API key 只从服务器环境变量读取。证据：`server/raiderio_payload.py`、`server/news_backend.py`、`server/community_talent_sources/raiderio.py`、`tests/raiderio_payload_test.py`。
- 2026-06-18：职业专精属性权重混合管线 v1 已落地。首版按全职业专精 registry 定时生成 Raider.IO 样本 + SimC scale factors + LLM 中文解释缓存，`/api/builds/detail` 在保留旧 `stats[]` 的同时新增 `scenarioWeights[]`、`sourceStatus`、`validation`、低样本/blocked 原因和 M+ 场景建议；前端属性权重模块增加单体/AOE/混合路线切换和“带当前构筑去 SimC”入口。证据：`server/stat_weights_payload.py`、`server/stat_weights_sync.py`、`server/wow-stat-weights-sync.*`、`pages/builds/detail.*`、`tests/stat_weights_payload_test.py`、`tests/builds-page.test.js`。
- 2026-06-19：账号化模板与数据库治理 v1 已落地。后端新增 `user_build_templates` owner 隔离、`schema_migrations` 记录、foreign key/WAL 规则和 `/api/me/build-templates` CRUD；小程序个人模板先落本地，HTTPS/合法域名可用时同步到账号，明文 HTTP + Bearer token 会被 API client 拦截。证据：`docs/database-architecture.md`、`server/news_backend.py`、`pages/common/build-template-storage.js`、`pages/profile/profile.js`、`tests/news_backend_test.py`、`tests/profile-auth.test.js`、`tests/build-template-storage.test.js`。
- 2026-06-19：SimC 结构化报告与 WCL 证据启动层已落地。SimC 分析 payload 新增 `evidenceState`、`runPolicy`、`allowedNumbers`、`report`，LLM 数字必须落在 allowedNumbers 内，否则回退 deterministic report；WCL 分析能解析 report URL/code/fight，在缺凭据时输出 blocked/missing_credentials，不调用 LLM，并在 WCL 页面展示证据状态。证据：`server/simulator_payload.py`、`server/news_backend.py`、`pages/simulator/wcl.*`、`tests/news_backend_test.py`、`tests/simulator-page.test.js`。
- 2026-06-19：真实数据化信任口径 v1 已落地。PVE 职业天梯中未接入授权 Archon/WCL API 的 fixture 统一降级为 `source_reference`，附带 `dataTrust`、`blockers` 和证据引用；`/api/data/health` 不再允许带 blocker 的组件保持 `verified`；WCL v2 OAuth/GraphQL 证据抽取启动层可解析 report/fight/events 摘要，失败时 deterministic blocked 且不调用 LLM。证据：`server/pve/home-payload.js`、`server/news_backend.py`、`server/simulator_payload.py`、`tests/pve-page.test.js`、`tests/news_backend_test.py`。

## 项目愿景

面向 WoW 玩家，构建一套一体化辅助工具：用可信数据追踪资讯、职业构筑、PVE 内容与赛季变化，并通过 WebSim、SimC 和 AI 分析把“看数据”推进到“做决策”。

第一阶段的核心判断是：项目不只是一个资讯小程序，而是一个围绕玩家构筑、战斗表现和版本变化的分析工作台。资讯、职业专精、PVE 数据和模拟器都应服务于同一个目标：帮助玩家更快理解当前版本、选择构筑、验证收益并复盘问题。

## 当前进展快照

| 状态 | 进展判断 | 当前重点 |
| --- | --- | --- |
| 已完成 | 后端统一部署、资讯详情完整中文化、WebSim 到 SimC 可提交闭环、SimC 输入契约、装备元数据补全、小程序原生天赋模拟器等基础能力已经形成第一阶段主干。 | 继续把这些能力从“能用”推进到“可观测、可复用、可解释”。 |
| 正在推进 | 项目重心已从单点功能转向模板化构筑、固定 SimC 输入、内嵌装备模拟、个人模板库、账号化 CRUD、数据可信度、健康哨兵、WCL 证据链、LLM 报告 schema 化，以及炸鸡队长证据教练 v0 的启动设计。 | 确保保存的天赋/装备模板能被确定性转换、校验和复用；任何缺字段、低样本或凭据不足都要清楚降级；炸鸡队长只能消费可追踪证据，不能把推理包装成已验证事实。 |
| 下一步 | 全职业天赋规则校验、正式发布可观测、模板加载/角色归档、WCL 同类样本窗口和端到端 SimC/WCL smoke 需要继续拆小落地。 | 优先补足会影响真实结论可信度和线上发布稳定性的环节。 |
| 后续 | 职业/PVE 数据采集自动化、角色绑定与个人化工作台、顶级日志与手法知识库等方向已经有候选池，但还不应抢在核心闭环稳定前展开。 | 保持想法池记录和证据边界，不把候选方向写成已实现能力。 |
| 待决策 | 产品定位命名、首页与 tab 权重仍需要最终取舍。 | 决定对外主叙事后，再同步 README、导航结构和页面文案。 |

## 当前产品能力

| 能力 | 当前状态 | 说明 | 关联文档 / 区域 |
| --- | --- | --- | --- |
| 资讯追踪 | 已完成基础版，详情完整中文化已上线 | 后端提供新闻首页、列表、详情、刷新任务和来源记录；详情页已升级为原题保留、完整中文正文、内容 tag 和发布质检。 | [news-architecture.md](news-architecture.md), `pages/news/`, `server/news_backend.py` |
| 职业专精 | 已完成首版基础入口，热门专精/属性权重/输出循环待规划 | 职业、专精、天赋、装备、属性权重和循环的结构化展示曾已建立；首版职业专精 tab 开放“天赋构筑”“装备模拟”“模拟 SimC”“任务列表”四个入口，分别沉淀天赋导出码、16 槽装备配置字符串、固定 SimC 提交流程和最近模拟任务追踪；“热门专精”“属性权重”和“输出循环”暂不在职业专精 tab 展示，保留既有代码和历史证据，待重新规划数据源、入口和验收标准后再接回。 | [builds-architecture.md](builds-architecture.md), `pages/builds/` |
| PVE 专区 | 待规划 | 当前赛季大秘境、团队 raid、boss 攻略入口、职业天梯和来源校验 UI 已有历史实现与数据契约，但首版暂时从 tabBar 与小程序页面注册中下线；相关页面、后端 payload 与测试保留为 dormant 证据，待明确真实数据源、首屏价值和发布验收后再恢复入口。 | `pages/pve/`, `server/pve/`, `server/news_backend.py`, [职业天梯 UI 契约](plans/2026-06-18-pve-spec-ladder-ui-contract.md) |
| 智能分析 / SimC | 首版只保留炸鸡队长，SimC 与任务列表迁入职业专精，WCL 待规划 | SimC 已从聊天式确认推进到固定模板组合：前端使用和天赋模拟器一致的 picker 流程，先从完整职业列表选择职业，默认定位最近保存过模板的职业，再筛选该职业下已保存的天赋模板、装备模板和场景预设，缺模板时明确空态并阻断提交；后端新增 `simcraft_template` 确定性转换层，把 WebSim 天赋码/官方天赋码/装备配置字符串转换为可执行 SimC profile，并复用 confirmOnly、SimC runner、任务保存、阶段输出、防误导策略、allowedNumbers 和结构化报告字段。首版中，SimC 入口和任务列表迁入职业专精 tab；智能分析 tab 只保留“炸鸡队长”；“分析 WCL”继续保持 dormant，WCL report 解析、缺凭据 blocked 证据 UI 和 v2 OAuth/GraphQL 证据抽取启动层保留为历史证据。炸鸡队长 v0 首版只消费当前可追踪证据，缺关键输入时输出缺失项和通用建议，不编造同类排名或日志结论。 | [simulator-simc-end-to-end.md](simulator-simc-end-to-end.md), [2026-06-11-simc-flow-risk-avoidance.md](plans/2026-06-11-simc-flow-risk-avoidance.md), [并行优化控制计划](plans/2026-06-21-parallel-optimization-control.md), `pages/simulator/simc.*`, `server/news_backend.py`, `server/simulator_payload.py`, `tests/news_backend_test.py`, `tests/simulator-page.test.js` |
| WebSim 工作台 | 已完成 | 已把天赋编码、完整核心装备 SimC-ready 校验、canonical WebSim profile、任务保存、真实 SimC 执行、装备中文名/图标和掉落查询体验连成基础闭环。 | `websim/`, `server/websim_payload.py` |
| 后端与部署 | 已完成基础版，数据库治理待推进 | 统一 Python 后端、SQLite、Lighthouse 部署脚本、systemd 服务和同步任务已建立；下一步需要把当前单库 SQLite 方案治理成有迁移、owner、缓存和 analytics 边界的数据库架构。 | [remote-debugging.md](remote-debugging.md), [2026-06-09-unified-backend-cloud-deploy.md](plans/2026-06-09-unified-backend-cloud-deploy.md), [数据库架构治理计划](plans/2026-06-17-database-architecture-governance.md) |
| 个人化工作台 | 正在推进 | 小程序已接入微信账号登录、后端 token 和用户资料基础；“我的”tab 已从能力占位推进到个人模板库 v1，可汇总、删除本地天赋/装备模板，并在安全网络条件下同步到账号模板接口；后续收藏、角色、订阅、任务等增删改查都要收敛到账号边界。 | `pages/profile/`, `pages/common/auth-client.js`, `pages/common/build-template-storage.js`, `server/news_backend.py`, `websim/app.js` |

## 路线主题

| 主题 | 方向 |
| --- | --- |
| 数据可信与新鲜度 | 所有玩家可见结论都应带来源、时间窗口、验证状态和 fallback 边界；赛季未验证时宁可阻断，也不展示可能过期数据。 |
| 构筑到模拟闭环 | 职业专精、WebSim、SimC 和任务详情要形成一条链：选择构筑、保存/加载模板、补齐可执行输入、提交模拟、保存和复盘结果。 |
| 证据化 AI 报告 | LLM 只做解释和表达，数字事实必须来自 SimC runner、日志解析或可信参考源；未来报告应逐步 schema 化。 |
| 移动端 UI 风格与密度 | 后续小程序 UI 统一遵守“移动端艾泽拉斯分析控制台”规范：暗色金边、真实游戏图标、状态色固定、紧凑 hero、同排按钮/文案和少滚动的信息密度。 |
| 个人化角色体验 | 从通用查询推进到账号化角色档案、收藏专精、订阅提醒、任务历史和个人数据源配置；所有增删改查都应有明确 owner。 |
| 运维与可观测 | 正式域名、HTTPS、合法域名、刷新记录、SimC / 游戏数据 / 模板健康检测、smoke 和部署状态要成为稳定发布链路的一部分。 |

## 里程碑

| 状态 | 里程碑 | 目标 | 用户价值 | 关键动作 | 完成标准 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| 已完成 | 统一后端与云部署 | 用一个轻量后端承载新闻、职业、PVE、模拟器和 AI 分析。 | 小程序不再依赖纯本地 payload，后续能力可以稳定接入服务端。 | 建立统一 API；接入 SQLite；部署到 Lighthouse；补齐服务文档。 | `/health` 与核心 API 可用；部署脚本和远程调试文档存在；相关测试通过。 | [统一后端计划](plans/2026-06-09-unified-backend-cloud-deploy.md) |
| 已完成 | 资讯详情完整中文化与发布质检 | 将资讯详情从摘要页升级为完整中文阅读页，同时保留原文标题和来源证据。 | 玩家点进资讯后能看到完整中文正文、内容 tag、官方核验和轻量来源入口，不再只看到一句话摘要或大块原文。 | Blizzard 官方文章二段式采集；LLM 逐块输出中文标题/摘要/正文/tag；新增来源注册表、raw/evidence 内部表、授权门禁、官方校验和冲突隔离；公共 API 隐藏原文正文；详情页改为原题副标题、信任 badge、块级正文和底部来源按钮；小程序端移除手动刷新入口。 | 只有 `contentStatus=ready`、`licenseStatus=approved`、`verificationStatus=official_verified`、`translationStatus=llm`、`translationFidelity=source_translation` 的文章进入首页/列表/详情；`bodyBlocksZh` 不得退化成 summary 或 LLM 导读改写；未授权第三方全文翻译、无官方依据或冲突条目 blocked；blocked 原因、verification counts、license blocked 和 conflict 进入 refresh run；本地 fallback seed 同样不展示缺少 source translation fidelity 的旧导读内容；相关 Python/Node 测试和远端 scheduled refresh 验收通过。 | [news-architecture.md](news-architecture.md), `server/news_collector.py`, `server/news_translator.py`, `server/news_backend.py`, `server/news/articles.seed.json`, `server/news/home-payload.js`, `pages/news/detail.*`, `tests/news_*`, remote `/api/news/refresh-runs/latest` |
| 已完成 | 职业专精基础体验 | 将原 BD tab 重构为职业专精查询入口。 | 玩家能从职业/专精维度查看天赋、装备、属性和循环。 | 重构 tab；建立职业专精数据契约；新增详情页和架构文档。 | 入口与详情页可用；职业专精覆盖完整；文档记录数据源和扩展方式。 | [职业专精计划](plans/2026-06-09-specializations-tab.md) |
| 已完成 | 小程序 UI 风格规范 | 固化后续小程序页面的主视觉、色彩、空间密度、按钮同排和状态语义规则。 | 新增功能不再每次重新微调 UI 细节，玩家在资讯、构筑、PVE、模拟器和我的页面之间获得稳定、紧凑、可扫读的 WoW 工具体验。 | 以现有 `app.wxss`、WebSim 暗铁金边样式、构筑/SimC 紧凑 action row 和 PVE 移动端数据表为证据，制定“移动端艾泽拉斯分析控制台”规范；明确色彩 token、hero 高度、按钮高度、卡片间距、同排布局、状态 badge 和数据可信 UI。 | `docs/ui-style-guide.md` 存在并可作为后续 UI 评审清单；新增页面优先复用该规范中的暗色金边、游戏图标、紧凑布局和状态语义；不得把 `verified / partial / source_reference / blocked / stale` 表现为不同页面的不同颜色或含义。 | [UI 风格规范](ui-style-guide.md), `app.wxss`, `websim/app.css`, `pages/builds/detail.wxss`, `pages/simulator/simc.wxss`, `pages/pve/detail.wxss` |
| 待规划 | PVE 职业天梯移动端排行 | 将 PVE 专区的「职业天梯」从普通列表升级为可扫读的高端玩家数据视图。 | 玩家能按 DPS、坦克、治疗快速理解当前专精强度分层，并在同页查看 WCL 统计明细和来源状态。 | 已完成 UI 与数据契约：`specLadder` 模块新增三角色入口、Archon tier board、WCL 移动端 chart table、来源验证区、gameAsset 图标语义和旧 `items` 兼容；未接入授权 API 的 Archon/WCL fixture 已统一标为 `source_reference` 并输出 blockers；首版发布范围暂时下掉整个 PVE 专区，页面代码与后端 payload 保留为 dormant 证据。 | 后续恢复前需要重新确认 PVE tab 首屏价值、真实 Archon/WCL 数据源或授权数据、样本量/更新时间门禁，以及 stale/blocked 时的强结论阻断策略；恢复入口时需重新通过 `tests/pve-page.test.js` 和 `tests/frontend-api-client.test.js` 覆盖。 | [职业天梯 UI 契约](plans/2026-06-18-pve-spec-ladder-ui-contract.md), `pages/pve/detail.*`, `pages/pve/pve-api.js`, `server/pve/home-payload.js`, `pages/common/game-asset.js`, `tests/pve-page.test.js`, `tests/frontend-api-client.test.js` |
| 已完成 | SimC 风险边界收敛 | 防止空 profile、模板 profile 和 LLM 误导性 DPS 输出。 | 玩家不会把模板、空输入或模型猜测误认为真实模拟结论。 | 区分 explicit / prompt / generated；限制真实执行；补齐阶段输出和防护文档。 | SimC 执行前校验清晰；generated 只作为预览；文档列出已知风险和规避原则。 | [SimC 风险规避](plans/2026-06-11-simc-flow-risk-avoidance.md) |
| 已完成 | WebSim 到 SimC 的可提交闭环 | 让 WebSim 选择的天赋和装备能安全生成 SimC profile 并提交模拟。 | 玩家可以从可视化构筑直接进入可信模拟，而不是手动拼 profile。 | 天赋节点编码；完整核心装备 SimC-ready 校验；canonical WebSim profile；候选装备提示；阻断不可提交状态。 | 预览、提交、保存任务和 SimC 执行复用同一份 profile；核心装备栏位完整；有天赋编码结果、装备 readiness、阻断原因、成功执行测试和 benchmark 状态；候选装备不会误写入 SimC；无外部参考窗口时明确标为 unverified。 | `websim/`, `server/websim_payload.py`, `tests/websim_payload_test.py`, `tests/news_backend_test.py`, [SimC 端到端](simulator-simc-end-to-end.md) |
| 已完成 | 小程序原生天赋模拟器 | 将 WebSim 天赋树移植到职业专精 tab 的“天赋构筑”入口。 | 玩家可以在小程序内直接点选职业、专精和英雄天赋，并保存为可复用的天赋模板。 | 新增天赋模拟器页；接入 WebSim bootstrap/talents；暴露职业/专精 WebSim key；职业、专精、英雄天赋保持单行选择；实现点数、前置、choice、导入导出、移动端分树标签展示、魔兽原生节点形状语义和模板保存。 | `pages/builds/talent-simulator` 可从职业专精 tab 的“天赋构筑”进入；热门专精入口首版暂不展示但历史 intel 页面保留；移动端以通用/英雄/专精标签切换单树渲染；payload 暴露 WebSim class/spec key；被动、主动和 choice 节点分别呈现圆形、圆角矩形和带左右箭头的正八边形；模板保存以 `rawString` 保存 WebSim/游戏天赋导出码，`simcLines` 仅保留空数组作旧字段兼容；页面不再调用 `/api/websim/profile` 或直接暴露带去 SimC 操作；相关前后端契约测试覆盖。 | `app.json`, `pages/builds/talent-simulator.*`, `pages/builds/talent-simulator-core.js`, `pages/builds/websim-api.js`, `server/builds/home-payload.js`, `server/simulator_payload.py`, `tests/talent-simulator-core.test.js`, `tests/builds-page.test.js`, `tests/frontend-api-client.test.js`, `tests/news_backend_test.py`, `tests/simulator-page.test.js` |
| 已完成 | SimC 输入契约产品化 | 把可执行 `/simc`、WebSim profile、confirm-only、submit、gear readiness、profileSource 和外部 benchmark 规则固化成用户可理解的流程。 | 玩家知道什么时候只是预览、什么时候可以真实模拟、缺哪些输入，以及模拟结果是否落在外部数据的合理区间。 | 强化 `/simc` 导入；确认与提交复用 payload；规范 SimC slot；区分展示 gear 与可执行 gear；标注 reasonable / outlier / unverified；补齐更多场景的外部参考窗口。 | 任意模拟提交前都能说明输入来源、是否可执行、缺失字段、是否保存任务和横向合理性状态；外部数据缺席时不得把 DPS 称为已横向验证。 | `websim/app.js`, `server/news_backend.py`, `server/websim_payload.py`, `tests/websim-page.test.js`, `tests/websim_payload_test.py`, [simulator-simc-end-to-end.md](simulator-simc-end-to-end.md) |
| 已完成 | 装备元数据与查询体验补全 | 用官方物品数据补齐装备中文名、图标、来源状态和掉落查询默认视图。 | 玩家在装备模拟器、职业装备页和装备查询里看到真实中文装备与图标，不再被英文名、空白掉落或展示型参考误导。 | 接入 Battle.net Game Data API 缓存；补齐别名映射、官方英文名精确检索、复合装备行拆分；区分 verified item、source_reference 和 fallback；装备查询优先定位有掉落的首领。 | WebSim 装备候选、地下城掉落和职业装备页均显示中文名/图标；Archon/Wowhead 参考行标记为 source_reference；空掉落首领不会作为默认空白结果；相关测试和远端 smoke 通过。 | `server/websim_payload.py`, `server/builds/home-payload.js`, `server/news_backend.py`, `pages/builds/detail.*`, `websim/app.js`, `websim/app.css`, `tests/websim_payload_test.py`, `tests/builds-page.test.js`, `tests/websim-page.test.js` |
| 正在推进 | 模板化构筑与固定 SimC 输入 | 将天赋字符串、装备字符串和职业/专精元数据沉淀为可保存、可加载的构筑模板，并让 SimC 作为独立消费者读取已保存模板。 | 玩家可以保存自己的天赋/装备模板，也能加载社区经典模板，快速组合并提交可信 SimC，不必每次从聊天或手动拼接开始。 | 已完成社区大秘模板 v1：独立社区模板模型、manual fixture 同步、Raider.IO / Warcraft Logs adapter 骨架、缺凭据状态、天赋模拟器社区模板 sheet、WebSim 可视化应用、SimC-only 外部导入码上下文保留、社区模板签名去重与择优；个人模板库 v1 已接入本地保存/去重/汇总/删除和账号模板同步基础；天赋构筑和装备模拟只生产模板 `rawString`，不再现场拼 SimC profile 或发起 SimC handoff；SimC 页面已改为非开放式模板组合表单，先选职业并只展示该职业下的天赋/装备模板，再固定读取 1 个天赋模板、1 个完整装备模板和少量场景预设。 | 模板记录包含类型、职业/专精/英雄天赋、场景、版本、来源和 raw string；天赋模板保存 WebSim/官方导出码，装备模板保存完整 16 槽 canonical 配置字符串；SimC 提交前由后端确定性转换并校验职业/专精一致、装备完整、slot/id 可解析和 `websim:` 天赋编码，confirmOnly 与 final submit 复用同一 payload；社区模板标注来源和适用赛季且同一可视化构筑不会重复占据列表；下一步补齐角色归档和跨端冲突合并。 | `server/websim_payload.py`, `server/community_talent_sources/`, `pages/common/build-template-storage.js`, `pages/profile/`, `pages/builds/talent-simulator.*`, `pages/builds/talent-simulator-core.js`, `pages/builds/websim-api.js`, `pages/builds/detail.*`, `pages/simulator/simc.*`, `server/news_backend.py`, `server/simulator_payload.py`, `tests/build-template-storage.test.js`, `tests/profile-auth.test.js`, `tests/news_backend_test.py`, `tests/websim_payload_test.py`, `tests/talent-simulator-core.test.js`, `tests/builds-page.test.js`, `tests/simulator-page.test.js`, [ideas.md](roadmap/ideas.md) |
| 已完成 | 职业专精内嵌装备模拟 | 将职业专精详情页里的“装备获取”模块升级为“装备模拟”，不新增独立小程序页。 | 玩家在当前职业/专精上下文里替换各部位装备，并把完整装备方案保存成可复用配置字符串。 | 已完成权威装备库 v1、社区装备模板导入与线上全职业候选收口：复用 `wow-websim-sync` 与社区模板同步链路，接入 Battle.net Game Data API、Raider.IO CN observed profile variants、SimC preset 装备模板和 stable signature 去重；`/api/websim/gear` 输出目录状态、来源、变体、observed refs、blockers、`communityTemplates` 和覆盖率，并过滤非通用槽位的跨职业 / 跨专精候选。装备页恢复调用 `/api/websim/gear/stats`，但只在完整装备和天赋导入码都存在时按签名请求 SimC JSON 角色快照；本地汇总不再伪造副属性百分比。 | 当前详情页可展示 13 职业 / 39 专精装备模拟状态；每个槽位能按全部/大秘境/团本筛选候选装备并选择来源、变体、宝石和附魔；难度/装等使用英雄、史诗、难度待补等中文文案；候选详情只展示玩家可理解的掉落来源和装备属性，真实实例属性来自 SimC JSON gear output，Battle.net metadata 用于中文名、图标、护甲/武器类型、职业限制和来源核验，低等级 preview stats 不伪装成当前实例属性；制造来源通过 governed `crafted` catalog 回到候选与筛选，仅在当前槽位存在制造候选时显示，并要求选择 verified 装等轨道和 `crafted_stats` 属性搭配后才可应用；宝石/附魔不使用默认假 seed，只有真实插槽或可附魔部位才展示对应选项；必须补齐 16 个 canonical 槽位才能保存装备配置字符串；静态掉落、制造属性搭配无确定 SimC 字段或 SimC item resolution warning 时保持 partial/blocker，不伪装为可执行或 verified 百分比；线上 smoke 已验证 13 职业 / 39 专精、624 个槽位全部非空且无 incompatible 候选，社区装备模板 sheet 不再溢出。 | [内嵌装备模拟设计](plans/2026-06-17-builds-inline-gear-simulator-design.md), [装备属性百分比 SimC JSON 快照方案](plans/2026-06-26-gear-stat-simc-json-snapshot.md), `pages/builds/detail.*`, `pages/builds/websim-api.js`, `pages/common/game-asset.js`, `server/websim_payload.py`, `server/news_backend.py`, `tests/builds-page.test.js`, `tests/frontend-api-client.test.js`, `tests/websim_payload_test.py`, `tests/news_backend_test.py`, remote smoke: `/api/data/health`, `/api/websim/gear?class=monk&spec=brewmaster&compact=1` |
| 正在推进 | 个人模板库 v1 | 把天赋模拟器和装备模拟器产出的可复用输入沉淀到“我的”tab。 | 玩家能看到自己保存过的天赋/装备模板，并清理不再使用的模板，为后续一键加载和角色化工作台打基础。 | 新增本地模板存储 schema；天赋模拟器保存天赋导出码 `rawString`；装备模拟保存完整 16 槽装备配置 `rawString`；个人页展示天赋/装备模块、最近模板、状态标签和删除确认；后端 `/api/me/build-templates` 已提供账号 owner 隔离的 list/create/delete，继续用 `user_id + template_type + raw_string` 去重，前端在 HTTPS/合法域名可用时同步，明文 HTTP 下拒发 Bearer token。 | 本地与账号 save/list/dedupe/delete 基础流程有测试覆盖；模板卡片显示职业/专精/场景/状态和保存时间；新模板状态使用 `saved` / `complete`，旧 `encoded` / `simc_ready` / `blocked` 仍可读取和删除；下一步接入模板加载、角色维度归档和跨端冲突合并。 | `pages/common/auth-client.js`, `pages/common/build-template-storage.js`, `pages/profile/profile.*`, `pages/builds/talent-simulator.*`, `pages/builds/detail.*`, `server/news_backend.py`, `tests/build-template-storage.test.js`, `tests/profile-auth.test.js`, `tests/builds-page.test.js` |
| 正在推进 | 账号化数据边界与 CRUD 权限 | 以微信账号体系作为后续所有用户数据增删改查的统一 ownership 边界。 | 用户的模板、角色、收藏、订阅、模拟任务和数据源设置不会混在游客态或本地态里，跨端/换机后也能形成稳定个人资产。 | 复用现有 `wx.login`、`wechat_users`、`auth_tokens` 和 Bearer token；已先把 `user_build_templates` 接入 owner 字段、鉴权、去重和跨账号隔离；保留只读/游客预览，但写操作必须登录；本地模板迁移需要明确归属确认。 | 模板写接口已要求有效账号且列表/删除只访问当前 owner；HTTP/insecure 环境不得发送 Bearer token；下一步把同一规则扩到角色、收藏、订阅、任务和数据源设置，并补齐权限测试。 | `pages/common/auth-client.js`, `pages/common/api-client.js`, `server/news_backend.py`, `tests/frontend-api-client.test.js`, `tests/news_backend_test.py`, [ideas.md](roadmap/ideas.md) |
| 正在推进 | 数据库架构治理与迁移路线 | 在继续扩展账号化 CRUD 前，先明确当前 SQLite 单库的域边界、迁移机制、owner 规则、缓存/analytics 边界和未来拆库条件。 | 后续模板、角色、收藏、订阅、任务等用户数据不会和可重建缓存、analytics、同步状态混成一团，线上变更也有备份和回滚路径。 | 保留 SQLite 作为近期方案；已新增数据库架构文档；引入 `schema_migrations`；启用 foreign key、busy timeout 和 WAL；为用户写表规定 `user_id` owner；首个账号表 `user_build_templates` 已接入；备份/迁移 runbook 和拆库条件已记录。 | 数据库架构文档和首批 migration/owner 测试已存在；新增用户数据表前仍必须先有 migration/version 记录；cache 表可重建、user 表不可丢、analytics 可归档的边界清晰；达到明确指标前不盲目换数据库。 | [数据库架构治理计划](plans/2026-06-17-database-architecture-governance.md), [数据库架构文档](database-architecture.md), `server/news_backend.py`, `server/websim_payload.py`, `server/analytics.py`, `docs/remote-debugging.md`, `tests/news_backend_test.py` |
| 下一步 | Roadmap 与想法池常态化 | 把项目目标、已完成动作、下一步和讨论想法放到同一套文档体系。 | 后续迭代有稳定上下文，减少反复解释和方向漂移。 | 维护本文件；新增想法池；定期把完成项和新想法同步进 roadmap。 | `docs/roadmap.md` 能回答目标、现状和下一步；`docs/roadmap/ideas.md` 能收纳待确认想法。 | [ideas.md](roadmap/ideas.md) |
| 正在推进 | 数据可信度与赛季同步完善 | 明确哪些数据来自官方、SimC、Wago、日志站或本地 fallback。 | 玩家能知道数据是否新鲜、可信，避免看到过期赛季内容。 | 已完成装备物品官方元数据、source_reference 分层和 Battle.net Game Data API 真实同步；官方 item preview stats 已作为装备属性展示来源；官方 journal loot 只补静态来源与 metadata，缺少确定 `bonus_id/gem_id/enchant_id/crafted_stats` 时保持 partial；Raider.IO observed gear 只有真实实例字段完整时才生成 verified SimC variant，装备实例属性必须由 SimC JSON gear output 或 Battle.net Game Data 产出，Raider.IO/WCL 角色明细只作为观测配置、链接和数据校验来源，不作为生产属性来源；默认假宝石/附魔 seed 不再进入装备候选，宝石/附魔必须由真实插槽、可附魔部位或明确服务端数据支持；当前确认把自有 `Season Data Cache` 作为可重建同步/索引/对账层，所有装备、宝石、附魔和变体数据都要带来源、赛季 revision、检查时间、状态和 blocker；PVE 职业天梯已补充 Archon/WCL 来源校验 UI 和 `verified / partial / stale / blocked` 契约；职业专精属性权重已接入 Raider.IO 样本 + SimC scale factors + LLM 中文解释缓存，并记录样本量、profile 成功数、SimC build、translationStatus 和 blockers；SimC 报告与 WCL 入口已开始输出 evidence/report/status 字段；2026-06-23 已确认当前赛季池修复范围：M+ 纳入 Magisters' Terrace、Maisara Caverns、Nexus-Point Xenas、Windrunner Spire、Algeth'ar Academy、Pit of Saron、Seat of the Triumvirate、Skyreach，团本纳入 The Voidspire、The Dreamrift、March on Quel'Danas、Sporefall；后续采用逐副本 / 团本切片补齐自建装备数据库，先修赛季池门禁，再按实例推进覆盖率和全职业验收；2026-06-24 已固化装备自建数据库治理标准，12.1 更新按 DB-first、verified 属性、partial/blocker 上报和发布审计清单执行；制造业装备回到自建库时必须通过受控 catalog seed/import、SimC JSON gear 属性验证和 `crafted_stats` 选项覆盖率进入 health。 | 关键页面展示数据状态；过期或未验证数据不会伪装成当前赛季；当前赛季副本 / 团本池必须以官方 allowlist / revision 校验，错季、缺失或漂移时 blocked；自建装备数据库按实例暴露覆盖率、verified / partial 计数和 blocker；任何 `partial` / `blocked` 或 blocker 都必须告知项目 owner，并带具体 item、slot、variant、source、原因和补齐路径一起分析，线上可放心使用的目标是玩家可见装备明细全部收敛为 verified；官方/SimC/Wago/Archon/WCL/Raider.IO 对账不一致、样本不足或缺可执行字段时标记 blocked / stale / partial / incompatible；自有库不得人工发明 BiS 或强度结论，只能保存规范化证据和读取模型，并阻断真实 SimC、WCL 分析或强度结论。 | [装备自建数据库治理标准](gear-database-governance.md), [逐副本切片计划](plans/2026-06-23-season-gear-instance-slice-plan.md), `server/websim_payload.py`, `server/news_backend.py`, `server/stat_weights_payload.py`, `server/pve/home-payload.js`, `server/simulator_payload.py`, `pages/pve/detail.*`, `pages/builds/detail.*`, `docs/roadmap/ideas.md` |
| 正在推进 | 数据与模拟健康哨兵 | 把 SimC 程序版本、Battle.net 赛季/天赋/装备数据、社区模板、WCL 凭据和端到端 SimC 兼容性纳入统一巡检。 | 玩家和维护者能知道当前模拟结果是否建立在最新且兼容的数据面上，避免 SimC 版本、天赋树、装备字段、模板或日志凭据过期时继续给出可信结论。 | 已新增只读 `/api/data/health` 聚合接口，展示新闻、Raider.IO、WebSim、社区模板、Stat Weights、Battle.net API、WCL credentials 和 gear_catalog 状态；本轮修正 `websim_sync` 新 state 口径，成功同步后不再误报 `websim cache has not been synced`；gear_catalog details 已输出 slot coverage、source coverage、observed variant count、variant readiness、宝石/附魔覆盖率、top blockers 和 blockers；`gear_catalog` 与新增 `talent_catalog` 已开始暴露统一 `catalogContract`，其中 `talent_catalog` 从本地天赋节点、法术详情、SimC profile presets、社区模板和 sync state 派生覆盖率、revision、sourceStatus、officialAuditStatus 与 blockers；继续复用现有 `wow-simc-version-check`。 | 当前 health surface 已能表达多源状态、装备库覆盖率和天赋 catalog 覆盖率；后续完成标准是每次巡检记录 checkedAt、SimC local/latest commit、seasonRevision、talentSchemaRevision、itemMetadataRevision、templateRevision、WCL credential status、gear/gem/enchant coverage、variant readiness、talent coverage、spell detail coverage、source drift、top blockers 和 compatibility status；不兼容、过期或缺可执行实例时阻断真实 SimC/WCL 结论并给出确定性原因。 | `server/deploy_lighthouse.sh`, `server/news_backend.py`, `server/websim_payload.py`, `server/game-season.js`, `pages/simulator/simc.*`, `docs/roadmap/ideas.md`, [talent catalog health contract plan](plans/2026-06-22-talent-catalog-health-contract.md) |
| 下一步 | 全职业天赋规则校验与追踪机制 | 为“全职业天赋规则权威层”建立持续验证、漂移追踪和失败归因机制。 | 玩家不会因为底层规则漂移遇到错误的不可选 / 可选状态；维护者能快速定位是缺树、缺边、缺 entry、点数门槛、choice 冲突还是官方对账差异。 | 建立 authority matrix runner 和规则回归样本库，固定冰法暴风雪、急咒师左链、多父 OR、choice 互斥、默认赠送点、降点裁剪等核心样本；记录 `talentSchemaRevision`、`simcBuild`、`traitEdgeSource`、`officialRevision`、`diffStatus`、`checkedAt`、失败节点和阻断原因；输出 CI / health / admin 可读报告；Blizzard API 凭据到位后再接入官方对账。 | 每次规则或数据更新都能跑全量 13 职业 / 39 专精 / 英雄树矩阵 smoke；失败项带 class/spec/hero/node/reason；任何缺树、缺边、缺 entry 或对账差异都不会被标为 verified；真实 SimC 提交复用同一阻断原因。 | `server/websim_payload.py`, `server/news_backend.py`, `tests/websim_payload_test.py`, `tests/talent-simulator-core.test.js`, `docs/roadmap/ideas.md` |
| 下一步 | 正式发布与刷新可观测 | 补齐 HTTPS、微信合法域名、443、刷新 run 管理和 health check。 | 体验版/正式版能稳定访问，刷新失败和环境问题可被定位。 | 配置正式域名；移除小程序手动刷新入口，公共刷新接口只接受 scheduled；记录刷新错误；拆分网络、SimC、LLM、参考数据探测。 | 正式环境不依赖 HTTP IP；刷新记录可查；部署后 smoke 有明确步骤。 | [news-architecture.md](news-architecture.md), [remote-debugging.md](remote-debugging.md) |
| 后续 | 职业 / PVE 数据采集自动化 | 将当前结构化 payload 逐步替换为定时采集、校验和数据库化数据。 | 构筑、PVE 和资讯能跟随版本、热修、赛季和日志样本更新。 | 接入 Raider.IO、Warcraft Logs、Archon、Subcreation 等可信源；优先补齐职业天梯的 Archon tier、WCL statistics、样本量、更新时间和角色分类映射；属性权重已先行建立独立日更 timer、SQLite cache、全职业专精遍历、代表 profile 选择、SimC scale factor 解析和 LLM JSON guard；后续继续扩大真实数据源与远端 smoke。 | 人工种子不再承担“最新结论”；每条结论都有来源、分析窗口、样本量、更新时间和校验状态；低样本、SimC 不可执行或翻译失败时输出原因而不是强结论。 | [builds-architecture.md](builds-architecture.md), [职业天梯 UI 契约](plans/2026-06-18-pve-spec-ladder-ui-contract.md), `server/pve/`, `server/stat_weights_payload.py`, `server/stat_weights_sync.py` |
| 后续 | 角色绑定与个人化工作台 | 从通用查询推进到玩家自己的角色、收藏、订阅和任务记录。 | 玩家能围绕自己的角色持续分析，而不是每次从零开始。 | 设计角色资料、订阅、收藏、历史任务和权限边界；所有个人数据 CRUD 默认基于微信账号 owner。 | 角色数据模型、隐私边界和账号归属明确；至少一个个人化场景可用；跨账号不能读取、修改或删除彼此数据。 | `pages/profile/`, `simulator_tasks`, `wechat_users` |
| 待规划 | WCL / 日志复盘链路 | 将战斗日志分析纳入 SimC 与 AI 报告链路。 | 玩家能把“理论收益”和“实战问题”放在同一份报告里看。 | 已完成日志输入启动层：WCL 页面和后端可接收 report URL/code/fight；缺 report 时提示 missing_input，缺 `WOW_WARCRAFTLOGS_CLIENT_ID/SECRET` 时 blocked/missing_credentials；v2 OAuth/GraphQL 启动层可抽取 report/fight/events 摘要并在失败时 deterministic blocked，不会在无证据时调用 LLM 或伪造排名；首版智能分析 tab 暂时下掉“分析 WCL”，页面不再注册到小程序入口。 | 后续恢复前需要重新确认凭据/授权、样本窗口、buff uptime 与机制事件归因的产品边界，并保持没有 report、无权限、低样本、GraphQL 失败或凭据缺失时输出确定性阻断。 | `pages/simulator/`, `server/simulator_payload.py`, `pages/simulator/wcl.*`, `tests/news_backend_test.py`, `tests/simulator-page.test.js` |
| 正在推进 | LLM 报告 schema 化 | 从自由文本 prompt/report 逐步转为 evidence JSON、允许数字列表和结构化输出。 | AI 报告更稳定，不会编造或误读数字。 | SimC 分析已定义 `report` schema、`allowedNumbers` 和 deterministic fallback；LLM 输出必须符合 schema 且不能使用 allowedNumbers 之外的数字，失败时回退后端报告。下一步把同一 schema 扩到 WCL、任务详情和前端完整报告视图。 | LLM 输出每个数字都有后端证据；失败时仍有可读报告；WCL、SimC 和参考数据的 evidenceRefs 能在 UI 中被追踪。 | [SimC 风险规避](plans/2026-06-11-simc-flow-risk-avoidance.md), `server/simulator_payload.py`, `tests/news_backend_test.py`, `tests/simulator-page.test.js` |
| 正在推进 | 炸鸡队长证据教练 v0 | 将分散在 SimC、WCL、装备/天赋模板、属性权重、PVE 数据状态和专精打法画像里的证据编排成面向玩家的下一步提升建议；英文 key 统一为 `chickenbro`。 | 玩家可以围绕当前构筑、模拟结果、日志输入和当前专精场景获得优先级明确的调优方向，而不是在多个页面之间手动拼上下文。 | 已完成证据受限 v0 与后台架构骨架：保留 simulator `mode=chickenbro` 的确定性 schema，同时新增独立 `/api/chickenbro/*`、轻量会话、`agent_jobs`、结构化用户画像和 `SpecPlaystyleProfile` envelope+JSON；后端负责话题边界、画像门禁、bounded context、CodexCli 输出校验和失败降级，非 WoW、闲聊或无关话题回避；缺 WCL、SimC、角色/装等/场景或已发布画像时只输出缺失项和通用建议。 | 不编造排名、DPS/HPS、日志发现或玩家分位；`published` 画像可支撑结论，`partial` 只做带免责声明的背景，`stale/blocked/needs_review` 不进入结论；CodexCli 不直连数据库、API key、外部 API 或原始日志；所有数字事实来自 SimC、WCL、Raider.IO、属性权重、画像 allowedNumbers 或其他可追踪证据；相关 schema、拒答边界、会话/任务 owner 隔离和降级有测试覆盖。 | [并行优化控制计划](plans/2026-06-21-parallel-optimization-control.md), `server/simulator_payload.py`, `server/news_backend.py`, `pages/simulator/`, `tests/news_backend_test.py`, `tests/simulator-page.test.js`, `docs/roadmap/ideas.md` |
| 暂缓 | 深度 Codex Agent 工作流 | 将 Codex worker 用于低频复杂任务，如 profile 修复和跨文件证据复核。 | 复杂问题可由 agent 深挖，但不会拖慢高频普通分析。 | 保持普通 LLM、SimC runner、Codex worker 职责分离。 | 有明确触发条件、任务隔离、超时和结果展示后再扩大使用。 | [SimC 风险规避](plans/2026-06-11-simc-flow-risk-avoidance.md) |
| 待决策 | 产品定位命名 | 明确项目对外是“资讯小程序”“构筑模拟器”还是“玩家分析工作台”。 | 对外表达更清晰，后续 UI、文案和优先级更统一。 | 补录本地讨论想法；确定核心用户和主场景。 | Roadmap 中有一句稳定的产品定位，并同步到 README。 | [ideas.md](roadmap/ideas.md) |
| 待决策 | 首页与 tab 权重 | 明确资讯、职业专精、PVE、WebSim/SimC、我的五条线的主次关系。 | 后续产品体验不会每条线平均用力，而是围绕主场景组织。 | 结合用户场景决定首页入口、默认工作流和导航文案。 | Roadmap 中确定“第一主线”和“辅助能力”。 | [ideas.md](roadmap/ideas.md) |

## 维护规则

- 本文件只维护长期方向、阶段状态和高层动作；具体执行步骤继续放在 `docs/plans/`。
- 新想法先进入 [roadmap/ideas.md](roadmap/ideas.md)，不要直接塞进里程碑。
- 当一个想法被采纳，移动到本文件的相应里程碑，并补上完成标准。
- 当一项工作完成，只更新状态和证据链接；不要改写历史计划文档。
- 无法从仓库或当前对话确认的历史想法，统一标记为“待补录”或“待确认”。
- 每次讨论后，如果用户明确说“OK”“认可”“就按这个”或等价确认，Codex 应主动把该想法纳入 [roadmap/ideas.md](roadmap/ideas.md) 或本文件；如果当轮不适合直接编辑，应明确提醒用户是否纳入 roadmap。
