# 通用研究证据与语义优化实施计划

状态：正在推进。用户已确认通用证据复用方向，并授权五项优化、提交、合入及发布。

目标：跨轮保留不同战斗窗口与用户约束，统一工具额度，增加可核验的结果声明检查与固定语义评测。
架构：复用现有WCL/SimC工具、持久消息和研究账本，不新增依赖、引擎、数据库或公开业务域。历史资料和用户表述始终是不可信输入，不成为系统权限。

- [x] 通用证据：research_evidence.py按报告/战斗/角色/视图/窗口/过滤条件保留独立证据，包含已有伤害/施法/资源摘要，完整性与截断显式传递。先复现窗口丢失，再验证整场/窗口、不同过滤、partial、空间上限和所有权。
- [x] 研究上下文：增加research_context.py，投影当前研究中最近20条之外的用户目标/修正原文，保留消息身份、先后顺序与裁剪标志；不把模型生成摘要作为用户授权。测试长对话、修正、新研究边界和注入隔离。
- [x] 额度：统一MCP说明为每研究4任务；研究状态提供范围已用/剩余与单轮执行保护。测试实际累计及状态投影。
- [x] 声明校验：收集本run owner-scoped SimC工具结果，校验明确完成/结果数字声明，有限错误进入现有一次修复流程；一般机制、假设、否定与未知不能误拒。不宣称完整因果语义验证。
- [x] 固定语义集合：保留已有3题并扩充到约20题；意图、俗称、错字、追问、修正、窗口、预算与执行边界均有人工rubric；自动合同和人工语义结果分开。
- [ ] 验证与发布：相关测试先红后绿，后端与控制面检查，独立PostgreSQL/隔离Candidate和限定真实模型验收；精确manifest、旧版本及环境恢复材料、排空门禁同时切API/Worker；线上业务与Web文件哈希核对。

验证预算：固定20题为长期集合，本轮先选6条无外部查询语义题与2条真实工具/业务题；失败按具体原因修复，最多两轮定向复验，不穷举、不绕过研究预算。不新增SimC实验，优先复用已完成任务核验结果读取/对照；必要新模拟最多1个云端业务smoke。未覆盖范围单列。

执行记录：基底c79b53d83；main无跟踪文件修改，既有未跟踪证据保留；隔离worktree为.worktrees/research-generalization。通用证据和评测由有界子任务实现，主任务负责上下文/声明/集成发布；所有提交由主任务统一完成。

本地/隔离单元验证：80项核心本地定向通过；63项控制面通过。云端已有运行环境执行同源码788项后端通过，独立Candidate数据库13项研究PostgreSQL通过；本地完整后端因缺少FastAPI等依赖未通过，未新增安装。两项独立审查发现（条件语句误拒、最近窗口边界指代丢失）均先复现后修复，17项定向复核通过。
