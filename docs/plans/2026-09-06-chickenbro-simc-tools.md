# 鸡哥按需调用云端 SimC

状态：正在推进（测试环境已验证，待用户验收）。2026-09-06 用户明确要求参考已验收的研究工具方式，将 SimC 按需接入鸡哥；本计划记录该执行授权。

## 正常与异常路径

鸡哥理解模拟请求后准备当前账号拥有的角色快照，展示/说明使用的角色和场景，调用正式 SimulationApplication 提交任务；查询进度与语义结果，结果与现有 Mini/Web SimC 列表共享。用户已经要求执行时不再机械要求确认。只讨论/解释时不自动跑任务。输入缺失报告真实 blocker，不猜等级/装备/天赋，也不把 API 未接入和输入不完整混淆。

范围包括单次模拟及图2所需的同快照宝石对照、固定时长/目标数、结果误差说明。只用现有云端 runtime、PostgreSQL queue 和 Worker，不新建平行任务系统，不安装或本地运行 SimC，不接入旧 simulator API。

## 实施

1. 源与可执行输入核验：检查真实 Raider.IO 快照，补齐可以从可信公开来源核对的缺字段；无法核实则保留 blocker。验证现有云端增强专精能力，不能仅改配置冒充 runtime 通过。
2. 场景/compiler：有界时长和宝石槽位替换，保留快照与原天赋装备；场景哈希涵盖全部变更。旧请求语义和既存任务可读；结果保留真实误差与 provenance。
3. Chat 工具：按服务器 Principal/会话/run 绑定短期 capability，模型不传身份；prepare/submit/get/list 经 application，幂等提交、有界任务数与轮询、结束撤销。跨用户 ID 不可读写。
4. MCP 与规则：暴露按需工具，解释/准备不冒充执行、排队不冒充结果、个人对照结论遵循差值与误差。通用工具优先，不为增强萨硬编码答案。
5. 验证：隔离工作树、针对回归、云端既有依赖的全后端/控制面、独立代码审查；测试 release 保留回滚，真实 Astra 发起任务、云端 SimC 完成、同账号列表/结果核对。正式切换和用户验收单独记录。

## 已知基线

基于已验收研究分支 a1201c15；原工作区保持3be4e6cb。本轮开始时 Drakthul 真实来源返回 CHARACTER_LEVEL_MISSING 和 RUNTIME_UNAVAILABLE；必须调查这些限制，不能仅新增工具名称后宣称可用。

## 当前验证

- 运行代码：5e4e7322；隔离测试 API/Worker 已部署，生产未切换。
- 云端精确源码后端 336 项、控制面 62 项、独立审查和针对回归通过。
- 原 Web 会话 Astra/high 在 179.414 秒内完成同快照四项真实模拟；原装/精通对照，单体 300 秒与 5 目标 120 秒，每项 10,000 次。结果与同账号 SimC 列表、详情一致。
- 修复真实运行发现的 MCP 环境转发和按工具授权；没有更改全局审批或把写操作标为只读。
- 证据见 ../../artifacts/releases/2026-09-06-chickenbro-simc-tools/。本功能用户验收仍单独保留；2026-09-06 用户已授权本轮仓库整合合入 main，这不代表生产切换或未测项目验收通过。


## 任务 ID 与换装重跑（2026-09-08）

用户要求 Web 任务列表显示并复制完整任务 ID，可把 ID 发给鸡哥换装重跑，同时添加 FAQ 使用示例。本节为该后续任务的范围授权。

- Web 列表显示完整 ID，独立复制按钮不触发报告导航；成功反馈，失败保留可选文本供手动复制。
- `submit_simulation` 接受 `snapshotId` 或 `baseJobId` 二选一。基于任务重跑时经原 application 的 Principal 校验归属；复用不可变快照与已存场景，合并明确变更，并由原幂等/预算/队列流程创建新任务。历史场景缺失拒绝推测。
- `equipmentOverrides` 按槽位指定完整物品 ID、装等、bonus IDs、宝石和附魔，只允许有界数字结构。生成 profile 指定替换物品装等；其余原输入保持不变。继续基于变体重跑时保留未改槽位；新装备不继承旧宝石覆盖。`get_simulation_job` 返回有效装备供核对。
- 编译语义新增 v4，仅 v4 接受整件装备替换；v4 worker 可按原 revision 处理旧 v1/v2/v3 任务。运行引擎保持不变，未启动本地 SimC；生产编译配置按下方发布记录更新。
- 装备参数必须由用户明确资料或可靠来源核验；未实现制造属性、美化等任意输入，不能猜补。FAQ 展示操作示例，不冒充已完成的历史换装案例。

### 发布条件与结果

部署经授权的 API/Worker/Web，API 与 Worker 同步设置 `WOW_SIMC_COMPILER_REVISION=chickenbro-simc-compiler-v4`。先完成 Candidate 中真实鸡哥按 ID 换装、云端 SimC 正数结果、有效装备/保留参数核对、基线与新任务对比、其他账号隔离，再切流。2026-09-08 用户明确授权“是的，部署”，上述后端 Candidate 与正式真实任务链路已通过；Web 在正式页面验证。

回滚保留旧 Web/API/Worker 与原编译配置。回退至不认识 v4 的 worker 前必须停止新提交并处理完 v4 队列；不得让旧 worker 领取新语义任务。无需数据库 schema 迁移，旧任务/快照不修改。

正式发布源补丁 f2932f8b6，七文件后端覆盖与完整 Web 产物、真实任务/隔离验证及回滚点见 [发布证据](../../artifacts/verification/2026-09-08-faq/README.md)。旧客户端不请求 scenarioVersion=2 时不返回新 equipmentOverrides 字段，避免已发布小程序列表与详情校验失败；Chat 工具仍读取完整真实配置。用户体验验收与仓库合入/推送尚未进行。
