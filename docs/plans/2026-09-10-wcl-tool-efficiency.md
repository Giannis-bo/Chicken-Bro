# WCL 工具效率优化

状态：已合入 main、推送并发布，隔离与公网业务验证通过。授权：用户于 2026-09-10 批准第一批优化与测试；随后要求适可而止，停止扩样本。结果见[验证记录](../../artifacts/verification/2026-09-10-wcl-tool-efficiency/README.md)。

## 范围与验收

- 保留默认完整返回，新增概览、事件和窗口统计视图；精简后仍有报告、角色、窗口、分页与来源身份，不把未返回字段当作零。
- 单轮相同 WCL 只读查询复用及并发合并；60 秒有效期、16 项/4 MiB 上限，不跨运行或账号；不缓存失败、partial，返回副本。
- 有界自动分页统计施法次数、资源浪费、有效治疗与过量治疗。显式窗口/角色、字段缺失、截断、分页未完和失败均保留覆盖说明；不推导有效治疗提升或操作因果。
- 不改 SimC 提交、账号权限、队列、数据库或客户端；不增加依赖。

## 验证顺序

先复现旧路径重复请求/冗余返回，再逐项测试实现。固定测试包括多页治疗与资源窗口、角色/时间变体、并发去重、过期、独立运行、失败重试、截断/非推进游标、旧默认合同、来源校验及混合批量。使用独立保留样本检查分页边界及数值缺失。比较相同输入的请求数、UTF-8 返回字节与确定性统计，不把 fixture 提速称为真实模型提速。

本地目标测试和后端/控制面通过后，尽可能用隔离真实只读来源/模型回放验证工具选择和答案覆盖；分别报告未验证环境、真实耗时和泛化限制。初始阶段保留 WIP，不推送、合入或切换生产；后续发布按下方新授权执行。

## 发布收尾（2026-09-10）

用户明确授权“是的，提交、合入、发布”。运行源码 `f95f6bbe234a2da8a67ca86466e96c6c29c6b794` 已合入并推送 main，五个后端文件按精确 overlay 发布；API/Worker 同步切换，原配置及 Web 保持不变。复用 667 项后端和 63 项控制面测试，补充隔离 QQ/CSRF、图片/SSE、账号隔离、真实 SimC 指标与来源，以及隔离/公网各一次 WCL 统计业务验证，全部通过。公网 14 个 Web 文件哈希一致。旧源码与私有恢复配置保留，未执行生产回切演练，也不宣称新人工 QQ 登录或用户体验验收。按用户要求停止扩样本。

[发布证据](../../artifacts/verification/2026-09-10-wcl-tool-efficiency/release/README.md)。
