# 炸鸡队长运行规则

状态：`正在推进`。2026-09-05 用户授权：整理专用 AGENTS.md，禁止魔兽世界以外的话题，回答精炼。

## 方案与边界

- 将业务规则集中到 `server/app/chickenbro/agent/AGENTS.md`，不修改仓库根工程指南或云端全局规则。
- Chat adapter 每次调用读取服务端固定文件，通过 Codex `developer_instructions` 注入；读取失败、空文件或超限时拒绝启动。
- 用户历史单独编码为 JSON；保留最近 20 条、每条 4000 字符边界。公开检索、WCL/Raider.IO 路由规则迁入同一文件。
- 保留认证、owner 隔离、工具网关和 read-only sandbox。规则不代替服务端权限控制。

## 步骤与验收

1. 先补注入、热加载、失败关闭和历史分离测试，再实现。
2. 运行 Chat adapter/application、后端及控制面测试，审查 diff。
3. 云端隔离 Candidate 验证无关问题、规则绕过、混合问题及正常魔兽问题；核对源文件 SHA。
4. 仅同步规则及其三个接入文件到正式/测试服务；精确核对旧文件并备份，保留回滚清单，不带入其他分支改动。
5. 验证正式调用，记录实际回答、运行身份与健康；用户体验验收、合入 main 单独记录。

回滚：按本次精确 manifest 恢复三个原 Python 文件及原规则文件状态，重启对应 API；不触碰数据库、模型配置和客户端构建。

## 初始规则接入记录

- 接入 commit：`1209850cdb6b601669706602603156a00c15d493`；本地专项 38 项、控制面 62 项通过，云端已有依赖环境执行后端 243 项通过。完整 adapter 测试在 Windows 有既有环境变量大小写与路径分隔符差异，Linux 全套通过。
- 独立代码审查未发现本次可操作缺陷。真实 Candidate 四组调用：范围外拒答、忽略规则拒答、正常魔兽简答、混合问题仅回答魔兽内容。
- 正式和测试均已同步四文件、重启 API，并核对 readiness 所有组件 ready。测试切换时因检查地址错误触发过自动回滚，修正后成功。
- 自动运行验证与用户体验验收分开；本任务尚未合入 main。具体文件身份、回答证据和回滚入口见 [验证记录](../../artifacts/releases/2026-09-05-chickenbro-agent-rules/verification.md)。

## 自动使用服务端 API（2026-09-05 用户确认继续落地）

用户要求通用规则，分析过程交由原生 Codex 自主判断，不添加“玩家分析必须确认角色/战斗/覆盖范围”的固定行为门槛。

本次修复已定位的网关端口及 cn.warcraftlogs.com 解析遗漏，沿用已有服务端凭据加载和 OAuth 刷新；工具返回角色标识与技能统计供 Codex 自主选择后续查询。事件样本范围仍作为工具数据属性如实返回，不作为回答流程门槛。不得扫描服务器密钥或向用户索要已有配置。

验证：先复现端口/国服链接/数据透传失败，再跑来源与后端回归；隔离 Candidate 使用相同 WCL 报告验证服务端真实 API，再用真实 Codex 验证自动调用。云端只同步本次相关文件，核对精确 SHA，保留旧文件与回滚，保护此前测试登录和 Web 顶栏成果。

- 实现 commit：`491d5379c5e7766f8e9260e3c6c9e7d8036c73f5`；30 项来源专项、247 项后端、62 项控制面通过。独立审查发现未指定 fight 时误归因首场，已修正并补回归，复核通过。
- 真实隔离 Codex 使用用户原问题自动调用两次 WCL 工具（报告、source=4），无需提示使用 API，均返回 verified 并输出技能统计分析。
- 正式/测试已按五文件清单部署并重启 API；测试继承此前 Web 顶栏构建，凭据及数据库未修改。运行记录见 [自动来源验证](../../artifacts/releases/2026-09-05-chickenbro-agent-rules/auto-sources.md)。

## 消息展示与 WCL 数据补查（2026-09-05 用户反馈）

修复 Web 回复纯文本展示 Markdown、无法选择复制的问题：语义渲染常用 Markdown，正文可选择，每条完成消息提供原文复制；流式和历史使用同一渲染。原始 HTML 不执行，仅 HTTP(S) 链接可打开，不新增依赖。

WCL 现有查询遗漏 playerDetails 和版本字段、只有首批事件且无继续查询参数。补充角色装备/天赋/属性，支持事件类型、报告相对起止时间及分页数量；工具按需补查，保留分页范围元数据。gameVersion/logVersion 不冒充具体补丁号。规则仅明确“未查询”不等于“无法提供”，不固定玩家分析流程。

验证包含 Markdown 安全和结构、复制状态、真实 DOM/浏览器选择、来源参数全链路和真实报告后续页；先隔离验证，再按精确文件部署与保留回滚。main 合入与用户体验接受单独处理。

渲染/数据层 ce7b924f 已部署，Web 格式和选择/复制通过；真实 WCL 取得装备天赋及跨页到战斗末尾的 Casts。完整 Chat 首轮暴露原 180 秒超时，追加 c4b6d4b0 配套前后端/Nginx/capability 预算修正。148 前端、253 后端、类型/lint/构建通过，详见 [本轮验证](../../artifacts/releases/2026-09-05-chickenbro-agent-rules/chat-render.md)。

## 清理无关声明（2026-09-05 用户反馈）

用户指出回答末尾的版本声明、不套用旧版、不虚报收益和版本参考没有价值。本次仅调整专用运行规则：版本条件与不确定性只在影响当前结论/建议时就近解释；去掉例行免责声明、工具过程汇报、自我表态与未支撑结论的引用。保留事实准确与必要的不确定性，不用前端字符串删除来掩盖重要信息。历史记录不改写，规则热加载作用于新请求。

用户进一步明确减少防御性回复：不要反驳用户没有提出的推论，不为未询问的收益/准确率/风险补预防性尾注；直接给判断、依据和建议。

已部署规则增量 91fb8ed1，正式/测试精确 SHA 一致并保留原规则备份，无服务重启。两组真实 Astra/high 样例分别验证直接建议不带无关尾注、关键数据不足仍如实说明；62 项控制面通过，历史回答未修改，用户体验验收和 main 合入待完成。
