# 未登录 Web 首页

> 当前结论（2026-09-08）：本计划已交付的 1.0 实现范围获用户整体验收，状态为 `已完成`。下面保留各阶段当时的状态与证据；旧“待验收/未合入/阻塞”描述不代表当前结论，也不授权重放迁移、发布或清理。未实现设想及微信公开发布不自动完成。详见 [1.0 说明](../releases/1.0.md)。

状态：`正在推进`

用户于 2026-09-07 明确要求移除顶部默认横幅和底部技术文案、修正二维码裁切，重新设计首页并在右上角引导扫码。

范围：沿用现有咕咕形象，以暖白色首页展示对话和云端模拟；桌面右侧上方常驻登录卡，窄屏顺序排列。首次确认未登录后自动创建二维码，取消后不自动重建，过期需手动刷新；二维码使用原生 Web 图片，登录确认、Cookie、owner 映射和测试账号开关沿用现有合同。仅在 Web runtime 隐藏 Taro 默认导航栏。

验证：新增首页登录 DOM 行为测试，覆盖自动生成、已登录恢复、取消、过期、错误重试、Mini 确认后交换和测试账号入口；运行前端回归、类型检查、lint、控制面与 H5 构建，并在实际浏览器检查桌面、窄屏、二维码完整性、键盘聚焦及失败/刷新状态。

交付：独立预览地址 `/previews/login-home-20260907/`，连接正式 API，二维码仍使用用户授权的体验版配置。预览构建以源码和产物 SHA 绑定；正式默认首页不在本次预览步骤替换，发布和用户扫码验收分别记录。

已完成预览：231 项前端、62 项控制面、类型检查、lint、H5 构建通过（保留已有体积警告）。1512/1024/390 宽度无横向溢出，二维码原图 430×430、显示 216×216，默认导航隐藏，技术页脚移除；真实体验版二维码创建、取消及扫码引导定位通过。25 个构建文件远端哈希匹配。用户手机扫码、界面体验验收与正式默认首页替换待完成。详见 [预览证据](../../artifacts/releases/2026-09-07-web-login-home/preview.json)。

2026-09-07 用户再次指出正式站黑色默认标题栏遮挡导航，明确要求移除。确认此前样式仅在预览生效；此次将相同的 Web 专属隐藏及 top:0 规则应用正式 Web，并给 CSS 加版本查询以刷新缓存。只有 CSS 与 index 样式链接变化，正式业务 JS、既有登录、Mini/API 和独立首页预览保持原状态。浏览器确认黑条 display:none / 高度0、页面 top:0，账号与原会话正常恢复。见 [正式标题栏修复](../../artifacts/releases/2026-09-07-web-login-home/formal-nav-fix.json)。

2026-09-07 用户反馈正式站退出后仍显示旧首页，要求纠正发布遗漏。已将包含新首页、完整二维码、文案清理及头像读取的完整正式 H5 构建发布到根路径 `/`；不再仅提供预览链接。新增“已登录 → 退出 → 新首页及新二维码”行为回归，238 前端、类型/lint/H5 构建通过。正式地址实测新版标题、右上二维码卡、430×430 原图以216×216完整显示，旧技术说明消失，默认标题栏保持隐藏。25 个远端文件哈希匹配；index/JS/CSS 均 no-cache，入口加版本查询。此次未变更 API/Mini；旧 Web、独立预览和回滚点保留。用户真机再次登录/退出验收待完成。见 [正式首页发布](../../artifacts/releases/2026-09-07-web-login-home/formal-home-release.json)。

2026-09-07 用户要求首页固定一屏、移除左下说明、缩小登录框。已将未登录首页固定为视口布局，登录/卸载时解除文档滚动锁；移除两组功能说明和页脚，桌面登录卡340→300、二维码216→184，并按窄屏和低窗口高度收紧布局。239项前端（含滚动锁清理回归）、类型/lint/H5通过。8组本地视口检查通过，正式真实二维码在1512×900、1024×600、390×667、568×320均完整，页面无横纵溢出、登录按钮可见。25个文件远端哈希匹配，正式根路径已更新，API/Mini及独立预览未变；用户体验验收待完成。见 [固定布局发布记录](../../artifacts/releases/2026-09-07-web-login-home/fixed-layout-release.json)。

2026-09-07 按用户补充要求移除右上角“魔兽世界 · 对话与模拟”，底部居中增加灰色小字“Powered by Lighthouse&Codex”。正式根路径已更新；10项首页回归及H5构建通过，正式桌面和568×320低窗口核验无滚动、署名不遮挡登录卡。见 [署名发布记录](../../artifacts/releases/2026-09-07-web-login-home/credit-release.json)。
