# Web 删除会话

状态：`正在推进`（本地实现，用户体验待完成）

## 已确认范围

用户在截图中指定每条历史会话右下角垃圾桶按钮，hover 显示“删除会话”，点击需二次确认。用户已确认后台软删除、双端历史移除、删除当前会话回空白、正在回复时提示等待。此次只增加 Web 删除入口；Mini 从服务端重新加载历史时遵循同一删除状态。

## 实现

- Web 使用独立垃圾桶按钮，避免嵌套按钮或误触打开会话。原生 dialog 提供取消、删除、进行中状态与错误说明，默认焦点为取消，取消后恢复焦点。
- DELETE `/api/v2/chat/conversations/{id}` 通过既有 Principal 与 CSRF 校验。服务端将当前账号的会话标记 archived，列表和详情不可再读，幂等重建不恢复会话；重复删除成功，不物理清除消息。
- 删除与发送都锁定会话行，避免同时删除和发起回复；streaming 时拒绝删除，沿用既有超时回收。无需新数据库 migration 或额外运行权限。
- ChatModel 仅在服务端确认成功后移除列表/当前内容，失败保留会话；防止延迟分页、历史恢复及导航失败带回已删除会话。
- 原目录的主题设计 WIP 未修改。当前隔离工作树 `/Users/boyuan/Documents/wow-chat-account-limit`，分支 `codex/chat-delete`，基线 c1acfca5e。

## 验证

独立审查反馈的 HTTP409 文案与导航失败保留旧会话两项已补回归修复。真实 PostgreSQL 覆盖软删除保留原始消息、双端不可读、owner隔离、streaming保护、发送/删除竞态以及 DELETE 的认证/CSRF/幂等合同。本地浏览器已验证原生居中弹窗、取消焦点恢复、确认删除与数量更新；仅使用演示数据。

最终测试与构建结果见下文。尚未提交、合入、推送、部署或上传新小程序；当前线上仍为已验收的账号并发限制版本。

最终验证：403 后端、77 迁移（无跳过）、275 前端、62 控制面通过，类型/lint/双端构建通过（既有包体积警告）；独立审查复核通过，`git diff --check` 通过。

## 验证部署授权（2026-09-08）

用户要求“部署我预览一下”，授权将本批 Web 与后端部署到现有正式入口供验证。保留现有代码和 Web 回滚点，不合入 main、不推送、不替代用户体验验收；本次不新增小程序删除入口，不上传新 Mini 版本，Mini 重新加载时由共享后端隐藏已删除会话。
