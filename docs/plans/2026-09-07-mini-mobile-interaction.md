# 小程序移动端整体交互优化

状态：正在推进；2026-09-07 用户授权先整体优化小程序，Web 体验保持现状。

## 范围与方案

仅调整 Mini 页面、测试登录容器和 Mini 底部导航；共享服务端、ChatModel、SimcModel 及 Web 功能保持不变。保护工作区已有未提交 Web 修改。

- 五个 Mini 页面使用微信原生导航，避免状态栏、刘海与胶囊遮挡。
- 页面容器限定可视高度，聊天正文独立滚动，输入区始终可访问；响应键盘高度并隐藏底部 Tab。
- 历史改为折叠纵向列表；支持首次直接输入建会话；等待提示、流式阅读、回到最新、未发送草稿保护。
- Mini 回复渲染文本型 Markdown：标题、粗体、列表、引用、代码和表格。表格按行转为字段条目，长链接换行；链接按需复制，不加载远程图片或 HTML。
- SimC 提交流程明确资料读取与参数范围，编辑来源后禁用旧快照提交；结果前置，来源与运行信息折叠；记录和异常状态便于手机阅读。

## 验证

本地：前端回归、类型、lint、控制面、weapp 构建和构建身份检查；逐文件校验 Web 原有内容未变。
运行态：微信开发者工具检查长对话、历史展开、输入区、SimC 记录/详情与返回。
用户验收：真实手机键盘/安全区、发送与续聊、长回复上翻与恢复跟随、SimC 创建与结果；自动测试不能替代真实设备验收。

## 发布边界

本轮交付本地 Mini 预览，尚未授权生产发布、推送合并或清理。保持当前隔离测试 API 配置。

## 本轮交付

已完成本地实现与测试预览刷新；219 前端、62 控制面、类型/lint/构建通过。开发者工具检查了原截图的长对话、Markdown 时间线、历史展开与折叠、输入区、导航高亮、SimC 历史、已有结果与运行详情展开。Web 34 个源文件与任务开始时的 SHA 一致。

本地 source diff 已按 dual_client 边界复核；未修改共享 ChatModel、SimcModel、API 或服务端。现有 detached HEAD 与 Web WIP 均保留，本轮未提交/推送/部署生产。构建身份与验证边界见 [验证记录](../../artifacts/releases/2026-09-07-mini-mobile-interaction/verification.json)。

待用户真机体验：键盘弹出/收起、长回复上翻暂停与恢复跟随、真实发送/续聊与模拟提交。本轮未新建真实 Chat 或 SimC 任务，也未重新执行微信扫码登录验收。

## FAQ 与更新日志入口补齐（2026-09-07）

用户反馈移动端缺少 FAQ 和更新日志入口；本轮在 Mini 公共容器顶部加入两个直接入口，覆盖 Chat、SimC、任务页及测试登录页。帮助在当前页面内独立滚动，关闭按钮常驻；保留底层页面挂载，避免丢失草稿、角色链接或中断回复。FAQ 沿用 Web 的产品主题并调整小程序表述，更新日志按平台标注测试版变化。Web 源码与五路由/两 Tab 契约保持不变。

入口已在开发者工具中检查，FAQ 可打开，更新日志可滚动且关闭按钮常驻；221 前端、62 控制面及类型/lint/构建通过。聊天草稿与 SimC 来源链接保留测试通过。见 [帮助入口验证](../../artifacts/releases/2026-09-07-mini-mobile-interaction/help-verification.json)。

## 正式整合（2026-09-07 用户授权）

用户确认“很好，现在把测试版合入正式”，授权本轮已验证 Web/Mini 和已合入 main 的测试版服务能力进入正式发布；覆盖上文旧的仅本地发布边界。真机键盘、微信平台上传/审核/发布仍按实际执行记录，不将此句话当作新的扫码测试证据。

发布准备已完成独立源码审查；修正持续流式增量导致滚动计时器被重置，以及新建对话期间草稿被清空两项问题，复审无遗留 P1/P2。223 前端、391 后端、类型/lint 通过；后端使用云端隔离源码目录和已有依赖，不影响线上服务。正式环境构建关闭测试登录、使用 /api/v2 与正式 CSRF cookie；更新日志按当前构建显示正式或测试文案。

下一步按以下顺序执行：
1. 解决 CodeRabbit 门禁：本机 cr 是失效链接，已向用户确认是否采用已完成的独立本地审查，或授权恢复安装。确认前不提交、推送或部署。
2. 提交精确源码，main 快进整合并核对远端；重新构建绑定最终提交的 H5/WeApp。
3. 云端使用独立版本目录，保存原 /opt/chickenbro 和 Web current 指针作为回滚点。生产库两项 schema migration 已存在，本次不重放迁移或旧切流脚本。API/Worker 显式设 chickenbro-simc-compiler-v3，保持现有生产身份、数据库、密钥和 SimC runtime。
4. 切换前再次确认没有运行中的 Chat/SimC；代码或 readiness 异常时恢复原目录/指针及配置。切换后执行业务 smoke、检查跨端同 owner 数据与 SimC 正数指标/来源，再记录 live evidence。
5. 刷新正式 Mini 构建；微信平台上传/审核/正式发布单独记录，不能把开发者工具预览称为线上发布。

分支处理：main 保留；本次 release 分支待整合验证后再清理；local-web-test-mode 有一笔独有假数据预览提交，不并入正式；远端 archive-local-stash-20260904 和四笔 stash 均保留。详见 [发布准备记录](../../artifacts/releases/2026-09-07-mini-mobile-interaction/release-readiness.json)。

用户随后明确“不需要再安装 CodeRabbit”，本次采用已通过的独立本地审查继续整合发布。生产 SimC 支持集同步为已在测试环境验证的元素/增强，编译 revision 为 v3。

## 发布结果

源提交 `1862e167fafa0469888c3b2b489a8799ae19a799` 已合入 main 并推送；正式 Web/API/Worker 切换成功，两个公开域名 readiness 与发布文件 SHA 匹配。双向 Chat/SimC、幂等重放、owner 隔离和独立退出自动业务 smoke 通过，SimC v3 有正数指标及来源核对。旧 acceptance core 固定 v1，本轮 wrapper 显式绑定预期 v3（非从响应推导），其余身份/语义断言保持原样；第一次旧门禁失败已如实记录，不作为业务成功。

小程序版本 `2026.09.07` 上传成功，开发者工具正式预览已刷新；微信平台审核/发布未执行，原因是浏览器站点安全策略拦截 mp.weixin.qq.com。需用户最后在平台操作，本轮没有新的真实微信扫码或真机键盘验收。

发布 manifest、回滚路径、业务结果及分支记录见 [release.json](../../artifacts/releases/2026-09-07-mini-mobile-interaction/release.json)。源与构建身份绑定 `1862e167f`；后续仅证据文档提交不会改写已部署代码的来源身份。
