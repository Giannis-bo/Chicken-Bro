# 测试账号快捷登录

## 回复宽度与插画层次（2026-09-05 用户要求）

范围为现有测试 Web 的局部视觉调整：鸡哥回复宽度由 `min(78%, 720px)` 扩为 `min(88%, 1280px)`，用户气泡保留原宽度；窄屏沿用 92% 响应式宽度。插画桌面透明度由 0.44 调为 0.62，减薄左侧白色遮罩；窄屏采用较低的 0.46。助手消息白底由 0.8 调为 0.72，正文保持完全不透明并加深至 #3d3d39。复用原插画资源，不改变聊天协议、滚动或后端行为。验证实际桌面/窄屏的宽度、插画与正文可读性、无水平溢出，构建后只更新测试 Web，保留旧 release 和全部后端文件。

已发布 `60371323cae73685df66ce50ff84d4d5174bcc8b`。154 前端、62 控制面、diff 检查和测试 H5 构建通过；现有两项体积警告保留。Edge 2552px 视口实测助手框约 1280px、用户框约 720px，人物与城门更明显、文字清楚；390×844 窄屏无横向溢出，输入框可见。视口已恢复，未发送新消息或改变草稿。22 个公网文件 SHA 一致，97 个非 Web 文件逐一保留，未重启服务；正式 Web 不变。测试回滚只需原子切回保留的 `b6f7450707566074d0ec9ca0103269ae8936ac50` release。见 [页面验证](../../artifacts/releases/2026-09-05-test-account-login/web-width-art-verification.json) 与 [部署记录](../../artifacts/releases/2026-09-05-test-account-login/web-width-art-deployment.json)。用户体验验收和 main 合入待完成。

## 鸡哥称呼与长回复跟随（2026-09-05 用户确认）

Web 展示昵称统一“鸡哥”。长回复正常路径：初次打开会话、发送、正文增量、最终历史替换和内容布局增长时，默认保持消息区底部。用户向上翻阅或选择正文时暂停，向下回到底部或点击“回到最新”恢复；计时不抢滚动，观察器和事件监听随卸载清理。桌面与窄屏保持输入区可见、消息区独立滚动。复用当前 Chat 协议，核对云端真实逐段输出，不用前端打字动画冒充模型流式。验证覆盖多段增长、完成后布局变化、手动翻阅、选择复制、恢复和切换会话；实际页面验证最终构建。

已发布测试版本 `b6f7450707566074d0ec9ca0103269ae8936ac50`。154 前端、256 后端、62 控制面及类型/lint/构建通过。真实 Edge 千字回复持续增长，窄屏上翻后正文从 678 增至 900 字时 scrollTop 保持 75.33，点击“回到最新”恢复到底部；桌面恢复与最终历史替换仍在底部。390×540 页面不溢出、输入框可见；120 字原生复制粘贴逐字一致，草稿清空。真实 WCL 查询 verified，刷新后两条回复完整保留。22 个公网文件 SHA 匹配，旧 release `63cad718f69d874a60ae198f34f6f8ecb042d2e6` 保留；回滚原子切回该链接并重启测试 API。详见 [浏览器证据](../../artifacts/releases/2026-09-05-test-account-login/web-persona-stream-verification.json) 和 [部署身份](../../artifacts/releases/2026-09-05-test-account-login/web-persona-stream-deployment.json)。用户体验验收、main 合入仍待完成。

状态：正在推进。2026-09-05 用户确认方案并授权实施。

## 需求合同

测试者无需微信扫码或确认，即可用 A/B 测试账号在 Mini/Web 操作真实 Chat/SimC。服务端仍拥有身份和数据归属，两个独立会话对应同一测试用户。凭证只输入一次，过期或退出后重新输入；失败如实显示，不退回假数据。

反方检查：全局跳过鉴权会失去 owner 合同；前端 fixture 无法验证真实业务。因此复用正式 Session 和业务链路，仅替换测试环境的登录入口。非目标：删除微信登录、生产匿名访问、迁移真实用户、改变 SimC 输入/执行规则。

## 实施与影响

1. Identity/config：默认关闭；生产禁止启用；只允许专用测试数据库。A/B 使用独立高熵凭证的 SHA256 配置，服务端固定用户映射，无客户端 user_id；会话有期限，关闭入口后测试会话立即拒绝。不新增依赖或 schema。
2. API/client：独立 Mini/Web 测试登录请求，Web 保留 Origin/CSRF/HttpOnly Cookie，Mini 保留 Bearer。错误不回显凭证。
3. Mini/Web：显式测试构建开关、密码输入、A/B 选择、已连接提示、退出和重新进入；业务模型继续调用正式 API。
4. 运维：记录测试环境配置和关闭步骤，使用独立数据库/API/Worker；云端 SimC 保持原有 runtime。不得在生产服务上启用。

依赖方向：测试登录 UI -> typed API client -> Identity application -> Identity repository -> 正常 Principal -> Chat/SimC。Chat、SimC、Worker 的 owner 和业务语义不变。身份创建为有界事务，不修改热点业务 application。

## 验收与回滚

先测试默认关闭/生产拒绝/数据库保护、错误凭证、固定用户、正常双端会话、过期退出与关闭失效；再验证 Mini/Web 客户端 transport 和编译构建。隔离 Candidate 验证真实持久化、Chat/SimC、A/B 隔离及独立退出。手工必验：mini_login、web_login、shared_chat、shared_simc、owner_isolation、independent_logout、disabled_mode。自动 API/内存测试不能冒充真实设备或云端 SimC 验收。

回滚：关闭服务端入口并重启，所有测试会话拒绝；关闭客户端测试构建并重建。必要时回退代码；测试数据保留于独立库，不删除生产资源。测试运行态未部署前最高证据只能为本地验证。

使用与运维入口见 [测试账号说明](../test-account-login.md)。

## 当前交付证据（2026-09-05）

代码及测试环境已就绪。运行版本 `d28c68bf10ccb79aae968a5697dc924429649393`，独立测试库/API/Worker 已部署。真实 Chat 回复、两端共享历史、B 隔离、单端退出、重启持久化、关闭入口与旧会话撤销通过；最终 CSS 修正已在浏览器截图复核。Mini 测试包已原子更新；开发者工具自身要求用户重新登录，未冒充真机验收。

定向验证：后端 240、前端 124、控制面 62 项通过；类型检查、lint、双端测试构建通过。迁移 64 项中 1 项既有可选 PostgreSQL 集成跳过；完整 ops 在 Windows/临时 Linux 源码环境仍有 Bash、Git/Taro 和历史清单限制，未宣称全套通过。SimC 仅验证正常鉴权列表，未运行新的模拟。完整记录见 `artifacts/releases/2026-09-05-test-account-login/evidence.json`。

下一步：用户运行配置脚本设置自行持有的 A/B 凭证，在真实 Mini/Web 完成业务验收。当前保留任务分支，main 未修改；不作合并、正式切流或清理收尾。

## 登录失败修正（2026-09-05）

用户已成功配置凭证，但网页仍失败。根因：API client 的 public 请求白名单仍硬编码旧前缀，且缺少测试登录路由，因此登录和 `/test/api/v2/me` 在发送前被拒绝；先前后端 HTTP smoke 和使用 RecordingTransport 的客户端测试未覆盖此交界。

修正版本 `1cb69626758a161170fbcab7ed9aa146ac171d34` 已部署，白名单使用统一配置前缀，仅测试构建允许两个精确登录端点，业务接口仍需登录。新增 9 项跨 API client/真实 transport 回归（修正前 4 项失败），当前前端 133 项、类型检查、lint 和双端测试构建通过。浏览器用明确无效的测试字符串复现旧版泛化错误，更新后收到服务端明确凭证错误，证明请求已通；未索取或重设用户凭证。真实正确凭证登录仍由用户验收。

## Web 输入框优化（2026-09-05 用户授权）

用户确认 Web 登录成功后，要求去掉输入框右侧“对话”、Enter 发送、修复回复过程中箭头和加载动画重叠。范围限定现有 Web 输入框：Shift+Enter 换行，中文输入法选词不发送，回复中保留下一条草稿并禁止重复发送，单一居中加载图标完成后恢复箭头。Chat API、模型及权限不变。通过真实 React DOM 事件回归与测试环境浏览器检查验收，Mini 功能范围不变。

输入框修正已部署为 `83b9684b605fa2496db14b81eceb18dc40424c9b`。前端 139 项（新增 6 项 DOM 交互测试）、类型检查、lint、控制面 62 项及双端测试构建通过。登录后的 Edge 中新建验证会话，Shift+Enter 换行、Enter 发送并收到真实回复“回车发送正常”；截图确认回复中为单一居中加载环，完成后恢复发送按钮。Web 登录单项已按用户明确反馈记录接受，其余跨端项和本次 UI 用户验收保持待验证。

## Web 顶栏精简（2026-09-05 用户授权）

移除聊天的“咕咕助手 / 在线 / 先聊清楚”整栏，健康灯置于顶部品牌右侧；“新对话”移到历史列表标题旁，保留 loading/sending 禁用逻辑。恢复被原栏占用的聊天高度，SimC 自身标题不变。

原在线灯为静态装饰，本次改为调用当前环境 `/health/readiness`：ready 绿、partial 黄、blocked/网络或响应异常红、检查中灰；提供悬停说明与读屏标签。每 60 秒检查一次，隐藏页面暂停检查，返回可见时刷新，卸载清理计时器。请求复用公开 transport 与配置前缀，不涉及认证数据。

前端 144 项、类型检查、lint、测试 H5 构建通过；无新增依赖。只发布测试 Web，保留既有服务端、凭证及正式客户端。实际页面验收与发布 identity 另记测试任务 release evidence。

已发布 `f144923c0a1ffb4c21aad4567f3e9e443b3b036d`，Edge 真实登录态确认旧栏消失、LOGO 旁绿灯显示“服务正常”、“新对话”位于历史列表旁，聊天和输入框布局正常。详见 [发布验证](../../artifacts/releases/2026-09-05-test-account-login/web-header-health.md)。

## Web 输入区强化与等待反馈（2026-09-05 用户确认）

范围限定当前 Web Chat：增加输入框高度、留白、边框及阴影，聚焦时使用主题色；输入字体显式继承 Web 正文，同为 14px、1.75 行高。发送后在回复位置立即显示动态圆点和“咕咕正在处理…”，10 秒后显示实际等待秒数；首段正文到达后移除占位，失败或断线停止动画并保留现有错误与恢复入口。等待指示只代表请求未完成，不推断模型内部思考或工具执行阶段。遵循减少动画偏好，读屏不逐秒播报。

复用现有 ChatModel，无 API、服务端或身份变更。发送时将等待占位一次滚入可视区域，计时更新不抢滚动位置。验证覆盖等待/流式回复/完成/失败/断线、计时清理与下一请求重置，以及已有 Enter、Shift+Enter、中文输入法和草稿保留回归；完成类型检查、lint、前端与控制面测试、测试 H5 构建，并检查桌面与窄屏。只发布隔离测试 Web，保留旧 release 供回滚；用户体验接受、main 整合仍单独处理。

测试 Web 已发布 `54e9562b647377201406b7f7c9beb6aa8f816596`。前端 151 项、类型检查、lint、测试 H5 构建通过；真实浏览器确认字体统一，桌面与窄屏等待后正常回复。22 个公网文件与构建一致，96 个非 Web 文件保留且未重启服务。用户体验验收待完成，详见 [验证记录](../../artifacts/releases/2026-09-05-test-account-login/web-chat-feedback.md)。

## 移除消息复制按钮（2026-09-05 用户明确要求）

用户消息和助手回复均去掉“复制”按钮，同时移除专用 clipboard 写入、复制提示状态及仅用于隐藏按钮的 streaming 属性。保留正文及 Markdown 子节点的 `user-select: text`，使用浏览器原生选择、Ctrl+C/Ctrl+V。删除已退役按钮行为的测试，保留 Markdown 渲染及聊天交互回归；构建测试 H5 并在登录态浏览器选择正文、复制粘贴到空白输入框核对后清空，不发送验证草稿。

已发布测试 Web `63cad718f69d874a60ae198f34f6f8ecb042d2e6`。150 项前端、62 项控制面、类型检查、lint、diff 检查和测试 H5 构建通过，构建保留既有体积警告。登录态 Edge 确认消息卡片无复制按钮；原生三击选择 38 字回复，Ctrl+C 后在空白输入框 Ctrl+V，粘贴与所选内容逐字一致，随后清空草稿且未发送消息。22 个 Web 文件远端/公网 SHA 与构建一致，96 个非 Web 文件保留，服务未重启，readiness ready；正式客户端未发布。回滚原子将测试 current 切回保留的 `54e9562b647377201406b7f7c9beb6aa8f816596` release。详见 [部署记录](../../artifacts/releases/2026-09-05-test-account-login/web-message-selection-deployment.json)。用户体验验收与 main 合入仍待确认。
