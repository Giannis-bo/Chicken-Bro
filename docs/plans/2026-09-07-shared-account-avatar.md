# Mini / Web 共用账号头像

> 最新界面：当前 Mini 不提供头像选择入口，Web 继续读取已保存头像；下面的选择头像步骤仅为历史记录。

> 当前结论（2026-09-08）：本计划已交付的 1.0 实现范围获用户整体验收，状态为 `已完成`。下面保留各阶段当时的状态与证据；旧“待验收/未合入/阻塞”描述不代表当前结论，也不授权重放迁移、发布或清理。未实现设想及微信公开发布不自动完成。详见 [1.0 说明](../releases/1.0.md)。

状态：`正在推进`

2026-09-07 用户已确认：通过小程序主动选择头像并保存，Mini/Web 共用；可跳过，不要求头像才能登录。不自动读取或跟踪微信头像变化。

正常路径：小程序顶部点击设置头像 → 微信 chooseAvatar → 有界压缩、上传 → 服务端按 Principal 保存 → 两端读取。Web 返回前台或打开账号菜单时刷新。取消选择不改变原头像；失败可重试；未设置及图片加载失败显示默认图标。不新增资料页，不修改 Chat/SimC owner 或认证运输。

实现：Identity users 的可空头像字段；独立 GET /me/avatar（Mini/Web）与 PUT /me/avatar（仅 Mini）。旧 /me 合同保持兼容。保存受限 PNG/JPEG 数据，剥离图片元数据，不保存本地临时路径，不开放公共图片或第三方下载。新增迁移保持旧 API 可运行。

验证：图片格式、长度、像素、元数据、无效输入不覆盖、owner 隔离、两种读取与 Mini 专属写入；客户端取消/失败/保存/刷新与默认图标；现有登录回归、前后端、控制面、schema、类型/lint及双端构建。先验证隔离 Candidate；生产发布与用户手机选择头像验收单独记录。

已实现：小程序顶部以及扫码确认成功页提供可跳过的 chooseAvatar；Web 恢复登录、返回前台或打开账号菜单时读取。256×256 客户端压缩，服务端限制 256 KiB / 1024 像素，剥离元数据，拒绝 SVG、URL、异常编码和额外 owner 字段；不变更旧 /me。

验证与交付：237 前端、399 后端、62 控制面、类型/lint/双端构建通过；最终 JPEG 长度边界专项 8 项通过。实际隔离 PostgreSQL 中以 wow_app 角色验证保存、双端相同、第二用户隔离和旧接口兼容，测试用户写入全部回滚。随后向正式库应用可空字段迁移，API 通过独立 WorkingDirectory drop-in 使用仅叠加头像能力的运行目录；两域名7项 readiness ready，认证头像读取正常。旧目录、已有体验版和默认 Web 首页保留。

[Web 预览](https://www.chickenbro.cloud/previews/login-home-20260907/)已更新，25 个文件哈希一致；真实既有 Web 登录与对话恢复、菜单提示和默认图标已检查。小程序生成 464600 bytes 开发预览，未上传替换现有体验版或提交微信发布。手机选择头像及用户本人双端外观验收待完成；源码尚未提交/合入。见 [证据和回滚](../../artifacts/releases/2026-09-07-shared-account-avatar/verification.json)。

2026-09-07 用户真机反馈未看到头像入口。确认此前只生成开发预览，实际扫码仍指向旧体验版；原代码也仅在确认成功后显示。现将可选头像区域提前至 Mini 登录完成、Web 确认之前，并在确认期间/之后保持挂载。新增回归证明无需选择头像也能确认。240前端、类型/lint/小程序构建通过，源码与构建身份核验一致。已通过开发者工具上传 `2026.09.07-avatar2`（465491 bytes）；浏览器安全策略禁止访问微信版本管理页面，需用户将该版本设为体验版。尚未声称扫码体验版已切换或真实头像保存通过。见 [上传记录](../../artifacts/releases/2026-09-07-shared-account-avatar/mini-upload-avatar2.json)。

2026-09-07 用户确认已在Mini与Web看到保存的头像，截图提供双端头像显示证据；随后要求确认页与Web鸡哥Logo风格一致并清理文案。已改为暖白导航/卡片与现有Logo，头像入口仅保留可点击圆形图片和编辑图标（无障碍标签保留），删除跨设备说明、隐私尾注、头像设置说明和重复成功块。微信chooseAvatar依赖用户点击，未实现自动弹授权；已保存头像自动读取。241前端、类型/lint/小程序构建及真实开发者工具渲染通过，自动读回用户已存头像。已上传 `2026.09.07-avatar3`（1006698 bytes）；待用户设为体验版及验收新样式，未自动提交审核/发布。见 [新版记录](../../artifacts/releases/2026-09-07-shared-account-avatar/mini-upload-avatar3.json)。

2026-09-07 用户认可新确认页，要求确认登录后自动登录进入小程序。原页面只持有临时会话ref，且成功后停留；现复用MiniSessionStore.login校验/保存本次Mini会话，显式Web确认成功立即switchTab进入对话首页。确认失败不跳转；跳转失败保留“进入小程序”重试，只重试导航，不二次确认票据。Mini/Web仍独立会话。243前端（含可复用持久会话和两种失败回归）、类型/lint/小程序构建通过，源码构建身份一致。已上传 `2026.09.07-login4`（1007387 bytes），待用户设为体验版并真机验证自动进入。见 [上传记录](../../artifacts/releases/2026-09-07-shared-account-avatar/mini-upload-login4.json)。

## 最新范围调整（2026-09-07）

用户要求去掉移动端头像展示和更新功能。本轮移除公共 Mini 容器与 Web 登录确认页的头像组件调用；不回退既有登录确认、会话持久化、Web 头像读取或服务端保存逻辑。上文主动选头像入口属于历史方案，不再作为当前 Mini 验收项。保留既有未提交组件及服务端工作，本轮不做数据或无关文件清理。
