# 2026-07 Phase 4 PG Read Model History

本文件归档从 `docs/roadmap.md` 顶部迁出的 Phase 4 PG/cache/admin read-model selector 逐 PR 运行日志。当前结论以 `docs/project-state.json`、`docs/roadmap.md` 顶部和 `docs/backend-owner-map.json` 为准；这里保留完整历史证据和上下文。

摘要：Phase 4 第四十刀后，PG read-model selector 纵切收口；公开 observed-only gear 入口、baseline 默认空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。

> 2026-07-10 CST Phase 4 PG cache/admin 只读 selector 边界审计（已完成 / 下一阶段待定）: 第四十刀合入后复核 `server/postgres_cache_store.py` 剩余读路径，当前 cache payload、active season、WebSim gear/talent/loot/assets/bootstrap 与 admin talent/template/variant/queue summary 的有实质组装复杂度 read model 均已有 backend-owned selector。`admin_gate_gear_records()` 等剩余 envelope 仅做两字段透传，继续抽取会增加无价值间接层；`get_sync_state()` 则属于 owner map 已标记的 read/write 共属 `sync_state_repository`，自动拆分会越过本轮“不碰 sync/write”边界。因此本轮 PG read-only selector 纵切在第四十刀收口，不再机械拆小；下一阶段应单独决定是进入 sync-state repository、转向其他 backend hotspot，还是先做 Phase 4 结构收益复盘。

> 2026-07-10 CST Phase 4 admin gate queue summary read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第四十刀继续拆 `server/postgres_cache_store.py` 的只读 PG/admin read-model selector 边界，新增 `server/pg_cache_read_model_selectors.py::build_admin_gate_queue_summary_read_model()`，把 `admin_gate_queue_summary()` 已组装 rows 到 queue `count`、`domainCounts` 与按次数降序/文本升序截前 8 的 `topBlockers` 聚合从 store 收敛到纯 selector。store 保留 talents/gear/gear-template admin SQL、`to_regclass` table check、official item metadata SQL、offhand coverage repair adapter；`server/news_backend.py` 继续拥有 admin authorization 与跨 content/cache store summary 合并；sync/write/backfill/cleanup/timer 均不触达。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，recommended/season/default/SimC/internal prototype 不回流公开装备入口。characterization RED 先因 helper 缺失失败，owner-map contract 也先红后绿；最终 helper tests 固定 verified-with-blocker、unknown domain、重复 blocker、top-8 tie ordering，store delegation test 固定 SQL row handoff，10,000 组旧实现等价样例零差异。完整本地验证为 `1055 Python tests OK (skipped=1)`、`344 Node pass`，目标 selector/store 为 `110 tests OK`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和 local CR 均通过。PR #49 exact head `e925aab` 已按 Candidate Deployment Gate 热部署：`/health`、`/api/data/health`、season、WebSim bootstrap/assets/talents/loot/gear initial+slot 与授权 `/api/admin/gates/summary` 均返回 200；admin queue 为 `overallStatus=partial / count=2906 / domainCounts={news:7,talents:19,gear:2836,gear_templates:44}`，top blockers 保持排序；40 专精巡检为 `failures=0 / active={True:40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={raiderio_observed_profile:40} / canApplyGear={True:40}`；backend/nginx active，PG-only runtime、候选窗口日志和 5 个 runtime hash parity 通过。部署命令未进入 async sync 启动块；12:04:52 CST 既有 data-health follow-up timer 随后独立完成 observed backfill，并于 12:05:00 启动 websim/stat-weight sync，作为既有 timer backflow 如实记录。PR #49 已 rebase merge 到 `main@a2aa378` 并同步到本地。Evidence: `server/pg_cache_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_cache_read_model_selectors_test.py`, `tests/postgres_cache_store_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-admin-gate-queue-summary-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-admin-gate-queue-summary-read-model-selector/manifest.json`.

> 2026-07-10 CST Phase 4 stat-weight latest-run read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十九刀继续拆 `server/postgres_cache_store.py` 的只读 PG cache read-model selector 边界，新增 `server/pg_cache_read_model_selectors.py::build_stat_weight_latest_run_read_model()`，把 `latest_stat_weight_run_payload()` 已读 rows 到 latest-run summary 的 accepted/blocked 计数、status/sourceStatus、lexicographic `refreshedAt`、empty-cache blocked envelope 收敛到纯 selector。store 保留 PostgreSQL connection、`cache.stat_weight_cache` SQL、`ORDER BY computed_at DESC`、`LIMIT 400`、`fetchall()`、全部 stat-weight save/sync/backfill/cleanup；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，recommended/season/default/SimC/internal prototype 不回流公开装备入口。characterization RED 先因 helper 缺失失败，review 后又用 `"9"` / `"10"` mutation RED 固定普通字符串最大值而非数字/日期排序；完整本地验证为 `1052 Python tests OK (skipped=1)`、`344 Node pass`，`py_compile`、`node --check`、JSON validation、`git diff --check`、任务级 review、整分支 CR 和 `4971` 组 base-to-head 等价样例均通过。PR #48 exact head `853c0dd` 已按 Candidate Deployment Gate 热部署：`/health`、`/api/data/health`、season、WebSim bootstrap/assets/talents/loot/gear initial+slot、stat-weight latest/detail 均返回 200；latest-run 为 `partial / accepted=34 / blocked=86 / scenarios=120 / errors=[]`，existing data health 保持真实 `partial`；40 专精巡检为 `failures=0 / active={True:40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={raiderio_observed_profile:40} / canApplyGear={True:40}`；backend/nginx active，PG-only runtime、候选窗口日志和 5 个 runtime hash parity 通过。部署命令未进入 async sync 启动块；既有 enabled `wow-gear-observed-backfill.timer` 在 11:44:49 CST 按计划触发并于 11:44:55 成功退出，作为独立既有 scheduled write 如实记录。PR #48 已 rebase merge 到 `main@5be9d81` 并同步到本地。Evidence: `server/pg_cache_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_cache_read_model_selectors_test.py`, `tests/postgres_cache_store_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-stat-weight-latest-run-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-stat-weight-latest-run-read-model-selector/manifest.json`.

> 2026-07-10 CST Phase 4 stat-weight cache read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十八刀继续拆 `server/postgres_cache_store.py` 的只读 PG cache read-model selector 边界，新增 `server/pg_cache_read_model_selectors.py::build_stat_weight_cache_read_model()`，把 `get_stat_weight_payload()` 已读 row 到 structured stat-weight payload 的 JSON 解析、missing-row `None`、identity/status/timestamp defaults 从 store 收敛到只读 selector。store 保留 cache-key validation、slugification、`cache.stat_weight_cache` SQL、`save_stat_weight_payload()`、`latest_stat_weight_run_payload()`、sync/write/backfill；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，recommended/season/default/SimC/internal prototype 不回流公开装备入口。characterization red 先因 helper 缺失失败，最终 review 又补齐 payload-owned 7 个字段 precedence、malformed/non-dict JSON 和 blocked fallback mutation sensitivity；完整本地验证为 `1049 Python tests OK (skipped=1)`、`344 Node pass`，`py_compile`、`node --check`、JSON validation、`git diff --check`、任务级 review 和整分支 review 均通过。PR #47 exact head `080e6e2` 已按 Candidate Deployment Gate 热部署：`/health`、`/api/data/health`、season、WebSim bootstrap/assets/talents/loot/gear initial+slot、stat-weight latest/detail 均返回 200；stat-weight detail 为 `partial / scenarios=3 / stats=5 / sampleCount=7 / simcSuccess=1`，existing data health 仍诚实为 `partial`；40 专精巡检为 `failures=0 / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={raiderio_observed_profile:40} / canApplyGear={True:40}`；backend/nginx active，PG-only runtime、日志和 5 个 runtime hash parity 通过。部署未启动 async sync；`wow-stat-weights-sync` 的 10:04:50 CST 既有任务早于 11:19:47 候选部署并继续运行。PR #47 已 rebase merge 到 `main@8bac8c2` 并同步到本地。Evidence: `server/pg_cache_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_cache_read_model_selectors_test.py`, `tests/postgres_cache_store_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-stat-weight-cache-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-stat-weight-cache-read-model-selector/manifest.json`.

> 2026-07-10 CST Phase 4 Raider.IO cache read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十七刀继续拆 `server/postgres_cache_store.py` 的只读 PG cache read-model selector 边界，新增独立 `server/pg_cache_read_model_selectors.py::build_raiderio_cache_read_model()`，把 `get_raiderio_payload()` 已读 row 到 structured cache payload 的 missing-cache blocked envelope、JSON 解析、timestamp 与 sourceStatus defaults 从 store 收敛到只读 selector。store 保留 `cache.raiderio_cache` SQL、cache key、`save_raiderio_payload()` INSERT/ON CONFLICT、sync 和 observed gear backfill；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，recommended/season/default/SimC/internal prototype 不回流公开装备入口。characterization red 先因新模块缺失失败；最小实现后 cache/season/store 目标 tests 为 `102 tests OK`，owner-map contract 完成 red/green；完整本地验证为 `1046 Python tests OK (skipped=1)`、`344 Node pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过；PR #46 branch 已按 Candidate Deployment Gate 热部署，Raider.IO health 保持 `verified / runCount=400 / profileCount=38 / blockers=[]`，stat weights 仍诚实保持 `partial / accepted=35 / blocked=85`，标准 season/WebSim/gear API 均返回 200；40 专精巡检为 `failures=0 / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={raiderio_observed_profile:40}`；backend/nginx active，PG-only runtime、日志和 5 个 runtime hash parity 通过，部署未启动 async sync。PR #46 已 rebase merge 到 `main@a0de2a1` 并同步到本地。Evidence: `server/pg_cache_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_cache_read_model_selectors_test.py`, `tests/postgres_cache_store_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-raiderio-cache-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-raiderio-cache-read-model-selector/manifest.json`.

> 2026-07-10 CST Phase 4 active-season read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十六刀继续拆 `server/postgres_cache_store.py` 的只读 PG active-season read-model selector 边界，新增 `server/pg_season_read_model_selectors.py::build_active_season_read_model()`，把已读 active-season row 与 dungeon rows 到 structured season payload 的 cached-payload fallback、字段组装、expiry stale、source refs、dungeon payload 和 raid-pool normalization 从 store 收敛到独立 selector。store 保留 `cache.websim_season_state` / `cache.websim_season_dungeons` SQL、`active = TRUE` + `data_status = 'verified'` gate、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开装备入口。characterization red 先因新模块缺失失败；最小实现后 helper/store 目标 tests 为 `101 tests OK`，owner-map contract 完成 red/green；完整本地验证为 `1045 Python tests OK (skipped=1)`、`344 Node pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过；PR #45 branch 已按 Candidate Deployment Gate 热部署，公网 `/health`、`/api/data/health`、`/api/game/season`、WebSim bootstrap/assets/talents/loot/gear initial+slot 均返回 200，active season 为 `verified / season-17-f131dd36ddf1 / dungeons=8 / raids=4 / sourceRefs=2` 且 raid pool 无 missing/stale/blocker；40 专精巡检为 `failures=0 / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={raiderio_observed_profile:40}`；backend/nginx active，PG-only runtime、日志和 5 个 runtime hash parity 通过，部署未启动 async sync。PR #45 已 rebase merge 到 `main@3c4c42b` 并同步到本地。Evidence: `server/pg_season_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_season_read_model_selectors_test.py`, `tests/postgres_cache_store_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-active-season-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-active-season-read-model-selector/manifest.json`.

> 2026-07-10 CST Phase 4 WebSim talents envelope read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十五刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_talents_read_model()`，把 `get_websim_talents()` 中已计算 talent authority/readiness/nodes/presets/community templates/tree sections/status 与 season 到最终 response envelope、blocker 去重和 `communityTemplateSync` summary 的只读组装从 store 收敛到 selector helper。store 保留 active season/sync state 读取、talent/community template SQL、node/hero 过滤、spell detail readiness、talent status/authority/readiness 计算、cache write 和 sync/write/backfill 路径；当前 WebSim talent read-model 纵切至此完成，后续只在独立 PG domain 中继续抽取，不向本 helper 无界堆叠。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开装备入口。红测 `test_build_websim_talents_read_model_assembles_sync_blockers_and_season` 先因 helper 缺失失败；最小实现后新 helper 单测与目标 selector/store tests 为 `124 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1044 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过；PR #44 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；talents envelope 返回 `class/spec/hero=mage/frost/spellslinger / talentSchemaRevision=websim-talent-rules-v1 / talentStatus=simc / authorityRuntimeSource=simc / nodes=110 / presets=2 / communityTemplates=2 / treeSections=3 / dataStatus=verified`，blockers 保留 2 条 spell detail 诊断，`communityTemplateSync.activeSpecSlots={total:2,verified:2,pendingCollection:0,blocked:0}`，全局 templates 为 `80/80 verified`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，6 个 sync/backfill/follow-up services inactive/dead success，`wow-websim-sync` 与 `wow-stat-weights-sync` 仍为 10:04 既有 data-health follow-up 在部署前触发的 `activating/start`，部署本身未启动 async sync，部署后 smoke 通过；9 个 timers listed，PG-only runtime、日志和 5 个 runtime hash parity 均通过。PR #44 已 rebase merge 到 `main@e8f7b27` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-websim-talents-envelope-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-websim-talents-envelope-read-model-selector/manifest.json`.

> 2026-07-10 CST Phase 4 WebSim community talent template read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十四刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_community_talent_templates_read_model()`，把 `_community_talent_templates()` 中已筛选 PG rows 到 structured community talent template list 的 JSON/int 解析、字段映射、source ref normalization、可应用状态和既有 spec/hero slot selection 组装从 store 收敛到 selector helper。store 保留 community talent template SQL、`status='verified'` 与 expiry 过滤、排序/limit、active season/sync state 读取、talent SQL、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开装备入口。红测 `test_build_websim_community_talent_templates_read_model_maps_rows_and_slots` 先因 helper 缺失失败；最小实现后新 helper 单测与目标 selector/store tests 为 `123 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1043 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过；PR #43 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；talent read model 返回 `talentStatus=simc / nodes=110 / communityTemplates=2`，`spellslinger` 与 `frostfire` 两个 slot 均为 `status=verified / coverageStatus=covered / slotStatus=verified / sourceKey=raiderio / sourceRefs=1 / canApplyVisual=true / canUseInSimc=true`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，6 个 sync/backfill/follow-up services inactive/dead 且 `Result=success / ExecMainStatus=0`，`wow-websim-sync` 与 `wow-stat-weights-sync` 由 10:04 既有 data-health follow-up 在候选部署前触发并处于 `activating/start`，部署命令本身未启动 async sync，部署后 talent/public gear smoke 仍通过；9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，日志无 Traceback/ERROR/SQLite fallback，5 个关键 runtime 文件远端 hash 与本地 branch 一致。PR #43 已 rebase merge 到 `main@493804e` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-websim-community-talent-template-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-websim-community-talent-template-read-model-selector/manifest.json`.

> 2026-07-10 CST Phase 4 WebSim talent authority read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十三刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_talent_authority_read_model()`，把 `get_websim_talents()` 中 talent status、active season、talent nodes 与 sync state 到 `talentAuthority` structured payload 的只读组装从 store 收敛到 selector helper；store 保留 active season/同步状态读取、talent SQL、节点过滤与 decoration、talent readiness、community template、cache write 和 sync/write/backfill 路径。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_talent_authority_read_model_preserves_runtime_and_official_state` 先因 helper 缺失失败；最小实现后新 helper 单测与目标 selector/store tests 为 `122 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1042 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过；PR #42 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；talent read model 返回 `talentStatus=simc / nodes=110 / talentAuthority.schemaRevision=websim-talent-rules-v1 / runtimeSource=simc / diffStatus=pending_official_audit / runtime.nodeCount=110 / official.status=pending_audit`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive/dead 且 `Result=success / ExecMainStatus=0`，9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，5 个关键 runtime 文件远端 hash 与本地 branch 一致。PR #42 已 rebase merge 到 `main@a6028ed` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-10-websim-talent-authority-read-model-selector/evidence.json`, `artifacts/releases/2026-07-10-websim-talent-authority-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim loot envelope read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十二刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_loot_read_model()`，把 `get_websim_loot()` 中已构建 `items`、`instances` 与 `season_metadata_fields(season)` 到 `/api/websim/loot` response envelope 的只读组装从 store 收敛到 PG read-model selector helper，并保留旧 `items[:limit]` 切片语义。`server/postgres_cache_store.py` 保留 active season gate、loot SQL、loot item selector 调用、instances 读取、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_loot_read_model_wraps_items_instances_and_season` 先因 helper 缺失失败；最小实现后新 helper 单测通过，local CR 又用负数 limit characterization 先红后绿，确认 helper 不额外改写旧切片行为；目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `121 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1041 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #41 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；loot read model 返回 `items=120 / instances=12`，`slot=head` 返回 `items=42 / instances=12`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive/dead 且 `Result=success / ExecMainStatus=0`，9 个 `wow-*` timers listed，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #41 已 rebase merge 到 `main@0a2878d` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-loot-envelope-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-loot-envelope-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim talent import read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十一刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_talent_import_template_read_model()`，把 `get_websim_talent_import()` 中已选 verified community talent template row 到 `websim_talent_import_response(..., template=...)` structured template payload 的只读字段组装从 store 收敛到 PG read-model selector helper。`server/postgres_cache_store.py` 保留 active season gate、class/spec/hero normalization、talent import SQL、no-row fallback response、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_talent_import_template_read_model_maps_row` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `120 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1040 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #40 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/talents/import`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；当前线上 167 个 class/spec/hero 组合均无 verified raw import template，talent import route 走 no-template fallback，helper template 分支由本地 characterization 覆盖；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive/dead 且 `Result=success / ExecMainStatus=0`，9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #40 已 rebase merge 到 `main@fd201e6` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-talent-import-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-talent-import-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 admin talent records read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第三十刀继续拆 `server/postgres_cache_store.py` 的只读 PG admin/WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_admin_talent_records_read_model()`，把 `admin_gate_talent_records()` 中已读 `cache.websim_community_talent_templates` rows 与 `cache.websim_talents` tree rows 到后台 structured admin talent records 的只读字段组装和 expired-template 过滤从 store 收敛到 PG read-model selector helper。`server/postgres_cache_store.py` 保留 admin SQL、表读取、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_admin_talent_records_read_model_filters_expired_templates` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `119 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1039 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #39 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 和授权 `/api/admin/gates/summary` 均返回 200；admin queue summary 为 `overallStatus=partial / queueCount=2905 / domainCounts={'news':7,'talents':18,'gear':2836,'gear_templates':44}`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive/dead 且 `Result=success / ExecMainStatus=0`，9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #39 已 rebase merge 到 `main@41c1442` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-admin-talent-records-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-admin-talent-records-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim bootstrap read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十九刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_bootstrap_read_model()`，把 `get_websim_bootstrap()` 中已读 season/classes/slots/scenarios/instances/sync/defaultSelection/SimC version 到 bootstrap structured payload 的只读组装从 store 收敛到 PG read-model selector helper。`server/postgres_cache_store.py` 保留 active season 读取、instances/defaultSelection/syncState 调用、fallback sync-state 决策、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_bootstrap_read_model_assembles_backend_owned_payload` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `118 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1038 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #38 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；bootstrap read model 返回 `classes=13 / instances=12 / defaultSelection={'classKey':'deathknight','specKey':'blood'}`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`Result=success / ExecMainStatus=0`，`wow-stat-weights-sync` 当前 timer 触发中 `activating/start`），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #38 已 rebase merge 到 `main@d70158b` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-bootstrap-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-bootstrap-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim default selection read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十八刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_default_selection_read_model()`，把 `get_websim_default_selection()` 中已选 row 到 `defaultSelection` structured payload 的只读字段组装从 store 收敛到 PG read-model selector helper。`server/postgres_cache_store.py` 保留 default-selection SQL 优先级查询、fallback 查询、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_default_selection_read_model_maps_row_and_fallback` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `117 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1037 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #37 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；bootstrap read model 返回 `defaultSelection={'classKey':'deathknight','specKey':'blood'}`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`Result=success / ExecMainStatus=0`，`wow-stat-weights-sync` 当前 timer 触发中 `activating/start`），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #37 已 rebase merge 到 `main@48ab4ec` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-default-selection-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-default-selection-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim profile presets read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十七刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_profile_presets_read_model()`，把 `_websim_presets()` 中 `cache.websim_profile_presets` rows 到 structured profile preset payload 的只读字段组装从 store 收敛到 PG read-model selector helper。`server/postgres_cache_store.py` 保留 presets SQL、no-row `fallback_presets()` 决策、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_profile_presets_read_model_maps_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `116 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1036 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #36 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；talents read model 返回 `talentStatus=verified / presets=2 / communityTemplates=2`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`Result=success / ExecMainStatus=0`，`wow-stat-weights-sync` 当前 timer 触发中 `activating/start`），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #36 已 rebase merge 到 `main@b19b31a` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-profile-presets-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-profile-presets-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim assets read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十六刀继续拆 `server/postgres_cache_store.py` 的只读 PG WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_assets_read_model()`，把 `get_websim_assets()` 中 `cache.websim_asset_registry` rows 到 structured assets、status/source counts、empty/verified status 的只读组装从 store 收敛到 PG read-model selector helper。`server/postgres_cache_store.py` 保留 asset filter mapping、SQL query、limit guard、PG unavailable blocked response、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_assets_read_model_counts_status_and_source` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `115 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1035 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #35 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；assets read model 返回 `status=verified / assets=200 / byStatus={'verified':200} / bySource={'blizzard':200}`，`entityType=item&limit=5` 返回 `assets=5`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`Result=success / ExecMainStatus=0`），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #35 已 rebase merge 到 `main@4d92d13` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-assets-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-assets-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim instances read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十五刀继续拆 `server/postgres_cache_store.py` 的只读 PG gear/WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_instances_read_model()`，把 `get_websim_instances()` 中 `cache.websim_instances` rows 与 `cache.websim_encounters` rows 到 structured instances list 的 encounter grouping 和字段组装从 store 收敛到 PG gear read-model selector helper。`server/postgres_cache_store.py` 保留 active season gate、instances/encounters SQL、空表短路、loot/gear response envelope、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_instances_read_model_groups_encounters_by_instance` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `114 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1034 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #34 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；loot read model 返回 `items=120 / instances=12 / firstInstanceEncounters=1 / firstItemHasGameAsset=true`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`Result=success / ExecMainStatus=0`），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #34 已 rebase merge 到 `main@895f5c6` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-instances-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-instances-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 WebSim loot item read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十四刀继续拆 `server/postgres_cache_store.py` 的只读 PG gear/WebSim read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_websim_loot_items_read_model()`，把 `get_websim_loot()` 中 `cache.websim_loot` rows 到 normalized loot item list 的只读 JSON 解析、`normalize_gear_item`、game asset 注入、filters 和 limit 组装从 store 收敛到 PG gear read-model selector helper。`server/postgres_cache_store.py` 保留 active season gate、loot SQL、instances 查询、response envelope、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_websim_loot_items_read_model_maps_filters_and_limits_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `113 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1033 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #33 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/loot`、`/api/websim/gear` initial/slot 均返回 200；loot read model 无过滤返回 `items=120 / instances=12 / firstItemSourceType=verifiedLoot / firstItemHasGameAsset=true`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`Result=success / ExecMainStatus=0`），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #33 已 rebase merge 到 `main@3baed2d` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-websim-loot-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-websim-loot-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 admin gear variant record read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十三刀继续拆 `server/postgres_cache_store.py` 的只读 PG/admin read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_admin_gear_variant_records_read_model()`，把 `admin_gate_gear_variant_records()` 与 `admin_gate_gear_variant_records_page()` 中 `cache.websim_gear_variants` row 到后台 `gearVariants` record 的只读 JSON/字段组装从 store 收敛到 PG gear read-model selector helper。`server/postgres_cache_store.py` 保留 admin gate SQL、分组分页 SQL、source label subquery、table/cache 读取边界、cache write 和 sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_admin_gear_variant_records_read_model_maps_variant_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `112 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1032 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #32 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 和授权 `/api/admin/gates/summary` 均返回 200；admin queue summary 为 `queueCount=2905 / gear_templates=44`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`wow-websim-sync`、`wow-stat-weights-sync` 为 timer 触发中的 `activating/start` 且 `Result=success / ExecMainStatus=0`，其余 sync/backfill services inactive/dead），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #32 已 rebase merge 到 `main@cb228f6` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-admin-gear-variant-record-selector/evidence.json`, `artifacts/releases/2026-07-09-admin-gear-variant-record-selector/manifest.json`.

> 2026-07-09 CST Phase 4 admin gear template display read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十二刀继续拆 `server/postgres_cache_store.py` 的只读 PG/admin read-model selector 边界，新增 `server/pg_gear_template_selectors.py::build_admin_gear_template_display_records_read_model()`，把 `_admin_gate_gear_template_display_records()` 中 admin gear template display slot grouping、community best 选择（`strict_active=False`）与 baseline display selection 的只读组装从 store 收敛到 PG template selector helper。`server/postgres_cache_store.py` 只保留薄委托；admin gate SQL、`to_regclass` 表存在判断、官方 metadata SQL 查询、offhand coverage repair adapter、cache write 和 sync/write/backfill 路径不触达；recommended/season prototype build paths、cleanup、payload cache write 和公开 observed-only policy 不变。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_admin_gear_template_display_records_read_model_groups_display_slots` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_template_selectors_test` + `tests.postgres_cache_store_test` 为 `109 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1031 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #31 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 和授权 `/api/admin/gates/summary` 均返回 200；admin queue summary 为 `queueCount=2905 / gear_templates=44`；40 专精线上 `mode=initial` 权威列表巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services 已记录状态（`wow-websim-sync`、`wow-stat-weights-sync` 为 timer 触发中的 `activating/start` 且 `Result=success / ExecMainStatus=0`，其余 sync/backfill services inactive/dead），9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #31 已 rebase merge 到 `main@946abbe` 并同步到本地。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-admin-gear-template-record-selector/evidence.json`, `artifacts/releases/2026-07-09-admin-gear-template-record-selector/manifest.json`.

> 2026-07-09 CST Phase 4 admin gear template queue read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十一刀继续拆 `server/postgres_cache_store.py` 的只读 PG/admin read-model selector 边界，新增 `server/pg_gear_template_selectors.py::build_admin_gear_template_queue_rows_read_model()`，把 `admin_gate_queue_summary()` 中 community gear template rows + official metadata 到 `gear_templates` queue summary rows 的只读 blocker 组装从 store 收敛到 PG template selector helper。`server/postgres_cache_store.py` 保留 admin gate SQL、`to_regclass` 表存在判断、官方 metadata SQL 查询、offhand coverage repair adapter、cache write 和 sync/write/backfill 路径；recommended/season prototype build paths、cleanup、payload cache write 和公开 observed-only policy 不变。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_admin_gear_template_queue_rows_read_model_preserves_blockers_after_repair` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_template_selectors_test` + `tests.postgres_cache_store_test` 为 `108 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1030 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #30 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 和授权 `/api/admin/gates/summary` 均返回 200；admin queue summary 为 `queueCount=2905 / gear_templates=44`；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive/dead 且 `Result=success / ExecMainStatus=0`，9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #30 已 rebase merge 到 `main@9ef7859` 并同步到本地，合入后 main tree 与 PR branch tree 一致。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-admin-gear-template-queue-selector/evidence.json`, `artifacts/releases/2026-07-09-admin-gear-template-queue-selector/manifest.json`.

> 2026-07-09 CST Phase 4 community template list read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第二十刀继续拆 `server/postgres_cache_store.py` 的只读 PG template read-model selector 边界，新增 `server/pg_gear_template_selectors.py::build_community_gear_templates_read_model()`，把 `_gear_community_templates()` 中 filtered rows + official metadata 到 deduped community template list 的只读组装从 store 收敛到 PG template selector helper。`server/postgres_cache_store.py` 保留 community template SQL、过期过滤、官方 metadata SQL 查询、offhand coverage repair adapter、cache write 和 sync/write/backfill 路径；`_community_gear_template_from_row()` 的 recommended/season 内部调用点不触达，recommended/season prototype build paths、cleanup、payload cache write 和公开 observed-only policy 不变。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_community_gear_templates_read_model_delegates_rows_to_dedupe` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_template_selectors_test` + `tests.postgres_cache_store_test` 为 `107 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1029 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #29 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive/dead 且 `Result=success / ExecMainStatus=0`，9 个 `wow-*` timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #29 已 rebase merge 到 `main@9decb21` 并同步到本地，合入后 main tree 与 PR branch tree 一致。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-community-template-list-selector/evidence.json`, `artifacts/releases/2026-07-09-community-template-list-selector/manifest.json`.

> 2026-07-09 CST Phase 4 community template row read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十九刀继续拆 `server/postgres_cache_store.py` 的只读 PG template read-model selector 边界，新增 `server/pg_gear_template_selectors.py::build_community_gear_template_read_model()`，把 `cache.websim_community_gear_templates` row + official item metadata 到 backend-owned community template read model 的字段组装从 store 收敛到 PG template selector helper。`server/postgres_cache_store.py` 保留 community template SQL、过期过滤、官方 metadata SQL 查询、offhand coverage repair adapter、dedupe、cache write 和 sync/write/backfill 路径；recommended/season prototype build paths、cleanup、payload cache write 和公开 observed-only policy 不触达。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_community_gear_template_read_model_preserves_payload_evidence` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_template_selectors_test` + `tests.postgres_cache_store_test` 为 `106 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1028 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #28 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive/dead 且 `Result=success / ExecMainStatus=0`，7 个相关 timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，部署后日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #28 已 rebase merge 到 `main@479df1a` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-community-template-row-selector/evidence.json`, `artifacts/releases/2026-07-09-community-template-row-selector/manifest.json`.

> 2026-07-09 CST Phase 4 community template item ref read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十八刀继续拆 `server/postgres_cache_store.py` 的只读 PG template read-model selector 边界，新增 `server/pg_gear_template_selectors.py::collect_community_template_item_refs_read_model()`，把 `_gear_community_templates()` 中 community template rows 的 `gear_items_json` item ref flatten 从 store 收敛到 PG template selector helper。`server/postgres_cache_store.py` 保留 community template row SQL、过期过滤、官方 metadata 查询 adapter、template envelope 组装和 offhand coverage repair；recommended/season prototype build paths、sync/write/backfill、payload cache write、cleanup 不触达。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_collect_community_template_item_refs_read_model_flattens_gear_item_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_template_selectors_test` + `tests.postgres_cache_store_test` 为 `105 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1027 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #27 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`，其余 sync/backfill services inactive/dead，7 个相关 timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #27 已 rebase merge 到 `main@d497ac0` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-community-template-item-ref-selector/evidence.json`, `artifacts/releases/2026-07-09-community-template-item-ref-selector/manifest.json`.

> 2026-07-09 CST Phase 4 community gear item hydration read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十七刀继续拆 `server/postgres_cache_store.py` 的只读 PG template read-model selector 边界，新增 `server/pg_gear_template_selectors.py::build_hydrated_community_gear_items_read_model()`，把 community template `gear_items_json` 到官方 metadata 水合后的 gear item list 组装从 store 收敛到 PG template selector helper。`server/postgres_cache_store.py` 保留 community template row 读取、官方 metadata 查询 adapter、template envelope 组装和 offhand coverage repair；`_community_gear_template_from_row()` 调用语义不变；sync/write/backfill、payload cache write、cleanup 不触达。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_hydrated_community_gear_items_read_model_applies_official_metadata` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_template_selectors_test` + `tests.postgres_cache_store_test` 为 `104 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1026 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #26 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`，其余 sync/backfill services inactive/dead，7 个相关 timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #26 已 rebase merge 到 `main@fc2a8d7` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-community-gear-item-hydration-selector/evidence.json`, `artifacts/releases/2026-07-09-community-gear-item-hydration-selector/manifest.json`.

> 2026-07-09 CST Phase 4 official item metadata row read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十六刀继续拆 `server/postgres_cache_store.py` 的只读 PG read-model selector 边界，新增 `server/pg_gear_template_selectors.py::build_official_item_metadata_by_id_read_model()`，把 `cache.websim_items` row 到官方 item metadata 映射的来源过滤、verified+official payload shape 兼容判断、展示名/等级/图标/类型 metadata 组装从 store 收敛到 PG template selector helper。`server/postgres_cache_store.py` 保留 template item id 收集、PG SQL 读取和 `cur.fetchall()` 委托；`_hydrated_community_gear_items()` 与 `_community_gear_template_from_row()` 调用语义不变；sync/write/backfill、payload cache write、cleanup 不触达。公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开入口。红测 `test_build_official_item_metadata_by_id_read_model_filters_unofficial_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_template_selectors_test` + `tests.postgres_cache_store_test` 为 `103 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1025 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #25 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`，其余 sync/backfill services inactive/dead，7 个相关 timers listed，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #25 已 rebase merge 到 `main@2e8943e` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-official-item-metadata-selector/evidence.json`, `artifacts/releases/2026-07-09-official-item-metadata-selector/manifest.json`.

> 2026-07-09 CST Phase 4 season catalog candidate read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十五刀继续拆 `server/postgres_cache_store.py` 的只读 PG read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_season_recommended_catalog_candidates_by_slot_read_model()`，把 `season_recommendation` 内部构建时“catalog items -> 按槽候选池”的合法性过滤、去重、排序和 24 条 limit 从 store 收敛到只读 selector helper。`server/postgres_cache_store.py` 保留 PG SQL 读取、active season 读取、raw row cache、payload cache fingerprint、cache put、sync/write/backfill 路径；公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空，`season_recommendation` 仍是内部候选/基线 evidence，不回流公开入口。红测 `test_build_season_recommended_catalog_candidates_by_slot_read_model_filters_and_limits_candidates` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `111 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1024 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #24 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`、`Result=success`、`ExecMainStatus=0`，其余 sync/backfill services inactive/dead 且 `Result=success`，7 个相关 timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #24 已 rebase merge 到 `main@796c147` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-season-catalog-candidate-selector/evidence.json`, `artifacts/releases/2026-07-09-season-catalog-candidate-selector/manifest.json`.

> 2026-07-09 CST Phase 4 PG gear mod option type read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十四刀继续拆 `server/postgres_cache_store.py` 的只读 PG row/read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_gear_mod_options_by_type_read_model()`，把 `cache.websim_gear_mod_options` row 按 socket/enchant/embellishment 分类并委托 slot-level option read model 的逻辑从 store 收敛到只读 selector helper。`server/postgres_cache_store.py` 保留 PG SQL 读取、payload cache fingerprint、cache put、sync/write/backfill 路径；option display/config policy 仍委托 `websim_payload` 权威 helper，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_gear_mod_options_by_type_read_model_groups_option_types` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `110 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1023 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #23 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-websim-sync` 与 `wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`、`Result=success`、`ExecMainStatus=0`，其余 sync/backfill services inactive/dead 且 `Result=success`，7 个相关 timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #23 已 rebase merge 到 `main@1f261b7` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-mod-option-type-selector/evidence.json`, `artifacts/releases/2026-07-09-pg-mod-option-type-selector/manifest.json`.

> 2026-07-09 CST Phase 4 PG gear catalog item row read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十三刀继续拆 `server/postgres_cache_store.py` 的只读 PG row/read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_gear_catalog_items_read_model()`，把 `cache.websim_items` row 与已抽出的 source/variant/mod option read models 组装为 catalog candidate items 的逻辑从 store 收敛到只读 selector helper。`server/postgres_cache_store.py` 保留 PG SQL 读取、item id selection、payload cache fingerprint、cache put、sync/write/backfill 路径；catalog source activation、item normalization、variant/default enrichment、mod option sanitize 仍委托 `websim_payload` 权威 helper，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_gear_catalog_items_read_model_enriches_item_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `109 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1022 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #22 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-websim-sync` 与 `wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`、`Result=success`、`ExecMainStatus=0`，其余 sync/backfill services inactive/dead 且 `Result=success`，7 个相关 timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #22 已 rebase merge 到 `main@1af6dba` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-catalog-item-row-selector/evidence.json`, `artifacts/releases/2026-07-09-pg-catalog-item-row-selector/manifest.json`.

> 2026-07-09 CST Phase 4 PG gear mod option row read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十二刀继续拆 `server/postgres_cache_store.py` 的只读 PG row/read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_gear_mod_options_by_slot_read_model()`，把 `cache.websim_gear_mod_options` row 到按 slot 分组的 socket/enchant/embellishment option read model 组装从 store 收敛到只读 selector helper。`server/postgres_cache_store.py` 保留 PG SQL 读取、mod option row filtering、catalog item assembly、payload cache fingerprint、cache put、sync/write/backfill 路径；option display/config policy 仍委托 `websim_payload` 权威 helper，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_gear_mod_options_by_slot_read_model_groups_option_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `108 tests OK`，owner-map 合同测试先红后绿；完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1021 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #21 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-websim-sync` 与 `wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`、`Result=success`、`ExecMainStatus=0`，其余 sync/backfill services inactive/dead 且 `Result=success`，7 个相关 timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #21 已 rebase merge 到 `main@a156e36` 并同步到本地，main tree 与候选 branch tree 一致。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-mod-option-row-selector/evidence.json`, `artifacts/releases/2026-07-09-pg-mod-option-row-selector/manifest.json`.

> 2026-07-09 CST Phase 4 PG gear variant row read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十一刀继续拆 `server/postgres_cache_store.py` 的只读 PG row/read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_gear_variants_by_item_read_model()`，把 `cache.websim_gear_variants` row 到按 `itemId` 分组的 variant read model 组装从 store 收敛到只读 selector helper。`server/postgres_cache_store.py` 保留 PG SQL 读取、season recommended catalog raw rows、catalog item assembly、mod option 组装、payload cache fingerprint、cache put、sync/write/backfill 路径；两个调用点改为委托 helper，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_gear_variants_by_item_read_model_groups_variant_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `107 tests OK`，owner-map 合同测试先红后绿。完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1020 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #20 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，`wow-websim-sync` 与 `wow-stat-weights-sync` 为 timer 触发中且当前 `activating/start`、`Result=success`、`ExecMainStatus=0`，其余 sync/backfill services inactive/dead 且 `Result=success`，7 个相关 timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟相关日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #20 已 rebase merge 到 `main@4c9cac1` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-gear-variant-row-selector/evidence.json`, `artifacts/releases/2026-07-09-pg-gear-variant-row-selector/manifest.json`.

> 2026-07-09 CST Phase 4 PG gear source row read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第十刀继续拆 `server/postgres_cache_store.py` 的只读 PG row/read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_gear_sources_by_item_read_model()`，把 `cache.websim_gear_sources` row 到按 `itemId` 分组的 source read model 组装从 store 收敛到只读 selector helper。`server/postgres_cache_store.py` 保留 PG SQL 读取、season recommended catalog raw rows、catalog item assembly、payload cache fingerprint、cache put、sync/write/backfill 路径；两个调用点改为委托 helper，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_gear_sources_by_item_read_model_groups_source_rows` 先因 helper 缺失失败；最小实现后新 helper 单测通过，目标验证 `tests.pg_gear_read_model_selectors_test` + `tests.postgres_cache_store_test` 为 `106 tests OK`，owner-map 合同测试先红后绿。完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1019 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #19 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive 且 `Result=success`，7 个相关 timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #19 已 rebase merge 到 `main@e199007` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-gear-source-row-selector/evidence.json`, `artifacts/releases/2026-07-09-pg-gear-source-row-selector/manifest.json`.

> 2026-07-09 CST Phase 4 PG catalog output read-model fragment（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第九刀继续拆 `server/postgres_cache_store.py` 的只读 PG read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_catalog_output_read_model_fragment()`，把 full/slot gear payload 中从 `catalog_read_model` 派生的 `catalogItems` 120 条切片、非 compact `slotGroups`、`presets`、`candidateItems` 与 `candidateLegalityAudit` 输出收敛到只读 selector helper。`server/postgres_cache_store.py` 继续负责 PG SQL 读取、payload cache fingerprint、template/catalog/common read model 调用、最终 response envelope 与 cache put；sync/write/backfill 路径不变，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_catalog_output_read_model_fragment_keeps_full_debug_fields_out_of_compact_payload` 先因 helper 缺失失败；最小实现后目标 tests 通过，完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1018 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #18 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive 且 `Result=success`，相关 timers active/waiting（manual prototype sync 无 timer），`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #18 已 rebase merge 到 `main@b654d77` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-catalog-output-read-model-fragment/evidence.json`, `artifacts/releases/2026-07-09-pg-catalog-output-read-model-fragment/manifest.json`.

> 2026-07-09 CST Phase 4 PG common gear read-model fragment（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第八刀继续拆 `server/postgres_cache_store.py` 的只读 PG read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_common_gear_read_model_fragment()`，把 initial 与 full/slot gear payload 共同使用的 `weaponRule`、`slots`、`readiness`、blocked `statSnapshot`、`maxLevel` 与顶层 `checkedAt` 组装收敛到只读 selector helper。`server/postgres_cache_store.py` 继续负责 PG SQL 读取、payload cache fingerprint、template/catalog read model 调用、initial/full/slot 最终 response envelope 与 cache put；sync/write/backfill 路径不变，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_common_gear_read_model_fragment_blocks_stat_snapshot` 先因 helper 缺失失败；最小实现后目标 tests 通过，完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1017 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #17 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive 且 `Result=success`，相关 timers active/waiting（manual prototype sync 无 timer），`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #17 已 rebase merge 到 `main@b0af412` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-common-gear-read-model-fragment/evidence.json`, `artifacts/releases/2026-07-09-pg-common-gear-read-model-fragment/manifest.json`.

> 2026-07-09 CST Phase 4 PG initial gear read-model fragment（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第七刀继续拆 `server/postgres_cache_store.py` 的只读 PG read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_initial_gear_read_model_fragment()`，把 initial gear payload 中 baseline template 到 `equippedSet`、16 槽 `replacementCandidates` summary、`slotReadiness`、`baselineSet`、`readiness` 与初始 `catalogItems` 的组装收敛到只读 selector helper。`server/postgres_cache_store.py` 继续负责 PG SQL 读取、payload cache fingerprint、template read model、catalog state fragment、statSnapshot、最终 response envelope 与 cache put；sync/write/backfill 路径不变，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_initial_gear_read_model_fragment_compacts_baseline_template` 先因 helper 缺失失败；最小实现后目标 tests 通过，完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1016 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #16 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive 且 `Result=success`，相关 timers active/waiting（manual prototype sync 无 timer），`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #16 已 rebase merge 到 `main@fed3f3e` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-initial-gear-read-model-fragment/evidence.json`, `artifacts/releases/2026-07-09-pg-initial-gear-read-model-fragment/manifest.json`.

> 2026-07-09 CST Phase 4 PG catalog state read-model fragment（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第六刀继续拆 `server/postgres_cache_store.py` 的只读 PG read-model selector 边界，新增 `server/pg_gear_read_model_selectors.py::build_catalog_state_read_model_fragment()`，把 initial 与 full/slot gear payload 共同使用的 `gearSchemaRevision`、`gearCatalogRevision`、`catalogStatus`、`catalogHealthSummary`、`catalogCoverage`、`itemDatabaseRevision`、`variantRevision`、`catalogCheckedAt` 和 `catalogBlockers` 组装收敛到只读 selector helper。`server/postgres_cache_store.py` 继续负责 PG SQL 读取、payload cache fingerprint、template/candidate read-model 调用、最终 response envelope 与 cache put；sync/write/backfill 路径不变，公开 `communityTemplates` 仍只允许 active `raiderio_observed_profile`，`baselineTemplates` 默认仍为空。红测 `test_build_catalog_state_read_model_fragment_exposes_health_and_coverage_envelope` 先因 helper 缺失失败；最小实现后目标测试通过，完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1015 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #15 branch 已按 Candidate Deployment Gate 热部署：公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200；40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive 且 `Result=success`，timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #15 已 rebase merge 到 `main@608b77f` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-catalog-state-read-model-fragment/evidence.json`, `artifacts/releases/2026-07-09-pg-catalog-state-read-model-fragment/manifest.json`.

> 2026-07-09 CST Phase 4 PG catalog read-model selector（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第五刀继续拆 `server/postgres_cache_store.py` 的只读 PG read-model 边界，新增 `server/pg_gear_read_model_selectors.py::build_catalog_gear_read_model_fragment()`，把 full/slot gear payload 分支中的 catalog item legality filtering、`replacementCandidates` 分槽、compact/full candidate 输出、`catalogItems` compaction、`readiness` / `slotReadiness` 与 `candidateLegalityAudit` 组装收敛到只读 selector helper。`server/postgres_cache_store.py` 继续负责 PG SQL 读取、payload cache fingerprint、template read model、catalog health 外层字段和最终 response envelope；sync/write/backfill 路径不变，公开 `communityTemplates` / `baselineTemplates` 仍由 observed-only template selector 管理。红测 `test_build_catalog_gear_read_model_fragment_groups_candidates_and_audits_blocked_items` 先因缺少 `server.pg_gear_read_model_selectors` 失败；最小实现后目标验证 `tests.pg_gear_read_model_selectors_test` 与相关 PG store/template tests 通过。完整本地验证：`python3 -m unittest discover -s tests -p '*_test.py'` 为 `1014 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #14 branch 已按 Candidate Deployment Gate 先热部署：`WOW_DEPLOY_SKIP_BOOTSTRAP=1 WOW_DEPLOY_START_ASYNC_SYNCS=0 ./server/deploy_lighthouse.sh` 部署到 `ubuntu@124.223.51.33:/opt/wow-mini-program`，未手动启动 async sync；公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 均返回 200。40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive 且 `Result=success`，timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #14 已 rebase merge 到 `main@6c7bf71` 并同步到本地。Evidence: `server/pg_gear_read_model_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_read_model_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-catalog-read-model-selector/evidence.json`, `artifacts/releases/2026-07-09-pg-catalog-read-model-selector/manifest.json`.

> 2026-07-09 CST Phase 4 PG template read-model adapter（正在推进 / 候选部署已通过 / 已合入）: Phase 4 第四刀继续沿 `server/postgres_cache_store.py` 的只读 PG read-model selector 边界小步拆分，新增 `server/pg_gear_template_selectors.py::build_public_gear_template_read_model()`，把 persisted community gear templates 经过 observed-only public selector 后的 payload-ready `communityTemplates` / `baselineTemplates`、raw selected templates 与 `communityTemplateSync` 组装收敛到 selector helper。`server/postgres_cache_store.py` 的 initial 与 full/slot 读模型分支继续负责 PG SQL 读取、catalog candidates、slot grouping、readiness 和 catalog health，但不再直接 compact/sync public template payload；sync/write/backfill 路径不变。红测 `test_build_public_gear_template_read_model_compacts_payload_without_public_baseline` 先因缺少 helper 失败；最小实现后目标验证 5 tests OK，完整本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1013 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON validation、`git diff --check` 和本地 CR 通过。PR #13 branch 已按 Candidate Deployment Gate 先热部署：`WOW_DEPLOY_SKIP_BOOTSTRAP=1 WOW_DEPLOY_START_ASYNC_SYNCS=0 ./server/deploy_lighthouse.sh` 部署到 `ubuntu@124.223.51.33:/opt/wow-mini-program`，未手动启动 async sync；公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 和 `/websim/` 均返回 200。40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；`wow-backend`/nginx active，sync/backfill services inactive 且 `Result=success`，timers active/waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 20 分钟日志无 Traceback/ERROR/SQLite fallback，关键 server runtime 文件远端 hash 与本地 branch 一致。PR #13 已 rebase merge 到 `main@a7fc163` 并同步到本地。公开入口仍保持 active `raiderio_observed_profile` observed-only，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开 `communityTemplates` / `baselineTemplates`。Evidence: `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-template-read-model-adapter/evidence.json`, `artifacts/releases/2026-07-09-pg-template-read-model-adapter/manifest.json`.

> 2026-07-09 CST Harness v0.5 + Phase 4 PG selector helper（正在推进 / 候选部署已通过 / 已合入）: 用户确认把“部署验证先于合入”作为后续 backend/API/read model 默认流程后，Harness 已升级到 v0.5，新增 Candidate Deployment Gate：backend/API、PG read model、公开 payload、health/admin、定时任务、部署脚本和用户可见 runtime 变更默认在 PR 候选阶段先部署或预览 smoke，通过后再合入。Phase 4 第三刀继续做小步结构拆分，新增 `server/pg_gear_template_selectors.py` 作为 PG gear selector 只读 helper，`server/postgres_cache_store.py` 的 initial 与 full/slot 读模型分支改为委托 `select_public_gear_templates_for_spec()`；SQL 读取、sync 写入路径和公开 observed-only 合同不变。红测覆盖 Harness gate 缺失与 helper 缺失；本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `1012 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、`node --check`、JSON 和 `git diff --check` 通过。PR #12 branch 已按 Candidate Deployment Gate 先热部署：`WOW_DEPLOY_SKIP_BOOTSTRAP=1 WOW_DEPLOY_START_ASYNC_SYNCS=0 ./server/deploy_lighthouse.sh` 部署到 `ubuntu@124.223.51.33:/opt/wow-mini-program`，未手动启动 async sync；公网 `/health`、`/api/data/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 和 `/websim/` 均返回 200。40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`；关键 runtime 文件远端 hash 与本地 branch 一致，`wow-backend`/nginx active，sync/backfill services inactive 且 timers waiting，`WOW_DATABASE_RUNTIME=postgres_only`，近 15 分钟日志无 Traceback/ERROR/SQLite fallback。`/api/data/health` 仍为既有 `overallStatus=partial`，其中 `community_templates`、`websim_sync`、`template_simc_bridge`、`season_cutover_readiness` 等 partial/blocked 不属于本次只读 selector split 回归。PR #12 已 rebase merge 到 `main@704714f` 并同步到本地。Evidence: `docs/harness.md`, `AGENTS.md`, `scripts/project-harness.js`, `server/pg_gear_template_selectors.py`, `server/postgres_cache_store.py`, `tests/pg_gear_template_selectors_test.py`, `tests/project-harness.test.js`, `tests/backend-owner-map.test.js`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-pg-selector-read-model-gate/evidence.json`, `artifacts/releases/2026-07-09-pg-selector-read-model-gate/manifest.json`.

> 2026-07-09 CST Phase 4 gear public contract PG selector callers（正在推进 / 已部署 / 线上 smoke 通过）: 在 `server/gear_public_contract.py` 第一刀抽取后，第二刀把 `server/postgres_cache_store.py` 的 PG gear selector 调用面从 `websim_payload.py` 兼容导出收敛到 `gear_public_contract` 模块限定调用，覆盖 public source policy、observed-only visibility、baseline fallback gating、real-player public import 配置与 `canApplyGear` 判断；`websim_payload.py` 兼容导出继续保留，sync 写入路径暂不移动。新增 `test_pg_initial_gear_selector_calls_gear_public_contract_module` 先因缺少模块依赖失败，再证明 PG initial selector 分支实际调用 `gear_public_contract.public_gear_templates_for_spec` 和 `gear_public_contract.public_baseline_fallback_templates_for_spec`；既有 `test_pg_gear_template_selectors_match_observed_only_golden_payload` 继续固定公开 read model 输出。完整本地验证：`python3 -m unittest discover -s tests -p '*_test.py'` 为 `1011 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，`py_compile`、JSON、Harness manifest 与 `git diff --check` 通过。合入后已按用户补充要求执行热部署：`WOW_DEPLOY_SKIP_BOOTSTRAP=1 WOW_DEPLOY_START_ASYNC_SYNCS=0 ./server/deploy_lighthouse.sh` 部署到 `ubuntu@124.223.51.33:/opt/wow-mini-program`，`wow-backend`/nginx active，`WOW_DATABASE_RUNTIME=postgres_only`，关键文件远端 hash 与本地 `main@7f1cb6d` 一致；公网 `/health`、`/api/websim/bootstrap`、`/api/websim/assets`、`/api/websim/talents`、`/api/websim/gear` initial/slot 与 `/websim/` 均返回 200。40 专精线上 `mode=initial` 巡检为 `checkedSpecs=40 / failures=0 / dataStatus={'verified':40} / communityTemplates={1:40} / baselineTemplates={0:40} / sourceKeys={'raiderio_observed_profile':40} / canApplyGear={'True':40}`。`/api/data/health` 仍为既有 `overallStatus=partial`，其中 `community_templates`、`websim_sync`、`template_simc_bridge`、`season_cutover_readiness` 的 partial/blocked 不属于本次 selector routing 回归；后端日志近 10 分钟无 Traceback/ERROR/SQLite fallback。公开入口语义保持 observed-only，不重新开放 `recommended_bis`、`season_recommendation`、`default_template`、`simc_preset` 或 `baseline_blocked`；本次部署没有手动启动 PG-native async sync，所有 sync/backfill service smoke 后为 inactive。线上既有 `wow-gear-observed-backfill.timer` 已处于 enabled/active waiting，并在 2026-07-09 16:43:54 CST 自动触发一次，`ExecMainStatus=0`、`status=partial` 后退出；这是既有 timer 状态，不是 `WOW_DEPLOY_START_ASYNC_SYNCS=0` 启动的手动同步。Evidence: `server/postgres_cache_store.py`, `server/gear_public_contract.py`, `tests/postgres_cache_store_test.py`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/releases/2026-07-09-gear-public-contract-pg-selector-callers/evidence.json`, `artifacts/releases/2026-07-09-gear-public-contract-pg-selector-callers/manifest.json`.

> 2026-07-09 CST Phase 3/4 gear public contract extraction foundation（正在推进 / 本地验证已通过）: Harness 后端热点治理已从 owner map / characterization 推进到第一条小步结构拆分。新增 `tests/fixtures/gear-serializer-compact-payload.json`、`test_gear_serializer_matches_compact_golden_payload` 固定 compact serializer/readiness/stat blocked 输出；新增 `tests/fixtures/pg-gear-template-selectors-observed-only.json`、`test_pg_gear_template_selectors_match_observed_only_golden_payload` 固定 PG `get_websim_gear(... compact=True, mode="initial")` 的 observed-only selector、空 baseline 与 template chain 输出；新增 `server/gear_public_contract.py` 作为公开装备模板合同 adapter/selector 模块，`server/websim_payload.py` 保留旧函数导出并委托新模块，PG store 现有导入面不变。公开入口语义保持不变：active `raiderio_observed_profile` observed-only，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开 `communityTemplates` / `baselineTemplates`。本地验证：`python3 -m unittest discover -s tests -p '*_test.py'` 为 `1010 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `344 pass`，JSON/py_compile/`git diff --check` 通过；本轮不部署、不触发 PG/sync/线上任务。Evidence: `server/gear_public_contract.py`, `server/websim_payload.py`, `tests/gear_public_contract_test.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `artifacts/releases/2026-07-09-gear-public-contract-extraction-foundation/manifest.json`, `docs/backend-owner-map.json`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`.

> 2026-07-09 CST Phase 3 gear legality source-map golden payload（正在推进）: 在后端热点文件拆分前，`gear_legality_source_map` 新增完整 golden payload characterization：`tests/fixtures/gear-legality-source-map-partial-authority.json` 与 `test_gear_legality_source_map_matches_partial_authority_golden_payload` 固定 `gear_legality_authority_health_payload` + `sourceMap` 的 partial authority 输出。当前语义保持 official / SimC source missing，observed Raider.IO profile 只作为 supporting evidence，不升级 verified；manual override 仍为 active 但 authority status partial，公开装备入口和 payload 行为不变。本轮不抽模块、不移动业务函数、不部署。Evidence: `docs/backend-owner-map.json`, `tests/fixtures/gear-legality-source-map-partial-authority.json`, `tests/websim_payload_test.py`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`.

> 2026-07-09 CST Phase 3 backend owner map v1（正在推进）: Harness Phase 3 已开始，但当前只做重构前 owner map / characterization 准备，不拆热点文件、不改公开 payload。新增 `docs/backend-owner-map.json`，覆盖 `server/websim_payload.py` 的 `gear_public_contract`、`gear_legality_source_map`、`gear_serializer_golden_payload`、`talent_public_contract`，`server/news_backend.py` 的 `runtime_api_wiring`、`health_admin_summary`、`news_content_api`、`chickenbro_session_api`，以及 `server/postgres_cache_store.py` 的 `gear_template_selectors`、`sync_state_repository`、`recommended_bis_evidence`、`cleanup_residue_control`；`docs/README.md` 与 `scripts/project-harness.js` 已将该 owner map 纳入 current truth。当前 characterization anchors 已验证：owner map test、Harness manifest test、`test_real_player_public_policy_applies_to_all_specs`、PG runtime mode/fallback/template-chain tests、非法 PG template legality gate tests；`gear_public_contract` 另新增 `tests/fixtures/gear-public-contract-observed-only.json` golden payload 和 `test_gear_public_contract_matches_observed_only_golden_payload`，固定当前 observed-only 输出。公开装备入口仍保持 active `raiderio_observed_profile` observed-only，`recommended_bis`、`season_recommendation`、`default_template`、`simc_preset`、`baseline_blocked` 不回流公开 `communityTemplates` / `baselineTemplates`。Evidence: `docs/backend-owner-map.json`, `docs/README.md`, `scripts/project-harness.js`, `tests/backend-owner-map.test.js`, `tests/project-harness.test.js`, `tests/fixtures/gear-public-contract-observed-only.json`, `tests/websim_payload_test.py`, `tests/news_backend_test.py`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`.

> 2026-07-09 CST Repository sync + autonomous progression + Harness Phase 2（正在推进）: 用户确认“本项目和远端仓库之间的同步相关操作不需要额外授权”后，项目规则已收口为常规 `git fetch`、`git pull --ff-only`、`git push`、项目分支发布、PR 创建/更新/状态读取/合入可直接执行；force push、公开历史改写、改 remote、clone 其他仓库、submodule、依赖安装、第三方下载和生产操作仍需明确确认。用户随后确认无明确阻塞或待决策点时 agent 应自动向下一步推进，不再每个步骤单独请求同意；只有 blocker、验证失败需权衡、待决策、范围变化、工作树/远端冲突或越界高风险操作才停下来确认。Harness 升级到 v0.4，并继续 Phase 2 evidence packet 标准化：`scripts/project-harness.js` 仍不执行测试、不部署、不下载、不写生产，但新增 `--evidence-file` 读取仓库内本地 JSON evidence packet，manifest 会输出 repository remote sync gate、autonomous progression gate 与 evidence packet 完整性。Evidence: `AGENTS.md`, `docs/harness.md`, `scripts/project-harness.js`, `tests/project-harness.test.js`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`.

> 2026-07-09 CST Harness-guided project optimization plan（Phase 0 + Phase 1 已完成 / 当前 UI 基线已验收）: 用户确认采用 “Harness + evidence first” 路线后，已完成 Phase 0 工作树主题收口和 Phase 1 UI 14 页 proof matrix：当前交付基线为 `runtime_recapture_complete_auto_14_screenshots_14_pass_final_accepted`，`finalAccepted=true`、`runtimeVerified=true`、`rescueDeliveryReady=true`、14 个 `app.json` 页面均有当前自动 route+screenshot 证据，risk/fail 为 0。后续 UI 视觉预期调整另开需求，不继续改写本轮已验收基线；下一阶段从 Harness evidence packet 标准化、后端 owner map / characterization、小步结构拆分和 live smoke 归档继续。Evidence: `docs/harness.md`, `docs/plans/2026-07-09-docs-implementation-current-truth-review.md`, `docs/plans/2026-07-09-harness-guided-project-optimization-plan.md`, `artifacts/ui-system-rebuild/20260708-0900-runtime-screenshot-manifest/manifest.json`, `artifacts/ui-system-rebuild/20260708-0900-runtime-screenshot-manifest/final-delivery-audit.json`.

> 2026-07-09 CST Docs / implementation current truth review（已完成 / 当前事实已同步）: 近期大量 PR 合入后的本地对账索引已更新为当前基线：全职业装备公开导入以 `public observed-only` 为准，`season_recommendation` 40/40 public baseline 旧口径已在 runbook 标为 superseded；破坏性 cleanup 仍保持 `shaman:elemental` pilot scope；UI 源码/静态救火不能晋级为 runtime verified，但本轮已通过本机 DevTools `--auto-port 9854` 和 `auto-route-screenshot.js` 补齐 14/14 当前自动截图证据，并由 final delivery audit 标记为 accepted。Chickenbro v0 骨架已实现但继续做证据质量/线上体验验证；PG-only runtime guardrail 已实现但正式迁移/发布口径需继续拆清。Evidence: `docs/plans/2026-07-09-docs-implementation-current-truth-review.md`, `docs/gear-simulation-full-chain-runbook.md`, `artifacts/ui-system-rebuild/20260708-0900-runtime-screenshot-manifest/automator-endpoint-probe.json`, `artifacts/ui-system-rebuild/20260708-0900-runtime-screenshot-manifest/page-captures/manifest.json`, `artifacts/ui-system-rebuild/20260708-0900-runtime-screenshot-manifest/final-delivery-audit.json`.

> 2026-07-08 CST All-spec real-player gear-template public import closure（已完成 / 已部署 / 本地收口）: 今日装备模板链路已从元素萨 pilot 推广为全职业/全专精公开 read model：`REAL_PLAYER_GEAR_TEMPLATE_PUBLIC_POLICY=all_specs`，公开 `communityTemplates` 只允许满足 sourceUrl、角色身份、`sampleCount=1`、`profileHash/gearHash`、`scanRunId/fetchedAt`、16 canonical 槽或合法双手占位、宝石/附魔/美化保留并通过 backend legality gate 的 active `raiderio_observed_profile`；`baselineTemplates` 不再自动补 `recommended_bis`、`season_recommendation`、`default_template`、`simc_preset` 或 `baseline_blocked`。旧方案残留被清理为内部 evidence / legacy fallback：PG 中 `recommended_bis`、`season_recommendation`、`simc_preset` row 可保留供 optimizer/历史基线审计，但小程序公开导入入口、`mode=initial` read model 与 frontend consumer-only 合同不会消费它们。防回流已覆盖 community sync、season recommendation sync、recommended prototype sync 与 guard sync；`communityTemplateSync.templateChains` 显示 `communityObserved`、`recommendedBis`、`legacyFallback` 的真实状态，非 DPS `recommended_bis_v1` 继续 `role_objective_blocked`。本地验证：`python3 -m unittest discover -s tests -p '*_test.py'` 为 `1004 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `295 pass`，`py_compile` 与 `git diff --check` 通过。线上 smoke：`/health=200`、`/api/data/health=200`，40 专精公开 gear API `checked=40 / failure=0 / communityTemplates=1*40 / baselineTemplates=0*40 / communitySourceKeys={'raiderio_observed_profile':40}`；PG active observed `complete/synced=40` 且 source/hash/slot 全齐。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `server/postgres_cache_sync.py`, `server/season_recommended_gear.py`, `server/raiderio_payload.py`, `pages/builds/detail.js`, `pages/builds/detail.wxml`, `tests/*gear*`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-08 CST Gear legality authority source map v1（已完成 / 已部署）: 用户认可“不是继续手写，而是 rule source map”的方向后，已将装备合法性 health/read model 扩展为 `gear-legality-source-map-v1`：每个 spec 记录 `official`、`simc`、`observed`、`manual_override` 四类证据源，当前 `official` / `simc` 均为 `missing`，`manual_override` 为 `active` 但不 verified，生存猎 `哈哈丶帅猎猎` 与狂徒贼 `Zacrebleu` 只作为 `observed` supporting 反例/回归样本。`gear_legality_authority` 继续保持 `partial`，`verifiedSpecs=0`、`manualOverrideSpecs=40`；observed 证据不会让任何专精变成 verified，也不会绕过 backend legality gate。已热部署并触发 `wow-community-best-guard-sync.service`、`wow-recommended-bis-guard-sync.service`，两者 `Result=success / ExecMainStatus=0`。线上 smoke：`/health=200`；`/api/data/health` 中 `sourceMap expected=40 verified=0 manualOverride=40 officialVerified=0 simcVerified=0 observedSupported=40`；40 专精公开 gear API `checked=40 failure=0 communityTemplates=1*40 baselineTemplates=0*40 communitySourceKeys={'raiderio_observed_profile':40}`，无 legacy/recommended/baseline 回流。PG row 汇总：`raiderio_observed_profile complete/synced=40 sourceUrl/sampleCount=1/profileHash/gearHash=40 readySlot=16..16`；内部 `recommended_bis`、`season_recommendation`、`simc_preset` rows 仍存在但不进入公开导入。Evidence: `server/websim_payload.py`, `tests/websim_payload_test.py`, `tests/news_backend_test.py`, `docs/gear-simulation-full-chain-runbook.md`, `docs/roadmap/ideas.md`.

> 2026-07-08 CST Raider.IO observed weapon-rule evidence correction（已完成 / 已部署）: 用户复核生存猎与狂徒贼 Raider.IO 真实角色链接后确认“Raider.IO 上可以拿就说明确实可以拿”，并进一步纠偏为“不是允许突破武器规则，而是完善武器规则”。本轮收回完整 observed 直接绕过 weapon gate 的实现，把真实快照作为规则缺口证据补入 `SPEC_WEAPON_EQUIPMENT_RULES`：`hunter:survival` 允许 Crossbow / Dagger 等实际可装备组合，`rogue:outlaw` 允许 off-hand Dagger；所有 observed、recommended、legacy 模板仍继续经过同一 backend legality gate，hunter off-hand Shield 等真正非法组合仍会被降级。source-less observed、缺 hash、缺槽、`recommended_bis`、`season_recommendation`、`simc_preset` 和 default/baseline 模板仍不能进入公开完成态。已热部署并重跑 `wow-community-template-sync.service`、`wow-community-best-guard-sync.service`，两者 `Result=success / ExecMainStatus=0`。线上 smoke：`/health=200`；`/api/data/health` 中 `communityObserved expected=40 covered=40 publicReady=40 publicBlocked=0 partial=0`，`gear_legality_authority` 保持 `partial` 且 `blockedTemplateCount=0`；40 专精 `/api/websim/gear?compact=1&mode=initial` `withCommunity=40`、`emptyCommunitySpecs=[]`、`communitySourceKeys={'raiderio_observed_profile':40}`、`baselineSourceKeys={}`、`violationCount=0`。PG `cache.websim_community_gear_templates` 当前 `raiderio_observed_profile complete=40`，其中 `hunter:survival` 和 `rogue:outlaw` 均为 `complete/synced/readySlotCount=16/sampleCount=1/profileHash+gearHash=true`。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`, `docs/roadmap/ideas.md`.

> 2026-07-08 CST Elemental Shaman accepted decisions captured for all-spec rollout（已完成）: 已按用户要求回顾本轮元素萨装备模板链路中被明确认可的 OK 结论，并把“全职业推广前不能遗漏”的决策清单补入装备模拟 Runbook：当前公开入口只展示 `community_best_v2` 真实玩家装备；`recommended_bis_v1` 保留为内部 projected/optimizer 证据，重新上线前必须完成 SimC、pairwise、observed anchor validation 和用户验收；`season_recommendation` 只能 legacy fallback；active observed 必须有角色身份、sourceUrl、`sampleCount=1`、`profileHash/gearHash`、完整宝石/附魔/美化和后端 legality gate；当前 `听凭风引` 是元素萨固定试点锚点，不是日更 election job；后续全职业推广需新增候选 ledger、winner reason、rejected/blocker、stale winner、cleanup 防回流、非 DPS role objective、PG/API/health/templateChains/小程序 smoke 和回归矩阵。Evidence: `docs/gear-simulation-full-chain-runbook.md`, `docs/roadmap/ideas.md`.

> 2026-07-08 CST Elemental Shaman real-player template end-to-end contract docs（已完成）: 用户确认元素萨真实玩家装备模板的线上实现符合预期后，已把当前从后端到前端的公开导入合同补入装备模拟 Runbook：角色选举 v0 说明当前试点是固定锚点，不是自动日更 election job；候选从 Raider.IO `shaman/elemental` 专精榜和高层 M+ 记录发现，硬门槛包括角色身份/sourceUrl、`sampleCount=1`、`profileHash/gearHash`、16 槽、宝石/附魔/美化保留与 legality gate，通过后才可成为 active observed，且在 WCL/Archon/SimC replay 接入前只称“高分玩家样本”，不声称“DPS 最高”。PG active observed row 固定为 `observed_profile_shaman_elemental` / `听凭风引`；sync 后置 cleanup 负责防止 legacy baseline、重复 observed/recommended rows 和非目标 profile 回流；`public_gear_templates_for_spec()` 对 `shaman:elemental` 只允许 active `raiderio_observed_profile`，`public_baseline_fallback_templates_for_spec()` 允许公开 baseline 为空；PG/runtime API 预期 `communityTemplates=['raiderio_observed_profile']`、`baselineTemplates=[]`；前端 `gearCommunityTemplatesForPayload()` 仅消费并合并 `communityTemplates + baselineTemplates`，因此小程序“装备模板 / 导入 / 社区推荐”只展示真实玩家装备，`applyGearCommunityTemplate()` 只应用 `canApplyGear=true` 的后端合法模板并保留内嵌宝石/附魔/美化。系统评分推荐 `recommended_bis_v1` 保留为内部 evidence，未重新开放公开入口。Evidence: `docs/gear-simulation-full-chain-runbook.md`, `server/websim_payload.py`, `server/postgres_cache_store.py`, `pages/builds/detail.js`, `pages/builds/detail.wxml`.

> 2026-07-08 CST Elemental Shaman public import real-player-only rollback（已完成 / 已部署）: 根据线上小程序实际体验复核，元素萨导入社区入口暂时只保留 `community_best_v2` 真实玩家装备模板 `听凭风引（元素萨）· 真实高分玩家角色模板`；`recommended_bis_v1` / “系统评分推荐模板”虽然已有内部 PG evidence、enhancement pilot 和人工 SimC 对比，但用户侧满意度未达标，先从公开 `baselineTemplates` 页面入口移除，不再与真实玩家模板并列展示。后端公开过滤策略仍只作用于 `shaman:elemental`：`public_gear_templates_for_spec()` 对 pilot spec 只允许 active `raiderio_observed_profile`，并允许公开 baseline 为空，不再自动补 `baseline_blocked` 占位；PG/runtime initial payload 统一返回 `communityTemplates.sourceKey=['raiderio_observed_profile']`、`baselineTemplates=[]`。线上 no-download deploy 完成，两个 guard service 均 `Result=success / ExecMainStatus=0`；公网 smoke `/health=200`、元素萨 `/api/websim/gear?class=shaman&spec=elemental&compact=1&mode=initial` `dataStatus=verified`、`communityTemplates=[observed_profile_shaman_elemental]`、`baselineTemplates=[]`、`recommendedBis.totalSpecCount=0`、`legacyFallback.totalSpecCount=0`。PG 仍保留内部 `recommended_bis_shaman_elemental_179f9a4c75e89ebf` evidence（`templateStatus=projected_bis`、`simc.status=passed`、`pairwiseCompares=0`、`anchorValidation.status=pending`），后续单独优化系统评分推荐的候选、UI 标签、pairwise compare、observed anchor validation 和用户验收，通过后再重新开放入口。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-08 CST Elemental Shaman manual SimC evidence writeback（已完成 / 已部署）: 元素萨 `recommended_bis_v1` 已把上一轮人工认可的 10k SimC 对比写入正式 evidence overlay，但仍只作用于 `shaman:elemental`，不升级 `verified_bis`。新增 `cache.websim_sync_state.id=recommended_bis_v1_simc_evidence` 作为持久 overlay：prototype sync 重跑时会把 `manual_cloud_simc_compare` 合并到 matching spec 的 `templateEvidence.simc`，记录 SimC `1205-01`、`HecticAddCleave 5 targets`、`iterations=10000`、recommended `188418.99 DPS`、observed `187783.46 DPS`、`deltaPctVsObserved=+0.3384%`，并清理已完成的 `high-iteration SimC compare has not run` 与 enhancement SimC blocker。热部署后备份 `/opt/wow-mini-program/.codex-backups/20260708T054628Z-elemental-recommended-bis-simc-evidence-backup.json`，写入 sync state，重跑 `wow-recommended-bis-prototype-sync.service` 得到 `recommended_bis_shaman_elemental_179f9a4c75e89ebf` / `scanRunId=recommended-bis-prototype-20260708T054637Z`；PG row 仍为 `templateStatus=projected_bis`、`simc.status=passed`、`highIterationRuns=1`、`pairwiseCompares=0`、`anchorValidation.status=pending`、`enhancementOptimization.status=pilot_applied`。公网元素萨 gear API 仍只展示两条模板，`recommendedBis.simcReviewRequiredSpecCount=0`、`pairwiseRequiredSpecCount=1`、`anchorPendingSpecCount=1`、`verifiedSpecCount=0`、`fullOptimizerRunRequiredSpecCount=1`，blockers 只剩 `missing verified community talent anchor`、`pairwise gear compare has not run`、`observed anchor validation is pending`。边界：这只是把已认可的人工 SimC 结果写成可审计 evidence；天赋锚点仍是 stale，pairwise 和 anchor validation 未完成，所以不能展示为 `verified_bis`。Evidence: `server/postgres_cache_store.py`, `server/season_recommended_gear.py`, `tests/postgres_cache_store_test.py`, `tests/season_recommended_gear_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-08 CST Elemental Shaman recommended_bis enhancement pilot（已完成 / 已部署）: 元素萨 `recommended_bis_v1` prototype 已按“装备选择 + 宝石/附魔/美化一起进入候选组合”的方向推进一步，但仍只作用于 `shaman:elemental`。后端在 `build_recommended_bis_prototype_templates()` 里新增元素萨 enhancement pilot：以 active `community_best_v2` 真实角色模板 `听凭风引` 的可执行增强分布为锚点，同 itemId 推荐候选会继承缺失 `bonus/gem/enchant/embellishment`，不同 item 只复制同槽附魔；当评分推荐只保留 1 件美化而 observed anchor 有 2 件美化时，允许用 observed 的第二件美化槽替换评分推荐的非美化槽，并把 `enhancementOptimization.status=pilot_applied`、`copiedFields`、`slotReplacements`、anchor sourceUrl/profileHash/gearHash 写入 evidence。线上 PG 备份：`/opt/wow-mini-program/.codex-backups/20260708T052235Z-elemental-recommended-bis-enhancement-pilot-backup.json`；热部署后重跑 `wow-recommended-bis-prototype-sync.service` 得到 `recommended_bis_shaman_elemental_7f3418ed18692093` / `scanRunId=recommended-bis-prototype-20260708T052240Z`，rawString 已包含头部宝石/附魔、肩/胸/腿/戒指附魔、双 `arcanoweave_lining` 美化（披风 + 护腕）。公网元素萨 gear API 仍只展示两条模板：`raiderio_observed_profile` 与 `recommended_bis`；`recommendedBis` chain 为 `projectedSpecCount=1`、`verifiedSpecCount=0`、`fullOptimizerRunRequiredSpecCount=1`，blockers 保留 `high-iteration SimC compare has not run`、`pairwise gear compare has not run`、`observed anchor validation is pending`、`recommended_bis enhancement optimization still requires SimC validation`，并因当前 active verified community talent anchor 过期继续保留 `missing verified community talent anchor`。验证：本地 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `985 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `295 pass`，`py_compile` 与 `git diff --check` 通过；线上 SimC `1205-01`、`HecticAddCleave` 5 目标、`10000` iterations、同一套最新可用但已过期的 Sugarsm/Farseer 天赋行下，听凭风引真实装备 `187783.46 DPS`，增强后的系统推荐 `188418.99 DPS`，推荐高 `+635.54 DPS / +0.34%`。边界：这不是 full optimizer、pairwise compare、active verified talent anchor 修复或 `verified_bis`；结果只证明增强项纳入后系统推荐不再被缺宝石/附魔/美化拖低，仍必须保留 `projected_bis`。Evidence: `server/postgres_cache_store.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-08 CST Elemental Shaman real-player template hardening（已完成 / 已部署）: 元素萨装备模板试点正式收口为两条公开模板：`community_best_v2` 真实高分玩家角色模板 `听凭风引（元素萨）· 真实高分玩家角色模板`，以及 `recommended_bis_v1` `元素萨 · 系统评分推荐模板（待 SimC 验证）`。API read model 新增元素萨 pilot policy：`shaman:elemental` 的公开导入只保留 active `raiderio_observed_profile` 与 `recommended_bis`，不再展示 `season_recommendation`、`default_template`、`simc_preset` 或 source-less observed；PG `mode=initial` 分支也统一应用 legality gate。同步入口新增后置清理：community template、season recommendation、recommended_bis prototype 同步后都会调用 `cleanup_real_player_gear_template_pilot_residue()`，删除元素萨 legacy baseline、重复 observed/recommended_bis rows 和非 `听凭风引` profile 的元素萨 observed variants，并规范化两个保留 row 的名称/sourceName；cleanup 还能从 active observed row 回填缺失 observed variants，防止后续重建因 variant 表少槽而降级。`build_season_recommended_gear_templates()` 对元素萨直接跳过，防止定时任务重新生成 legacy baseline。线上 no-download deploy 完成；PG cleanup `manual-elemental-real-player-variant-backfill` 返回 `observedVariantRowsBackfilled=2`，最终 smoke `/health=200`、`/api/data/health overallStatus=partial`、元素萨 `/api/websim/gear ... mode=initial` `dataStatus=verified`，API `communityTemplates.sourceKey=['raiderio_observed_profile']`、`baselineTemplates.sourceKey=['recommended_bis']`、`legacyFallback.totalSpecCount=0`；PG 只剩 `raiderio_observed_profile=1 / recommended_bis=1`，observed row `sourceUrl=https://raider.io/characters/cn/sylvanas/听凭风引`、`sampleCount=1`、`profileHash/gearHash` 存在、`readySlotCount=16`，observed variants 为 16 槽且角色/URL 只指向 `听凭风引`；recommended row evidence 仍是 `projected_bis`、`simc.status=required`、`highIterationRuns=0`、`pairwiseCompares=0`、`anchorValidation.status=pending`、`verifiedSpecCount=0`、`fullOptimizerRunRequiredSpecCount=1`。新增回归覆盖元素萨公开模板只剩 observed + recommended_bis、真实角色模板命名、recommended 命名、season generator 不再产出元素萨、PG cleanup SQL/rename/backfill、`season_recommended_gear_sync` 写入 cleanup evidence。边界：这只作用于 `shaman:elemental`，不降低 `projected_bis` 的 SimC/pairwise/anchor 门禁，也不把 `season_recommendation` 扩展性删除到全职业。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `server/postgres_cache_sync.py`, `server/season_recommended_gear.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `tests/postgres_cache_sync_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST gear_legality_authority health/admin audit output（已完成 / 已部署）: `/api/data/health` 已新增独立 `gear_legality_authority` component，后台治理总览也显示为“装备合法性权威层”。组件输出 `schemaRevision=gear-legality-authority-health-v1`、`ruleVersion`、`ruleSource`、`sourceAuthorityStatus`、`totalSpecs`、`verifiedSpecs`、`manualOverrideSpecs`、`blockedTemplateCount`、`warningTemplateCount`、`excludedCandidateCount`、`examples` 与 blockers；当前规则源仍是 `manual_override`，所以 health 明确为 `partial`，`verifiedSpecs=0`、`manualOverrideSpecs=40`，不把 manual override 包装成 verified authority。illegal promoted template 会让组件 `blocked`，warning-only template 只有超过阈值才降为 partial。新增红灯覆盖 PG-only health 不走 SQLite 也必须包含组件、helper 必须披露 manual override 且统计 illegal template/candidate audit。本地验证：`python3 -m unittest tests.news_backend_test tests.websim_payload_test -q` 为 `597 tests OK`；最终全量 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `970 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `294 pass`，`python3 -m py_compile server/news_backend.py server/postgres_cache_store.py server/websim_payload.py server/gear_legality.py` 与 `git diff --check` 通过。已用 no-download hot deploy 部署到 `http://124.223.51.33`，触发 `wow-community-best-guard-sync.service` 与 `wow-recommended-bis-guard-sync.service` 均 `Result=success / ExecMainStatus=0`；线上 smoke `/health=200`、`/api/data/health=200 overallStatus=partial`、`/api/data/health?audit=1=200` 且同样包含 `gear_legality_authority`，线上 component 为 `status=partial ruleSource=manual_override totalSpecs=40 verifiedSpecs=0 manualOverrideSpecs=40 blockedTemplateCount=0 warningTemplateCount=0 excludedCandidateCount=0`。远端 admin summary 通过 systemd `EnvironmentFile=/etc/wow-backend.env` 验证 `hasModule=true title=装备合法性权威层 status=partial statusLabel=部分通过`。40 专精 initial 巡检继续 `failureCount=0 catalogStatus.partial=40 dataStatus.verified=40`，代表样本元素萨、奥法、防骑、神牧、增辉均无 `gearLegalityBlockers`。边界：这只是 health/admin/audit 输出面，不是完整 Battle.net/SimC source map、自动生成 40 专精规则、专门 admin drilldown 页面、full optimizer 或 verified BiS。Evidence: `server/news_backend.py`, `server/websim_payload.py`, `tests/news_backend_test.py`, `tests/websim_payload_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST slot detail enhancement legality cleanup（已完成 / 已部署）: 装备强化配置面板的前端合同已补齐：`mode=initial` 继续保持轻量，`openGearEnhancementSheet()` 只对已选且缺少完整宝石/附魔/美化 options 的槽位按需请求 `mode=slot` detail；前端不维护武器/附魔/美化规则推断，只消费后端返回的 `equippedSet`、`slotReadiness` 与 `gearLegalityBlockers`。当 slot detail 标记当前同槽同 itemId 的已选装备为 `legalityStatus=blocked` 时，前端会清理该槽选择、同步删除过期 `enhancementBySlot` 与 sheet draft，并把后端 blocker 展示在 slot row / enhancement sheet；`buildGearEnhancementSheet()` 仍只负责草稿编辑边界，提交仍走 `confirmGearEnhancementSheet()`。新增红灯覆盖 `gear enhancement sheet clears stale enhancement when slot detail marks selected gear illegal`；本地最终验证继承本阶段代码完成后的全量结果：`python3 -m unittest discover -s tests -p '*_test.py'` 为 `969 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `294 pass`，`python3 -m py_compile server/news_backend.py server/postgres_cache_store.py server/websim_payload.py server/gear_legality.py` 与 `git diff --check` 通过；本轮部署前复核 `node --test tests/builds-page.test.js --test-name-pattern "gear enhancement sheet clears stale enhancement"` 返回 `111 pass`，`git diff --check` 通过。已用 no-download hot deploy 部署到 `http://124.223.51.33`，触发 `wow-community-best-guard-sync.service` 与 `wow-recommended-bis-guard-sync.service` 均 `Result=success / ExecMainStatus=0`；线上 smoke `/health=200`、`/api/data/health overallStatus=partial`，元素萨 initial `heavyCandidateLeakCount=0`，`main_hand/off_hand/neck/finger1/legs` slot detail 均 `http=200`、`gearPayloadMode=slot`、`detailMode=complete`，40 专精 initial 巡检 `failureCount=0 catalogStatus.partial=40 dataStatus.verified=40`，模板状态仍为 `communityTemplateStatus complete=22 / partial=18`、`baselineTemplateStatus complete=66`。边界：这仍不是 `gear_legality_authority` health/admin audit、authority source map、full optimizer、pairwise compare 或 verified BiS；DPS 26 个仍是 projected/待 SimC optimizer，坦克、治疗、增辉仍不能按 DPS BiS 口径包装。Evidence: `pages/builds/detail.js`, `tests/builds-page.test.js`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST gear template import/save legality gate（已完成 / 已部署）: 装备模板导入、保存与 PG read model 模板出口已接入同一后端合法性门禁。SimC template 的 structured/JSON `gearBySlot` 解析现在会保留合法 partial items，但把非法槽写入 `templateValidation.errors`，最终阻断 profile 生成与提交；元素萨布甲头红灯线上确认返回 `template_blocked`、`canSubmitTask=false`、`simcItems=15`、`hasHeadItem=false`，同时包含 `missing gear slots: head` 与 `head gear incompatible with shaman/elemental armor rule: Cloth` blocker。用户保存的 build template 列表也通过后端 `simcraftReadiness` 暴露相同门禁，非法 structured snapshot 不再被标成完整可执行。PG `PostgresCacheStore.get_websim_gear()` 对 `communityTemplates` 和 `baselineTemplates` 应用 `apply_gear_template_legality_gate()`，让 direct read model 与 runtime wrapper 口径一致；`prepare_simcraft_template_request()` 在 `WOW_DATABASE_RUNTIME=postgres_only` 下不再进入 SQLite `db_connection()`，PG-only 生产 `/api/simulator/analyze` 对非法模板返回受控 blocked payload 而不是 502。新增覆盖包括 structured snapshot partial-preserve、PG baseline template gate、user-saved readiness block、PG-only confirm degrade；本地最终验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `969 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `293 pass`，`python3 -m py_compile server/news_backend.py server/postgres_cache_store.py server/websim_payload.py server/gear_legality.py` 与 `git diff --check` 通过。已用 no-download hot deploy 部署到 `http://124.223.51.33`，触发 `wow-community-best-guard-sync.service` 与 `wow-recommended-bis-guard-sync.service` 均 `Result=success / ExecMainStatus=0`；线上 smoke `/health=200`、`/api/data/health overallStatus=partial`，40 专精 compact 巡检 `failureCount=0 catalogStatus.partial=40`，`communityObserved observed_simc_replay_required=27 / observed_blocked=13`，`recommendedBis recommended_projected=26 / recommended_role_objective_blocked=14`，`legacyFallback=40`。边界：这仍不是完整 authority source spike、`gear_legality_authority` health/admin audit、slot detail enhancement contract 或 full optimizer/verified BiS；DPS 26 个仍需 full optimizer/SimC/pairwise/anchor validation，14 个非 DPS 仍需 role objective。Evidence: `server/news_backend.py`, `server/postgres_cache_store.py`, `server/websim_payload.py`, `tests/news_backend_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST gear candidate legality audit for replacementCandidates（已完成 / 已部署）: `replacementCandidates` 候选库已接入后端 `gear_legality` evaluator：SQLite 与 PG read model 在分组候选前按请求职业/专精/slot 调用 `apply_gear_candidate_legality()`，非法 armor/weapon 不进入候选池；full payload 新增 `candidateLegalityAudit.schemaRevision=gear-candidate-legality-audit-v1`、`excludedCandidateCount` 与去重 `excludedExamples`，候选对象带 `legalityStatus`、`legalityReasons`、`sourceTrust`，而 compact payload 继续通过 `compact_gear_candidate()` 保持玩家侧小字段集，不输出 `legalityReasons/sourceTrust`。新增红灯覆盖：元素萨 preset 中布甲头被排除并进入 audit，合法项链保留 `legalityStatus=legal/sourceTrust=simc_preset`；PG full payload 输出 debug 字段，PG compact payload 不带 debug 字段。本地最终验证：`python3 -m unittest discover -s tests -p '*_test.py'` 为 `965 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `293 pass`，`python3 -m py_compile server/gear_legality.py server/websim_payload.py server/postgres_cache_store.py` 与 `git diff --check` 均通过；本地 CR 未发现 blocker。已用 no-download hot deploy 部署到 `http://124.223.51.33`，触发 `wow-community-best-guard-sync.service` 与 `wow-recommended-bis-guard-sync.service` 均 `status=0/SUCCESS`。线上 smoke：`/health=200`、`/api/data/health overallStatus=partial`；元素萨 full gear payload 有 `candidateLegalityAudit`，compact payload 16 个 slot group 且无 debug keys；40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `checked=40 failureCount=0 catalogStatus.partial=40`，`recommendedStateCounts.projected=26 / role_objective_blocked=14`。当前覆盖数据：`communityImportTemplates=80/80`，`communityObservedGuard expected=40 covered=27 provisional=27 blocked=13 missing=0 simcReplayRequired=27 verified=0`，`recommendedBisGuard expected=40 projected=26 blocked=14 optimizerRequired=26 fullOptimizerRunRequired=26 verified=0`，`recommendedBisPrototype scanRunId=recommended-bis-prototype-20260707T135759Z projected=26 verified=0`。边界：这仍不是 authority source spike、自动生成 40 专精规则、`gear_legality_authority` health component、template save 全字段门禁或 full optimizer；非 DPS 的 `role_objective_blocked` 和 DPS 的 `projected_bis` 仍按 fail-closed 展示。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST gear legality authority evaluator extraction（已完成 / 已部署）: 装备合法性门禁从 `websim_payload.py` 内的散落判断向后端权威层推进了一步：新增 `server/gear_legality.py`，提供 `gear_legality_for_item()` / `gear_legality_for_template()`，统一返回 `status`、`reasons`、`blockedSlots`、`warningSlots`、`ruleVersion=2026-07-07-live-manual-overrides` 和 `ruleSource=manual_override`。现阶段仍复用既有 manual override 的 class/spec weapon rule 与 armor type rule，不引入 Battle.net/SimC 自动生成器、不下载外部数据；`apply_gear_template_legality_gate()`、保存/模拟前的 selected gear blocker 已切到 evaluator 结果，再转换成旧 blocker 文案，保持前端 partial import 修复逻辑兼容。新增红灯覆盖元素萨拳套主手允许、增强萨盾牌副手阻断、法系双手主手占用副手、锁甲专精布甲槽位阻断；并新增 websim gate 回归，错误 armor 会降级为 partial 并跳过非法槽。本地验证 `python3 -m unittest discover -s tests -p '*_test.py'` 为 `963 tests OK (skipped=1)`，`node --test tests/*.test.js` 为 `293 pass`，`py_compile server/gear_legality.py server/websim_payload.py` 与 `git diff --check` 均通过。已用 no-download hot deploy 部署到 `http://124.223.51.33`，触发 `wow-community-best-guard-sync.service` 与 `wow-recommended-bis-guard-sync.service` 均 `status=0/SUCCESS`；远端只读 smoke 证明 `shaman/elemental` 布甲头为 `blocked`、拳套主手为 `legal`、`Staff + Held In Off-hand` 阻断副手；公网 `/health=200`、`/api/data/health=200`，40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `failureCount=0 dataStatus.verified=40 communityTemplateCounts.1=40 baselineTemplateCounts.2=26 baselineTemplateCounts.1=14`，元素萨 `weaponRule.mainHandTypes` 仍包含 `Fist Weapon / One-Handed Axe`。边界：这不是完整 authority source spike、自动规则生成、candidate library 全量 sourceTrust 标注或 `/api/data/health` 的 `gear_legality_authority` 组件；后续还需按计划推进 source map、candidate library 应用、import/save 全门禁和 health/admin audit。Evidence: `server/gear_legality.py`, `server/websim_payload.py`, `tests/gear_legality_test.py`, `tests/websim_payload_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST recommended_bis_v1 projected DPS prototype sync（已完成 / 已部署）: `recommended_bis_v1` 现在已有独立 PG-only projected prototype 同步入口：新增 `server/recommended_bis_prototype_sync.py` 与 `wow-recommended-bis-prototype-sync.service`，只消费当前 PG read model 的真实社区装备与装备 catalog 候选池，不下载外部数据、不执行 SimC、不触发 full optimizer。DPS 26 个专精都会写入 `sourceKey=recommended_bis`、`status=projected_bis`、`optimizerVersion=gear-bis-optimizer-v1`、`candidatePool/statPriorPolicy/simc/anchorValidation/blockers` evidence；`season_recommendation` 保持 legacy fallback，不再被 recommended baseline 去重挤掉。线上最终刷新 `scanRunId=recommended-bis-prototype-20260707T135759Z`，`recommendedBisPrototype projected=26 missingDps=0 verified=0 fullOptimizerRunRequired=26`；`recommendedBisGuard status=partial expected=40 total=26 missing=14 blocked=14 optimizerRequired=26 anchorFailed=0`；40 专精公网巡检 `failureCount=0 dataStatus.verified=40 recommendedStatuses.projected_bis=26 role_objective_blocked=14 observedStatuses.observed_provisional=16 observed_partial=11 observed_blocked=13 baselineTemplateCounts.2=26 baselineTemplateCounts.1=14`；aggregate health 保持 `legacyFallback starterBaseline=40`、`seasonRecommendation complete=40 provisional=40 verified=0`。边界：这仍不是 verified BiS、candidate_bis、pairwise compare、high-iteration SimC 或 role-specific tank/healer/support optimizer；26 个 DPS 全部还需要 full optimizer/SimC/pairwise/anchor validation，14 个非 DPS 明确为 `role_objective_blocked`。Evidence: `server/season_recommended_gear.py`, `server/postgres_cache_store.py`, `server/postgres_cache_sync.py`, `server/recommended_bis_prototype_sync.py`, `server/wow-recommended-bis-prototype-sync.service`, `server/websim_payload.py`, `tests/postgres_cache_store_test.py`, `tests/postgres_cache_sync_test.py`, `tests/websim_payload_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST recommended_bis_v1 observed-anchor validation gate（已完成 / 已部署）: `recommended_bis_v1` 在真正产出 optimizer winner 前已补上入口安全门禁：任何 `verified_bis` 必须带 `anchorValidation.status=passed`，且若 `winnerDps` 相对 `bestObservedDps` 低于 `blockThresholdPct`（默认 2%）会被强制降级为 `anchor_failed`，进入 `blockedExamples`、`anchorFailedSpecCount`，并计入 `fullOptimizerRunRequiredSpecCount`，不能继续作为 verified 推荐展示。已用 Mandur/元素萨回归形态写入红绿测试：`winnerDps=211177`、observed anchor `237956`、`deltaPctVsBestObserved=-12.68` 会从自称 `verified_bis` 降为 `anchor_failed`；缺 passed anchor 的 `verified_bis` 也会 fail-closed。已热部署到云服务器并刷新 `wow-recommended-bis-guard-sync.service`；远端只读 synthetic smoke 返回 `anchorFailedSpecCount=1`、`verifiedSpecCount=0`、`fullOptimizerRunRequiredSpecCount=1`。真实线上当前仍无 recommended winner，`/api/data/health` 保持 `overallStatus=partial`，`recommendedBisGuard status=blocked expected=40 missing=40 optimizerRequired=40 fullOptimizerRunRequired=40 anchorFailed=0`；40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `httpOk=40 failures=0 dataStatus.verified=40 baseline=40 observedCovered=27 recommendedStatuses.optimizer_blocked=40`。边界：这不是 full optimizer、candidate search、pairwise compare 或 verified BiS 产出；它只确保未来 optimizer 输出不能绕过 observed anchor 反证。Evidence: `server/websim_payload.py`, `tests/websim_payload_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST observed profile SimC replay evidence bridge（已完成 / 已部署）: `community_best_v2` observed 链路已接入“角色整套 SimC replay 证据”桥接，但仍按 fail-closed 规则阻止未满足条件的模板升级。`gear_observed_backfill` 现在会从角色 profile SimC JSON 提取 `observedProfileSimcReplay` 并写入 PG variant；`websim_payload` 在 observed community template 中保留 `templateEvidence/profileHash/gearHash/sampleCount`，只有同一角色、16 槽全量 replay、且每槽 replay summary 一致时才允许写 `scenarioResults`。已热部署并触发线上 observed backfill、PG-only community template rebuild 与两个 guard：observed variants `10744` 条中 `47` 条已有 `observedProfileSimcReplay`；observed community templates `40` 条完整，其中 `3` 条已带 `templateEvidence`，但 `scenarioResults=0`，因为尚无 16 槽模板满足全槽 replay 一致升格条件。线上 `/api/data/health` 仍为真实 `overallStatus=partial`；`communityImportTemplates=80/80`、`realCommunityGearTemplates=40/40`，`communityObservedGuard expected=40 covered=27 provisional=27 blocked=13 verified=0 simcReplayRequired=27`，`recommendedBisGuard` 仍为 `blocked expected=40 optimizerRequired=40`。40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `httpOk=40 failure=0 dataStatus.verified=40 baseline=40 observedCovered=27`，observed 单页状态为 `observed_provisional=16 / observed_partial=11`，recommended_bis 全部 `optimizer_blocked`。边界：这次完成的是 replay evidence 写入和模板传播，不是 full `community_best_v2` winner ledger、全专精 SimC replay 升格，也不是 `recommended_bis_v1` optimizer。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST community_best_v2 persisted observed guard control plane（已完成 / 已部署）: `community_best_v2` 从即时 `templateChains.communityObserved` 继续推进到可持久、可定时、可健康页审计的 observed guard 控制面。新增 PG-only 脚本 `server/community_best_guard_sync.py`、sync state key `community_best_v2_guard`、`wow-community-best-guard-sync.service/timer`，deploy 会安装并启用 daily timer；该 guard 只读取当前 PG read model / health summary 并写 `cache.websim_sync_state`，不下载外部数据、不抓角色、不执行 SimC replay、不替换 winner。已热部署并触发线上 `wow-community-template-sync.service`、`wow-community-best-guard-sync.service` 和 `wow-recommended-bis-guard-sync.service`；community template refresh `status=0/SUCCESS`，observed guard service `status=0/SUCCESS` 且 timer `active`。公网 `/api/data/health` 的 `community_templates.details.communityObservedGuard` 显示 `schemaRevision=community-best-v2-guard-state-v1`、`status=partial`、`guardMode=readiness_only`、`expected=40 covered=26 verified=0 provisional=26 partial=0 blocked=14 missing=0 simcReplayRequired=26`、`errors=[]`、`sourceScanRunId=pg-community-template-2026-07-07T124822z0000`、`lastGuardCheckAt=2026-07-07T12:50:06+00:00`；同一 health 中 `recommendedBisGuard` 仍为 `status=blocked expected=40 missing=40 optimizerRequired=40 fullOptimizerRunRequired=40`。40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `checkedSpecs=40 failureCount=0`，每个 spec 仍有 1 个 community template 和 1 个 baseline template；observed 状态分布 `observed_provisional=16 / observed_partial=10 / observed_blocked=14`，recommended_bis 全部 `optimizer_blocked`，legacy fallback 全 40 保留。边界：这仍不是 full `community_best_v2` ledger、SimC replay、winner history diff、stale winner 保守保留或 `recommended_bis_v1` optimizer；它只是为后续 observed replay / optimizer 提供 fail-closed 日更守卫和健康审计面。Evidence: `server/postgres_cache_sync.py`, `server/community_best_guard_sync.py`, `server/wow-community-best-guard-sync.service`, `server/wow-community-best-guard-sync.timer`, `server/deploy_lighthouse.sh`, `server/news_backend.py`, `server/postgres_cache_store.py`, `server/websim_payload.py`, `tests/postgres_cache_sync_test.py`, `tests/postgres_only_scripts_test.py`, `tests/deploy-script.test.js`, `tests/news_backend_test.py`, `tests/postgres_cache_store_test.py`, `tests/websim_payload_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST recommended_bis_v1 persisted guard control plane（已完成 / 已部署）: `recommended_bis_v1` 从上一轮即时 readiness 口径继续推进到可持久、可定时、可健康页审计的 guard 控制面。新增 PG-only 脚本 `server/recommended_bis_guard_sync.py`、sync state key `recommended_bis_v1_guard`、`wow-recommended-bis-guard-sync.service/timer`，deploy 会安装并启用 daily timer；该 guard 只读取当前 PG read model / health summary 并写 `cache.websim_sync_state`，不下载外部数据、不执行 SimC、不入队 full optimizer。已热部署并手动触发线上 `wow-recommended-bis-guard-sync.service`，service `status=0/SUCCESS`，公网 `/api/data/health` 的 `community_templates.details.recommendedBisGuard` 显示 `schemaRevision=recommended-bis-v1-guard-state-v1`、`status=blocked`、`guardMode=readiness_only`、`expected=40 total=0 missing=40 blocked=40 optimizerRequired=40 fullOptimizerRunRequired=40`、`errors=[]`、`lastGuardCheckAt=2026-07-07T12:34:37+00:00`；40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `checkedSpecs=40 failureCount=0`，每个 spec 仍有 1 个 community template 和 1 个 baseline template，per-payload `recommendedExpected=1 / fullOptimizerRunRequired=1 / guardMode=readiness_only`。边界：这仍不是 full `recommended_bis_v1` optimizer、candidate ledger、SimC replay 或 anchor validation；它只是为后续 full optimizer 提供 fail-closed 日更守卫和健康审计面。Evidence: `server/postgres_cache_sync.py`, `server/recommended_bis_guard_sync.py`, `server/wow-recommended-bis-guard-sync.service`, `server/wow-recommended-bis-guard-sync.timer`, `server/deploy_lighthouse.sh`, `server/news_backend.py`, `server/postgres_cache_store.py`, `tests/postgres_cache_sync_test.py`, `tests/postgres_only_scripts_test.py`, `tests/deploy-script.test.js`, `tests/news_backend_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST recommended_bis_v1 readiness gate（已完成 / 已部署）: 双链路 health/API 现在不再只把 `recommendedBis total=0` 当作空状态，而是按 expected spec 显式暴露毕业推荐链路缺口。`communityTemplateSync.templateChains.recommendedBis` 新增 `guardMode=readiness_only`、`expectedSpecCount`、`missingSpecCount`、`optimizerRequiredSpecCount`、`fullOptimizerRunRequiredSpecCount`、`blockedExamples` 和 `guardPolicy`；当某个 spec 没有 `recommended_bis_v1` optimizer winner 时，会以 `optimizer_blocked` blocker 报告“optimizer has not produced a candidate”，但不会执行或入队 full SimC optimizer。已热部署到 `http://124.223.51.33`；公网 `/api/data/health` 显示 `recommendedBis expected=40 total=0 missing=40 blocked=40 optimizerRequired=40 fullOptimizerRunRequired=40 guardMode=readiness_only`，同时 `legacyFallback starterBaseline=40`、`communityObserved covered=26 blocked=14`、`communityImportTemplates=80/80`。40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `checkedSpecs=40 failureCount=0`，每个 spec 的单页 payload 都有 `recommendedExpected=1 / fullOptimizerRunRequired=1 / guardMode=readiness_only`，并保持 `communityTemplates=1`、`baselineTemplates=1`、`dataStatus=verified`。边界：这仍不是 `recommended_bis_v1` optimizer/service，不能展示为 `projected_bis`、`candidate_bis` 或 `verified_bis`。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST gear template dual-chain read-model gate（已完成 / 已部署）: 装备模板出口已新增 `communityTemplateSync.templateChains`，把 `communityObserved`、`recommendedBis`、`legacyFallback` 分开上报。真实 observed 模板必须有具体角色 `sourceUrl`、`sampleCount > 0` 和可复现 `profileHash/gearHash`；缺任一证据即 fail-closed 到 `observed_blocked`。`season_recommendation` 已在该口径中降级为 `legacyFallback.starter_baseline`，不计入 `recommendedBis` 或 `verified_bis`。本轮还修复 observed variant -> community template -> PG row hydration -> compact API 的 evidence 保留链路，避免 `/api/data/health` 与 `/api/websim/gear` 口径不一致。已热部署到 `http://124.223.51.33`，并用 PG-only 模板重建 `scanRunId=pg-community-template-source-evidence-20260707T121521Z` 刷新线上读模型；公网 `/api/data/health` 显示 `communityObserved covered=26 verified=0 partial=0 blocked=14`、`recommendedBis total=0`、`legacyFallback starterBaseline=40`、`communityImportTemplates=80/80 missing=0`、`seasonRecommendation complete=40 provisional=40 verified=0`。40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检 `checkedSpecs=40 failureCount=0`，每个专精都有 1 个 community template 和 1 个 baseline template；compact API confidence 分布为 `observed_provisional=16 / observed_partial=10 / observed_blocked=14`，与 health covered 口径一致。当前仓库没有独立 `recommended_bis_v1` guard/service，`recommendedBis=0` 是真实状态，不包装成已完成 optimizer。Evidence: `server/websim_payload.py`, `server/postgres_cache_store.py`, `server/news_backend.py`, `tests/websim_payload_test.py`, `tests/postgres_cache_store_test.py`, `tests/news_backend_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST Elemental Shaman recommendation stat-priority correction（已完成 / 已部署）: 以真实高分元素萨样本复核后确认当前 `season_recommendation` 的元素萨默认权重方向错误，旧 baseline 副属性堆成急速优先，和外部高分样本/聚合的 `精通 > 暴击 > 急速 > 全能` 方向不一致。本次修正 `season-rec-score-v1`：元素萨默认权重改为 `mastery > crit > haste > versatility`；PG stat weight cache 只有 `verified/partial/stale/synced` 才能参与推荐，`blocked` 权重只写 evidence 不参与评分；`mplus_aoe` 会优先读取 `mplus_aoe_pack` 权重、再回退 `mplus_mixed_route`；带低收益属性的第一名若只在 2% 灰区领先，会先回退到无低收益替代并写入 `lowYieldGrayZoneFallback` 与 `simcReview.required`，不再把灰区全能装备直接放进基线。已热部署并重跑 `wow-community-template-sync.service` 与 `wow-season-recommended-gear-sync.service`，最新 baseline `scanRunId=season-recommended-gear-20260707T095101Z`；线上元素萨 baseline 仍为 `provisional`，但副属性轮廓已从旧的 `crit=430/haste=1041/mastery=758/vers=68` 改为 `crit=629/haste=738/mastery=862/vers=68`，戒指 2 改为 `白金星辰指环`。当前元素萨 stat weight cache 仍 `blocked`，原因是 CN Raider.IO 没有该专精代表样本、无 SimC-ready profile，所以本轮使用 fail-closed 专精默认权重而非伪造 SimC 权重；后续需要补 Raider.IO/WCL 样本或支持 stat weights targeted refresh。线上 smoke：`/health=200`、`communityImportTemplates=80/80 missing=0`、`seasonRecommendation complete=40 provisional=40 verified=0`、40 专精 initial gear 巡检 `checked=40 failure=0 baselineComplete=40 community=40 sameSignature=0`。Evidence: `server/season_recommended_gear.py`, `server/postgres_cache_store.py`, `tests/season_recommended_gear_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST season recommendation scoring objective and SimC-review gate（已完成 / 已部署）: `season_recommendation` 不再把真实社区装备 winner 直接当作推荐最优，而是在后端用 `season-rec-score-v1` 对社区样本与当前装备 read model 候选池做规则评分，证据记录装等、主属性、绿字权重、低收益属性惩罚、替代候选、套装 2/4 与 5 件散件替代、武器/饰品/特殊效果裁决、等价戒指/饰品去重、双手主手占用副手、`simcReview` 和最终 confidence。DPS/可验证模板只有在 `simcReview.status=passed` 后才允许从 `provisional` 升为 `verified`，否则保持 fail-closed；元素萨低全能收益已纳入专精权重强惩罚，线上元素萨 baseline `candidateCount=143`、`tierSetDecision.selectedTierCount=4`、腿部 5 件套低收益部位被散件替代评估，且因低收益灰区、武器/饰品/套装关键冲突 `simcReview.status=required`。部署复核发现三类系统性退化并已修复：PG catalog metadata trust 字段只在 payload 内导致候选池被误滤；生成器只消费 complete 社区样本导致 legality gate partial 后不重建 baseline；戒指/饰品重复同 itemId、双手主手+副手组合未在评分器后处理导致模板构建失败或 API 降级。线上已触发 `wow-community-template-sync.service`（`pg-community-template-2026-07-07T082004z0000`）和 `wow-season-recommended-gear-sync.service`（最终 `season-recommended-gear-20260707T091748Z`）；公网 `/health=200`、`/api/data/health=200`，`realCommunity=40/40`、`seasonRecommendation=40/40`、`communityImportTemplates=80/80 missing=0`。40 专精 `/api/websim/gear?compact=1&mode=initial` 巡检：每个 spec 都有 1 个真实社区模板和 1 个 baseline，baseline API 状态 `complete=40`，community observed API 状态 `complete=22 / partial=18`（真实样本非法武器被 gate 降级），baseline 与 community 无完全相同专精，差异槽位最少 6、最多 14。当前 confidence 分布为 `verified=0 / provisional=40`，因为 SimC 复核尚未实际通过；不能包装成 BiS 或 verified。Evidence: `server/season_recommended_gear.py`, `server/postgres_cache_store.py`, `server/websim_payload.py`, `tests/season_recommended_gear_test.py`, `tests/postgres_cache_store_test.py`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-07 CST Elemental Shaman gear legality Phase 0 hotfix（已完成 / 已部署）: 装备模拟已修正元素萨主手 weapon rule 的临时硬编码缺口，`weaponRule.mainHandTypes` 现在包含 `Dagger / Fist Weapon / One-Handed Axe / One-Handed Mace / Staff`，不再把单手斧和拳套过滤掉。强化配置面板在打开前会对已选且声明支持宝石/附魔/美化、但 initial/import payload 缺少具体 options 的槽位补拉 `mode=slot` detail，避免项链、戒指、腿和副手等槽位的配置选项缺失。同时新增运行时模板合法性 gate：PG/SQLite 读模型返回的社区模板和 `season_recommendation` baseline 模板如果违反当前职业专精武器规则，会在 API 出口跳过非法槽位并降级为 `partial/canApplyGear=true`，保留合法槽位可导入；只有没有任何合法槽位可应用时才 `blocked/canApplyGear=false`。线上元素萨当前 Raider.IO observed 与 season recommendation 模板的 `Two-Handed Mace + Held In Off-hand` 会被跳过并写入 blocker，避免小程序继续套用非法武器，同时不阻断其余合法槽位；前端导入 partial 模板时会对被跳过的武器槽补拉 `mode=slot` detail，并用合法 SimC-ready 候选自动补齐主手/副手组合，双手主手会保持副手占用而不套非法副手。已部署到 `http://124.223.51.33`，本地验证 `python3 -m unittest tests.news_backend_test`、`python3 -m unittest tests.websim_payload_test`、`node --test tests/builds-page.test.js`、`python3 -m py_compile server/news_backend.py server/websim_payload.py`、`git diff --check`；线上 smoke 验证 `/health` ok，元素萨 initial payload 返回新 weaponRule，`main_hand` slot 候选类型仅为合法集合且不含 `237849`，`neck/finger1/legs/off_hand` slot detail 均返回对应 socket/enchant/embellishment options。后续全量治理继续按 [Gear Legality Authority Implementation Plan](plans/2026-07-07-gear-legality-authority-plan.md) 的 authority source spike 推进。

> 2026-07-07 CST data health follow-up, SimC runtime auto-update, and cloud egress proxy hardening（已完成 / 已部署）: 线上日常更新控制面已从人工日报扩展为可自动续跑的 `wow-data-health-followup.timer`。该 timer 每 2 小时读取 `/api/data/health`，可安全触发新闻 queue refresh、装备 observed backfill、WebSim sync、stat weights sync，以及 SimC runtime update；当 `template_simc_bridge` 或 `season_cutover_readiness` 报告配置好的 SimC runtime `updateAvailable=true` 时，`wow-simc-runtime-update.service` 会通过锁串行下载配置源、构建、原子切换 `/opt/wow-simc/current` 并刷新 version state。日常新闻 refresh 改为 `scope=queue&limit=1`，只处理 backlog，超过 `WOW_NEWS_RETRY_MAX_ATTEMPTS` 的翻译失败会转为 blocked/report。云上所有会访问海外源的生产入口已接入本机 `mihomo.service` 代理：backend/Codex/LLM、WebSim sync、community template sync、gear observed backfill、stat weights、SimC version check/runtime update；只访问本机 health 的 follow-up 和只消费 PG read model 的 `season_recommendation` 不需要代理。历史旧 `wow-news-backend.service` 已在云上 mask，避免误启未代理旧后端。Evidence: `server/data_health_followup.py`, `server/simc_runtime_update.sh`, `server/wow-data-health-followup.*`, `server/wow-simc-runtime-update.service`, `server/deploy_lighthouse.sh`, `server/refresh_cron.sh`, `server/wow-*.service`, `tests/data_health_followup_test.py`, `tests/deploy-script.test.js`, `docs/remote-debugging.md`, `docs/news-architecture.md`, `docs/gear-simulation-full-chain-runbook.md`.

> 2026-07-06 CST Chickenbro v0.2 playerized evidence coach（已完成 / 待线上验收）: 炸鸡队长已按“老玩家陪练 / 专业教练 / 证据档”三层落地。后端新增 `answerLayer`、`basisLabel`、`missingInputs`、`nextQuestion`，缺 published profile 时默认直聊，只有职业/专精/场景时进入诊断 checklist，有 published profile 或显式 SimC/WCL context evidence 时才进入证据档；Codex schema、owner isolation、scope refusal、allowedNumbers、evidenceRefs 和 deterministic fallback 继续 fail-closed。运行时 evidence packet 只携带 compact profile、保存模板摘要、SimC/WCL task 摘要、allowedNumbers 和 evidenceRefs，不透传 raw SimC profile、stdout、raw log、token 或完整装备 rawString。前端智能分析 tab 与独立炸鸡队长页现在展示回答层级、依据标签、下一问、缺口、行动、证据引用和限制。Evidence: `server/news_backend.py`, `pages/simulator/chickenbro-chat.js`, `pages/simulator/chickenbro.wxml`, `pages/simulator/simulator.wxml`, `tests/news_backend_test.py`, `tests/simulator-page.test.js`.
