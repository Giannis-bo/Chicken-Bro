# Roadmap Ideas

这里只保留尚未进入正式里程碑且仍有决策价值的想法。已完成事项和执行流水不在这里归档。

## 维护规则

- 用户认可且可执行的方向进入 `docs/roadmap.md`。
- 已废止、已完成或被当前计划替代的条目直接删除。
- 每条候选只记录问题、价值、边界、建议里程碑和状态。
- 状态使用：`待补录`、`待确认`、`候选`、`待规划`、`后续`、`暂缓`。

## 当前候选

| 想法 | 问题与价值 | 主要风险 / 边界 | 建议里程碑 | 状态 |
| --- | --- | --- | --- | --- |
| 坦克、治疗与增辉目标函数 | 为非纯 DPS 职责建立独立评分与模拟目标 | 需要职责特有证据，不能复用输出职业评分 | 模板化构筑 / SimC | 待规划 |
| 项目对外定位 | 明确资讯、构筑模拟和玩家分析之间的主叙事 | 影响导航、文案和优先级，宜在 14 路由稳定后决策 | 产品定位 | 待确认 |
| 玩家反馈与需求入口 | 形成线上问题、数据缺口和诉求的可追踪入口 | 需要隐私、反滥用、去重和 owner 机制 | 发布运营 | 候选 |
| 定时任务分域 | 拆分同步、证据检索、审计和入库任务 | 需统一 revision、锁、重试和可观测性 | 数据平台 | 待规划 |
| [装备静态事实统一证据登记与事实编译](../plans/2026-07-26-gear-evidence-registry-design.md) | 来源：2026-07-26 用户确认。当前 PG 来源记录、能力推导输入、Resolver Evidence Ledger、Release evidence digest 和展示字段都使用“证据”语义，容易让多个组件分别维护和解释同一事实。第一阶段统一装备变体、装等、静态属性、宝石槽、附魔、美化、套装和槽位兼容性的原始证据登记；Registry 以不可变来源原件 Artifact 和引用原件的标准化 Observation 两层保存，由唯一事实编译器消费 Observation、生成 Canonical Facts，并封存到现有 immutable Gear Release；Resolver、API、前端和 health 只消费该发布事实，不再维护第二份能力判断。结构事实 unresolved 时阻断精确变体；宝石、附魔、美化等能力事实 unresolved 时只阻断对应功能，其他 verified 事实继续可用 | 第一阶段不扩展为全项目通用 Claim 平台，不纳入社区样本、SimC 运行结果、WCL 或聊天引用；复用现有 Gear Release、Manifest、候选 shadow、原子指针和回滚，不新增第二种 Fact Release 或第二套指针。Artifact 按来源 revision 和 payload hash append-only，parser 修复只重放 Artifact 生成新 Observation，不要求重新采集；装备静态事实没有时间 TTL，只由 season、来源内容、parser/compiler rule revision 或显式撤销触发受影响事实重编译，相同输入必须保持相同 Fact Hash，no-op 对账不得生成新有效 Release。Fact Compiler 不建立全局来源排名，而为每个 factType 集中定义允许来源、组合方式、false/0 成立条件、冲突处理、阻断范围和 compilerRuleRevision。Canonical Fact 必须区分 verified false/0、unresolved missing 和 unresolved conflict，真实来源冲突集中保留并 fail closed，不能通过覆盖旧值或前端默认值消解。unresolved Fact 只生成引用 factKey 和缺口条件的内部操作队列；自动 Worker 只能补充或重放 Artifact、生成 Observation、重新编译并构建候选，不能直接修改 Fact、活动 Gear Release、Manifest 或 compiler policy。迁移采用短期双算但单一线上权威：先导入/重放现有来源并 shadow 对比，区分 parity、预期纠错、新暴露缺口和 regression；再由新 compiler 构建候选 Gear Release，经 Resolver/API/Taro 同路径验证后原子切换 Manifest，最后用 caller-proof 删除旧事实写入和消费路径，禁止长期双写 | P1 构筑到模拟闭环稳定 / 数据可信度 | 待规划 |
| 社区装备端到端导入资格与原因追溯 | 来源：2026-07-25 用户确认及候选修复 `255413`。280 名 Raider.IO 候选玩家是 80 个社区装备槽位的缓冲池；必须按槽位从完整、可导入的候选中选出 winner，而非按全局前 80 截断。更关键的是，列表的 `canApplyGear` 不能替代最终导入：本次 80 张卡片均显示可导入，但真实 POST 审计为 79/80，根因是 PostgreSQL 元数据刷新遗漏了官方媒体登记，导致最终展示证据缺失。后续资格应以同一份后端完整导入证据为准，并输出可追溯的槽位、物品、证据层和写入 owner | 保持 fail-closed：不得以缺少 SimC、版本、官方媒体或完整显示证据的装备冒充已验证版本；不得用默认款、非官方图标或泛化 Gear Release 重建掩盖单件资料缺口。候选发布前执行 40 专精 / 80 真实 POST 导入审计；任何失败都要按“观测来源、精确变体、不可变 authority、官方媒体、最终 import”逐层追因，而不是只报 `blocked` | P1 构筑到模拟闭环稳定 | 待规划 |
| 版本初期证据分级门禁 | 新赛季样本不足时提供诚实的 partial 体验 | partial 不得进入排名、BiS、DPS 等强结论 | 数据可信度 | 待规划 |
| 微信 AI 能力适配 | 评估微信原生 AI 对延迟、流式体验和合规的价值 | 供应商锁定、能力差异和数据边界 | 炸鸡队长 / 微信 | 待规划 |
| 社区天赋采集补强 | 提升低覆盖职业、专精、英雄树和场景发现率 | 必须经过 authority 校验与稳定签名去重 | 社区模板 | 待规划 |
| 职业工具入口与构筑背景官方素材 | 来源：2026-07-21 至 2026-07-22 用户确认。专精首页四个工具入口采用 Blizzard 官方 WoW 图标；增辉 / 鳞长样板获确认，当前前端 40 个职业专精使用游戏内天赋背景与对应英雄天赋主题徽章；类树标签统一为“通用天赋”，且已在 DevTools 刷新后的真实预览中获用户确认 | 只使用经链接可访问性验证的游戏媒体；每个专精背景按职业/英雄/专精三段裁切，英雄树优先主题徽章、失效时回退现有根节点图标；“通用天赋”仅改显示文案，仍使用后端 `class` tree key；不得把低分辨率图标放大成主视觉，也不能以未核验素材冒充游戏事实 | Taro 14 路由重建 / 构筑体验 | 正在推进 |
| 天赋模板闭环恢复 | 恢复天赋页的命名保存、我的保存导入、社区模板导入与安全重置，使玩家能从配置到复用继续进入模拟；社区选举覆盖全部可采集区域，每个职业、专精、英雄天赋和场景只公开一个可导入 winner | 社区模板必须公开玩家、服务器、实际区域、分数、快照时间和来源；M+ winner 先通过可导入与精确归属校验，再按 Raider.IO 大秘境总分选第一，WCL 仅作可信背书和同分决胜；过期 winner 必须标注“已过期”但仍可导入，同步失败不得清空最近一次已校验 winner；保持每日同步，连续 7 次失败或距最后成功同步超过 7 天才标记过期，并以分区重试、来源健康告警和同步成功率/新鲜度门禁降低过期与同步失败，且不得冒充最新推荐 | P1 构筑到模拟闭环稳定 | 待规划 |
| Embedding Provider | 为公共知识和个人证据增加可替换向量层 | PostgreSQL full-text 足够前不引入 | 知识检索 | 后续 |
| 玩家授权公共知识分享 | 将明确授权的模板或复盘沉淀为公共证据 | 默认私有；必须匿名、可撤回、可审计 | 社区知识库 | 后续 |
| CMS 与内容角色体系 | 为资讯编辑、审核和发布建立工作流 | 权限、来源授权和发布审计必须完整 | 内容平台 | 待规划 |
| 复杂复盘工作台 | 汇总日志、SimC、构筑、时间线和行动建议 | 核心模拟与证据合同稳定后再展开 | 玩家分析 | 后续 |
| 顶级日志与手法知识库 | 按版本、专精和场景沉淀高质量操作模式 | 样本偏差、版权、赛季漂移和可解释性 | 知识库 | 候选 |
| 正式域名、HTTPS 与微信合法域名 | 消除体验版网络白名单问题并保护登录 token | 涉及证书、备案、白名单和回滚 | 发布可观测 | 候选 |
| 线上异常监控与告警 | 统一 API、同步、SimC、LLM 和数据健康告警 | 控制噪声并避免泄露敏感 payload | 发布可观测 | 候选 |
| SimC 版本升级闭环 | 串联更新检测、构建、切换、回滚和兼容 smoke | 未通过数据与任务兼容验证不得自动切流 | SimC 运维 | 候选 |
| 角色绑定、订阅和收藏 | 围绕玩家角色形成持续分析、提醒和历史 | 数据必须有微信账号 owner 和隐私边界 | 个人工作区 | 后续 |
| WebSim 角色档案同步 | 从玩家授权输入加载真实角色 | 官方 API 可用性、身份确认和 stale 状态 | 个人工作区 | 待确认 |
| 12.1 Catalyst 保留副属性 | 支持非套装转化后保留基础副属性 | 权威 catalog、resolver、SimC 和 manifest 证据未齐前保持 fail closed | 装备模拟 Phase 6 | 暂缓 |
| 装备逐槽静态属性投影与计算溯源 | 来源：2026-07-25 用户确认。装备详情需展示每件装备已核验的绿字属性，且后续整套属性计算必须能逐槽追溯到同一份权威静态事实；导入展示不能把整套汇总值或候选装备属性反推为单件事实 | 触及装备可信边界、公共导入快照与 Resolver 消费链。仅在精确 itemId + variantKey 与权威 Resolver 一致时消费逐槽属性；数据缺失时保留明确 unavailable/待核验状态，不运行 SimC、不修改推荐、同步、发布或健康判断 | P1 构筑到模拟闭环稳定 | 待规划 |
| 退役同步 gear stats | 异步快照稳定后移除 HTTP 请求内 SimC 执行 | 需两个客户端发布周期的调用遥测、410 门槛和回滚 | 装备模拟运维 | 后续 |
