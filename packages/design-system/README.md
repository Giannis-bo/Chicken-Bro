# Design System Source Surface

本 package 提供当前 Taro 路由使用的 tokens、primitives 和 owner。源码引用、编译成功与单元测试只证明工程可用，不证明 target fidelity。

组件是否复用、扩展或重做，由当前 route 的 target inventory、truth adaptation 和 component contract 决定。共享缺陷必须在共享 owner/token 修复；页面只保留目标中确实独有的 composition。
