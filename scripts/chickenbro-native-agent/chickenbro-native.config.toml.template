# Render this file into $CODEX_HOME/chickenbro-native.config.toml for one
# candidate only. __CHICKENBRO_CANDIDATE_ROOT__ is substituted at deploy time.
model = "gpt-5.6-luna"
# Native Codex research uses its own current web-search capability; the MCP
# tools remain optional read-only helpers for service-owned snapshots and URLs.
web_search = "live"

[mcp_servers.chickenbro_toolbox]
command = "/usr/bin/python3"
args = ["__CHICKENBRO_CANDIDATE_ROOT__/server/chickenbro_native_mcp.py"]
cwd = "__CHICKENBRO_CANDIDATE_ROOT__"
# MCP subprocesses receive an allowlist rather than the parent environment.
# Forward the normal proxy contract so the generic, read-only public-web tool
# can reach the same public network as the candidate service.
env_vars = [
  "CHICKENBRO_NATIVE_OBSERVATIONS_PATH",
  "HTTP_PROXY",
  "HTTPS_PROXY",
  "ALL_PROXY",
  "NO_PROXY",
]
startup_timeout_sec = 10
tool_timeout_sec = 30
enabled = true
required = true
default_tools_approval_mode = "auto"
