# QQ 群反馈修复与停机发布

用户于 2026-09-24 授权修复四组已登记问题并发布，明确要求不启动 QQBot。修复和候选验证已完成；发布回执另行绑定，真实群验收待用户恢复服务。

## 改动

- 专业路由接受支持的日志链接和同一发言者的明确续问，保留领域、账号及工具边界；准入失败不再发送模型的“已开始”草稿。
- 根据目标 observation 序号和时间取上下文，明确目标发言者，避免排队后的其他话题挤掉目标。
- 优先提供目标／引用消息的图片，标注来源关系；其他斗图素材不能冒充当前图片。
- 决策规则明确安静请求后不主动回应，明确 @ 仍回应；保持既有上下文窗口，不新增固定静音时长、频率配额或权限。

没有数据库迁移或客户端变更。实际 QQ 生产基底为 persona-v6，其人设保持逐字节相同。本次五个源码文件单独覆盖，其他源码沿用实际 QQ 发布目录；主分支另一个任务的两个 WCL 源文件不进入本次 QQ 发布。

## 验证

- 新增失败回归捕获专业链接被拒绝、拒绝后虚报启动、目标被截断、图片归属缺失；修复后通过。
- 集成主分支后的云端完整后端：928 项，927 通过，1 项 Windows 平台相关跳过。
- 与实际 QQ 发布基底一致的云端相关测试 26 项及隔离 PostgreSQL 测试 45 项通过；覆盖专业任务幂等、未来消息隔离、账号与群边界、图片优先级和失败恢复。
- 同条件固定样本：基线 10、修复后 10；独立留出 7，均完成语义审阅。简化基线在当前 Luna 上也通过，因此未将历史全部症状归因于模型；确定的代码问题由失败测试证明。
- 最终独立 Candidate 使用真实模型与 WCL 来源，走通 social decision → professional admission → Chat Worker → verified WCL result → recorded outbox。回答的首领、599.924 秒时长与未击杀状态符合工具事实；第二账号读取拒绝；无 OneBot sender、无真实群发送。
- 产品模型调用累计：Q1 12/32，Q2 6/32，Q3 5/32，Q4 8/32。Q1 包含主分支源码初验及精确 QQ 基底终验各两次调用。内部 provider 尝试未独立计量。
- 独立代码审查的两项 P2 均已修复，复审无阻断问题。63 项控制面检查通过。

[验收与预算](acceptance.md) · [独立审查](review.md) · [候选文件清单](candidate-manifest.json) · [专业链路结果](professional-final.json)

## 发布与恢复

执行器 `deploy-stopped.py` 在发布锁内校验旧发布清单及全部源码哈希，校验五文件变更，确认队列空闲和接入已停。停止 QQ Worker 后切代码指针和版本标记；不执行任何 start/restart，不重启 NapCat、不改变网站进程或业务数据。

旧 persona-v6 完整目录保留，配置复制至私有恢复目录并读回校验。发生切换异常时恢复旧指针和配置，仍保持两个 QQ 服务停止。此次不执行生产回退演练。

业务状态应为“已部署、保持停机、待线上验收”；不得标记已解决或声称用户认可。恢复 QQBot 需用户后续明确授权。
