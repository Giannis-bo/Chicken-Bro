# 独立代码审查

审查范围：QQ 五个源码文件、回归测试及停机发布脚本。审查基底为 fd97bbc5d8ec83a71dd8e591a4c46378be20f81e。原工作区未提交的 persona-v6 与其他任务文件不属于本次提交。

独立审查发现两个 P2，均已修复：

1. 目标移出最新 80 条窗口后，追加旧目标会将后续消息误作背景。改为按目标时间和 observation seq 读取上下文，增加同秒新增 90 条消息的 PostgreSQL 回归。
2. 畸形 URL 的 urlsplit 异常可越过专业准入传播。改为拒绝无法解析的 URL，解析和白名单核对使用同一去尾标点 URL，并加入坏链接回归。

复审无阻断问题。停机发布脚本仅调用 stop，不调用 start/restart；前置检查接入已 inactive，发布保留旧代码和配置恢复副本，失败回退也保持两个 QQ 业务服务停止。实际五文件清单、哈希及验证结果在 deploy-spec.json 和发布回执中读回。

固定语料/留出评审：专业请求返回 wow_read；不同成员的新话题不借前一个人的事实；群友互聊保持 silent；当前蓝图识别 BOAT、引用旧图识别 TREE、缺图不借旧图作答；安静请求后未 @ 为 silent，明确 @ 仍回应。基线在当前 Luna 上这些简化语料也通过，不能将历史全部问题归因于同一模型；已确定的代码缺陷通过失败测试与修复后通过测试证明。
