# G1 通用修复与真实鸡哥回归

2026-09-09；本地修改、隔离云端真实模型回归，未提交/发布，不代表正式上线或真人验收。

## 修改

- Chat WCL URL先移除type/view展示参数，再沿用严格域名、report、fight/source与未知参数校验；不改SimC导入合同。
- 裸报告只查询目录与角色，明确queryScope=report_discovery；不对无范围的报告直接请求统计。由鸡哥按用户目的选择场次或澄清，不默认将全报告归到第一场。
- GraphQL分项错误且仍有战斗数据时，保留有效数据与错误，并在网关维持partial；缺失事件不标成完整页，不把部分数据称作已解决。

## 证据

先添加测试并观察失败，然后实现。针对性28项、完整后端428项通过。全量使用已有 /private/tmp/chickenbro-merge-check-venv-20260907 环境；系统python缺fastapi的首次运行有10个错误，不记为通过。没有安装依赖。git diff --check通过。

源码身份见source-hashes.json。真实模型以云端ubuntu、现有生产profile及产品规则运行，两个修复模块只在独立进程内加载；8791来源网关使用短期capability，只允许WCL查询，未绑定真实用户SimC身份。测试结束关闭端口；不改生产文件、服务或会话。harness.py是远端诊断主体，需要注入PATCHES两个源码字符串，不能直接当正式调度器运行。

## 真实回归结果

| 输入 | 结果 | 范围 |
|---|---|---|
| 裸报告分析桃花别死 | 160.86秒，先目录后4场指定范围，共22次成功WCL调用；返回死亡、减伤和输出复盘 | 第一轮修复真实验证，随后新增type/view规范化不影响此路径 |
| 第一组原始URL，含fight=2&type=summary&source=5 | 最终99.19秒，6次成功WCL查询，识别元素萨，继续查死亡、承伤和施法分页 | 原样URL通过；评分百分位未提供，不宣称已证明低分主因 |
| 第二组带fight=7&type=damage-done&source=4 | 最终98.91秒，6次成功WCL查询，返回指定角色和两次死亡复盘 | 不再以空数据要求截图；未核验所有职业机制与替代技能收益 |

第一轮原样URL曾返回失败且无成功网关调用，帮助定位额外根因：通用URL解析器拒绝type/view。保留于live.jsonl，不隐藏失败；修复后的回答和调用清单见live-display-fix.jsonl。

这轮验证了查询可达、模型主动选范围/分页和有证据边界的分析输出；没有逐个独立重算所有回答数字，也没有取得rankPercent/bracketPercent。不能把它标为完整评分归因能力通过。历史进程退出失败也不因本轮查询修复而宣称解决。

## 审查与交付边界

已检查精确源码diff：改动仅Chat来源读取/网关及对应测试；保留现有未知参数与owner/capability边界，未改客户端或数据库。现有其他任务的AGENTS、runbook、roadmap等WIP保留。

G1查询修复完成并有真实鸡哥回归证据；待用户查看。未纳入正式发布批次。评分数据缺口与历史执行失败单独跟踪。
