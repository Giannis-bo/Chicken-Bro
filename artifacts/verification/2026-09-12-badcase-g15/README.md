# G15：非激活英雄树天赋解析

状态：**本地修复验证通过，发布阻断，原反馈未闭环**。

新反馈的目标是两套完整配置各自进行 300 秒单目标和五目标对照。历史工具回执中有两次 `TALENT_BUILD_INVALID`；本地用原天赋串复现到非激活英雄树节点检查，不能把旧角色 DPS 当作这次结果。

修复只调整现有天赋导出解析：按选择器过滤不生效的英雄树，过滤前验证 rank；活动树点数、专精、运行版本和路径约束保留。当前云端固定引擎源码明确规定非激活英雄树节点不生效，不需要升级引擎。

原题及死亡骑士、恶魔猎手变体在基线均失败；修改后相关 63 项测试通过，涵盖正常官方配置、非法点数和现有工具权限合同。独立审查未发现代码阻断问题；这不是五类真实模型前后对照或 Candidate/线上业务验收。

## 为什么不发布

原题需要完整插件配置，但当前产品只支持 Raider.IO/已有任务输入。编译器未接入原配置包含的 Omnium、制造副属性及重定向属性等字段。仅消除天赋报错不能证明四组精确配置已恢复；自动化不能以忽略字段、替换旧配置或重新定义通过标准来发布。

保留局部修复，等待明确的完整输入合同决策，或能证明等价的完整受支持来源。之后按剩余预算固定原题、变体、独立留出、正常和权限样本，完成真实前后对照、Candidate、发布与线上门禁。无新增条件不重复消耗模型/SimC预算。

本轮新增产品模型、WCL、Raider.IO、SimC调用均为0；历史用户消耗未知，不计为0。实际记录与未验证项见 [result.json](result.json)。未合入、推送或部署，不修改原反馈。私有交接位于自动化状态目录。
