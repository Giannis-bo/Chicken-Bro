# 2026-09-11 项目收尾

用户授权：任务 `01a08f46-d2fd-7383-bdb2-fa3854166a03` 明确“把这些都处理收尾一下，然后该清理清理，该合入合入，该发布发布”。范围为已盘点任务的状态、到期副本处置接入、安全 Git 清理与必要交付；不把收尾视为新增事实、收费服务、发送外部工单或手工产品验收。

## 实际完成

- 控制面从笼统 `in_progress` 改为 `delivered_with_followups`，G10 顶层实现/Candidate/live 更新到最终证据，历史失败与消耗保留。
- 现有 `badcase` cron 已通过 Codex 自动化工具更新并读回，保留原时刻、独立会话、模型和执行范围。Agent 在扫描前消费原保留清单和两份固定逐文件哈希基线；未新增调度器、未部署确定性清理程序。配置接入不冒称到期执行完成。
- 当前本地6个目标共10文件，云端7个目标共19文件已读回并生成私有基线。原清单SHA为 `2446de8c65b7a441e2312397db81e4e08c1869ab65ff561a457877066f1e1902`；各自基线和精确操作规则见[运维入口](../../../docs/badcase-workflow-operations-history-20260911.md#联合私有副本到期处置)。尚未到期，未删除任何联合私有副本。
- 已移除11个完成工作区与8条功能分支；所有补丁均为main祖先或 `git cherry` 等价，无遗漏独有补丁。完整Git bundle经过独立bare仓库恢复、13个工作区提交可读；13份ignored验证日志/辅助材料已归档并逐文件校验。没有强推、历史改写或 `git clean`。
- 主工作区58条未跟踪文件/目录记录及旧 `.worktrees/badcase-workflow` 未跟踪证据保留；不将原始私密资料提交仓库。私有恢复材料位于 `/Users/boyuan/.codex/badcase/chickenbro/closure-20260911/`，`before-cleanup.bundle`、`ignored-evidence.tar` 与清单可恢复原提交和日志。
- CLI实时状态仍将G7/G10/G8/G9显示为approved；新增[报告哈希绑定交接](external-dispositions.json)，已加入现有调度前置核对，同一报告范围跳过重复执行，新报告独立评估。原CLI账本及failed记录未改写。
- 读取最新自动化交接：Badcase 9月11日12点扫描新增0，避让当时活动任务；当时的G13旧eligible状态不能覆盖后续released。更新日志最近成功为9月10日19点；9月11日19点仍交由原调度，未重复补发。

## 当前线上与发布判断

本轮SSH读回 `/opt/chickenbro` 及 API/Worker 主进程实际cwd均为 `badcase-22c681f99f457329f1b25570343b7f9be401201b`，两服务active；Web current为 `research-22c681f99f457329f1b25570343b7f9be401201b`。公网13文件按原发布manifest逐字节SHA重新核对通过，见[live-web-check.json](live-web-check.json)。首次本机Python TLS证书严格检查失败，改用系统curl且保持证书校验后成功。

本次没有新增产品代码，应用与构建输入相对已发布源码无差异；文档和调度配置不需要API/Worker或Web重新部署。未重复模型/SimC业务测试、未做新的手工验收或生产回切演练，现有真实业务证据仍绑定原发布。[清理回执](cleanup-result.json)与[自动化配置读回](automation-check.json)独立记录。

## 保留事项及恢复条件

| 事项 | 状态 | 下一步和停止条件 |
| --- | --- | --- |
| 联合副本到期 | 已接入现有调度，未到期 | G10/混合归档9月17日10:21:38起，原生18:01:49起；下一可运行窗口按精确清单执行。路径/哈希/活动引用/适用恢复门禁异常即保留并报告。离线或额度失败不保证准点执行。 |
| 制造/特殊装备 | 待真实版本资料 | 取得一份含物品ID、bonus/品质、引擎版本及报告读回的真实fixture后，限定该版本验证，不扩展全装备。 |
| 附魔品质 | 待效果映射及报告读回 | 取得同一附魔不同品质的权威效果证据后，限定品质对照；当前名称覆盖不当作效果精度证明。 |
| 首领阶段 | 待时间轴及入场状态 | 明确首领、阶段起止、技能/资源/增益与目标状态；短时实验不视为完整战斗还原。 |
| 饰品机制 | 待实测参数 | 取得当前赛季机制证据与引擎实现对照后验证，不以DPS抽样精度掩盖机制不确定性。 |
| 原生历史2ed6失败 | 根因未知 | 原始协议未保留；只在新事件有协议/日志证据时重开，不重跑旧题猜测同因。 |
| COS历史调查 | 外部账号/供应商阻塞 | 目标APPID 1257807175的授权只读身份尚未到位；当前已知控制台账号不匹配。取得账号及供应商书面回复后核对日志/版本能力。工单草案保留，未发送；未开通收费服务、迁移或声称独立灾备完成。 |
| 跨研究账号总量 | 待产品需求 | 明确账号额度周期和用户交互后另定范围；现跨轮研究预算交付不包含该承诺。 |

以上不属于本轮待合入代码，不继续消耗已停止的旧验收预算。用户明确收尾授权已记录，但不替代实际手工产品测试。

## 本地验证

63项控制面测试通过，JSON和本轮Markdown本地引用有效，git diff --check通过。首次控制面62/63：新证据目录尚未登记keep规则；补充精确保留前缀后63/63，不修改测试或历史清理清单。无应用代码或构建输入变化，未安装依赖、未构建或发布产品。

## 后续用户决定：COS关闭

2026-09-11用户明确“这个也忽略，不用管了，没问题”。上表COS外部依赖为收尾时的历史状态；该项及相关审计/恢复跟进现已按用户决定关闭，不再推进或提醒，不发送工单。原事实与证据保留。
