# 首次进入直接发送 · 2026-09-09

用户报告无历史会话时必须点击“新会话”才能发送，修复后明确授权“提交、合入、发布”。源码 `cb0395f302aed134675c277dce7d5e8ddbc5123a` 已推送远端 main 并发布 Web。首次有效发送自动创建会话，创建失败保留草稿，重试复用创建幂等键；防止创建期间重复发送，离开页面后不继续发送。

283 项前端测试、TypeScript、Lint、diff 检查通过；生产 H5 构建通过，存在两个资源体积警告。发布使用最新 main 上的隔离工作区，不包含主工作区其他任务尚未提交的 SimC 修改。

发布前使用新 Web 产物与真实生产 API、合成 QQ 测试身份验证空历史账号按 Enter 首次发送；发布后使用公网页面和另一空历史账号验证点击发送。两次均不点击新会话，各只创建一个会话并发送一次，真实模型回复 Horde，刷新恢复历史，另一个账号读取返回 404，无页面异常。属于真实业务验证，不代表新的真人 QQ OAuth 登录验收；发布前未新建隔离数据库。

manifest.json 绑定源码、压缩包与 13 个文件 SHA。promotion.json 和 public-verification.json 证明 Web 原子切换后 13 个公网文件全部匹配，readiness ready。后端保持 `/opt/chickenbro-releases/g2-08404a091a6dc9a08bcf65a759b47763c2942711`。旧 Web `/var/www/chickenbro-web/releases/inline-images-4a519c4c1882db9085fe5b4ad345cc1c062e522d` 保留用于回滚；未演练生产回滚。

测试完成后仅归档本次两个合成身份的会话、撤销其会话并禁用身份，确认无活动生成；凭据未写入证据。主工作区存在其他任务持续修改，未执行 stash 或覆盖式同步，其 WIP 与未跟踪文件保留。
