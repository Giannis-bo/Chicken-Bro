# WCL 工具效率优化验证

状态：本批实现与专项验证完成，未提交、合入、推送或发布。用户于 2026-09-10 授权优化/测试，随后要求适可而止；不再扩样本或重跑。实际身份见 identity.json。

## 交付

- 原 15 个工具不变，WCL report/batch 增加 `full`（兼容默认）、`overview`、`events`、`statistics` 视图。概览不取事件；事件视图不取/返回重型表格与角色装备。
- 每轮相同 WCL 请求复用与并发合并：成功结果 60 秒、16 项/4 MiB 上限；保留原独立上下文缓存。不同运行不共享，失败/partial 不缓存，调用者获得副本。生产 Worker 每次运行独立创建 reader。
- 统计限定明确 fight/source 和报告相对时间窗口，最多 5 页且共享 20 秒上游预算。按不重叠半开区间统计 cast、已知有效治疗/过量治疗和资源浪费；不去重合法的同时同类事件。字段缺失、分页/截断/范围异常保留不完整标记。最多 128 个技能/资源分组，超出明确标记缺失。无自动因果判断、有效治疗收益推断或跨账号缓存。
- 结构化统计贯通实际 MCP → 网关 → 回答证据索引，保留范围与局限；不改变 SimC、数据库、队列或 Web 合同。

## 验证结果

后端 667 项通过，含本批新边界测试与现有 Chat/SimC/owner 回归。使用已有 `/tmp/chickenbro-merge-check-venv-20260907`，未安装依赖；首次系统 Python 缺 fastapi 的环境失败不算通过。控制面 63 项中 62 项通过，1 项为基线已有的文档保留分类错误：`docs/roadmap.md` 引用 `artifacts/verification/2026-09-09-badcase-workflow/production-final-release.json`，HEAD 的旧规则仍把它归为 delete。已用 HEAD 原规则核验，不扩大本轮范围去修改旧发布记录。新计划、代码和证据均归类为 keep。最终 `git diff --check` 通过。

真实来源使用云端 `/tmp/chickenbro-wcl-efficiency-20260910` 的隔离源码与现有只读 WCL API；没有切换服务、写正式业务数据或提交 SimC。

| 窗口 | 完整返回大小（有界投影前） | 统计返回 | 独立逐事件核对 |
| --- | ---: | ---: | --- |
| 圣能资源 | 40,751 B | 2,052 B | 22 个当前 actor 事件，圣能浪费 9 点，合计一致 |
| 另一报告施法 | 32,102 B | 2,312 B | 17 个事件中的 13 次 cast，11 个技能 ID 逐项一致 |
| 治疗 | 198,359 B | 2,145 B | 两页共 366 个 actor 事件；264 个 heal 的已知值合计一致；77 个缺失值导致 metricsComplete=false，未判完整指标 |

治疗的完整返回超过现有 180 KB 门槛，实际有界响应为 249 B 的 partial，不把 198 KB 称为模型实际接收量。统计保留有依据的已知值及缺口。三种窗口重复事件查询均为 0 次新增上游请求。见 source-probe-final.json。

## 真实模型对照

相同 Astra/low profile、相同问题与 240 秒预算、独立 app-server 和只读工具网关。没有真实用户会话写入、SimC capability 或生产 HTTP/SSE/数据库验收。

| 问题 | 基线 | 候选 | 结果 |
| --- | ---: | ---: | --- |
| 圣能浪费 | 28.129 秒 / 40,750 B | 24.482 秒 / 2,052 B | 均为 9 点，均明确不等于有效治疗损失 |
| 独立报告施法统计 | 31.117 秒 / 32,101 B | 27.512 秒 / 2,312 B | 均列对 11 个技能，共 13 次，保持角色与窗口范围 |

候选均自主选择 statistics，各用 1 次工具调用。返回量分别下降约 95% 和 93%；两次回答分别快约 3.6 秒。仅各一组对照，不声称稳定延迟改善、总体解决率提高或复杂排行榜问题已经提速。第二份报告没有用于调整生产实现；独立原始事件统计作为参考。没有重复模型实验或覆盖全部用户问题。

首轮模型隔离 harness 漏传生产代理环境，未产生工具调用；只终止本任务临时目录下的 app-server 进程组，保留 `*-missing-proxy.json`，补齐代理后才运行上述对照。首次来源 probe 把战斗内时间误当成报告时间，空窗口结果留在 source-probe.json，不用作非空统计验收；修正 harness 后另存 scoped/final。实现增加了统计窗口须落在已选战斗范围内的拒绝检查。

## 审查与未验证范围

本地审查确认无角色/报告/问题文本特判、无新依赖、无生产配置或账号权限改动；报告 ID 只在验证 harness 中。默认完整视图与原事件分页合同回归通过。工具统计仍可能遇到数据缺失和上游失败，必须遵循 complete/metricsComplete；本轮没有为缺失字段假设零值。

未执行完整隔离 DB/API/Web Candidate 发布、真实 QQ 登录、生产切流或回滚演练。本轮证据是本地代码测试、隔离真实来源和真实 NativeCodex 对照，不是新生产验收。

## 本地收尾（2026-09-10）

用户要求继续推进且避免无尽穷举。本轮未改变运行/测试源码，通过 hash 确认与上述 667 测试、真实来源和两组模型对照的版本一致，不重跑或扩样本。仅将当前路线图已经引用的上一批正式发布记录加入精确保留路径；控制面一次重跑 63/63 通过，历史失败日志保留。线上五个目标路径与本地基底逐一核对相同，发布清单见 release-preparation.json；生产仍未改变。完成本地提交，尚未同步远端、合入或发布；首次完整 Candidate/生产 HTTP 验收仍须在发布阶段执行。
