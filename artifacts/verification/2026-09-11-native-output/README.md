# 原生策略拒绝终态与脱敏诊断修复已上线

联合发布运行源码为 `22c681f99f457329f1b25570343b7f9be401201b`。API/Worker 已使用本批独立后端目录，Web 已切换到 `research-22c681…`；13 个公网文件哈希、四项联合语义审查及 45.6 秒观察通过。见 [联合发布回执](joint-release.json)。后续文档 main 提交不是新的运行源码。

本次修复两项已证实的适配问题：原生错误缺失诊断分类，且策略拒绝被误报为输出无效；持久化订阅又把所有 failed 标记为可重试。现有原生调用现保留白名单错误类别、代码位置、结构化上游类型与合法 HTTP 状态，策略拒绝返回 `CODEX_REQUEST_REJECTED`，持久化重放使用与直接交付一致的可重试错误码白名单。Web 明确提示未通过安全检查、消息已保留。没有新增搜索工具、通用 fallback、绕过策略或自动模型重试，也不记录错误原文、提示词、凭据或协议包。

历史失败 `2ed6e056-58d3-4fcc-aff1-9fe93e6c4fbf` 没有足够协议细节，具体根因仍未知，原 9/10 与历史消耗不变。新复现取得了匹配 thread/turn、不可原生重试的 `cyberPolicy` 回执，证明新复现是原生服务策略拒绝，不能反推历史单次失败必定同因。原始记录见 [G12 最终窗口](../2026-09-11-g12-final-window/README.md)，本轮脱敏错误回执见 [diagnostics.json](diagnostics.json)。

`9e92bc124` 联合 Candidate 原题返回了正确拒绝码，但因 retryable=true 未通过，保留该失败。修复后只读取同一持久化运行补验：原记录仍为 failed / CODEX_REQUEST_REJECTED、retryable=false、无助手及工具、owner 隔离，数据未改变，0 模型 / 0 SimC。见 [Candidate 补验](candidate-replay.json)。

正式线上权限题运行 `5829b167-a0bd-4d94-a463-e148c28de1fb` 同样明确策略拒绝；幂等重放不可重试，零工具、无助手记录，第二账号无法读取，QQ/CSRF 检查通过。已独立核对生产数据库和私有回执 SHA。此项通过表示拒绝终态处理正确，不表示模型成功生成回答。见 [线上独立审查](live-review.json)。联合真实图片、研究预算、SimC 指标及来源验证由共同发布记录承载。

本地 128 项联合后端相关测试、31 项 Chat 状态测试、typecheck 和单项 H5 构建通过。后续 durable 定向测试先复现三个错误标记，再通过修复；104 项相关测试在云端既有 runtime 的独立目录通过。本机该组完整测试缺少 FastAPI，未记为通过、未安装依赖。最终 Web 由联合发布构建并核验。

本任务消耗 5/8 次直接模型调用、0/4 次直接云端 SimC；共享通用验收在联合发布任务记账，不重复计数。历次失败、采证脚本错误与源目录输入错误均在 [result.json](result.json) 保留。私有输入输出按原七天到期时间归档并校验，不延长原保留期限，见 [retention.json](retention.json)。原诊断 stash 未 apply/drop。

旧版本及恢复材料由本批联合发布保留；未执行生产回滚演练，未宣称用户手工验收，也未宣称消除所有偶发原生错误。
