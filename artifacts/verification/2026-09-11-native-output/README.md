# 原生策略拒绝终态与脱敏诊断修复

已合入推送 `22c681f99`；Candidate 原题与零模型重放补验通过，线上验收待联合发布。预算任务的 `0009` 迁移与新版 Web 必须一同纳入最新发布包，不能只部署旧后端 overlay。

历史失败 `2ed6e056-58d3-4fcc-aff1-9fe93e6c4fbf` 未保留足够原生协议细节，根因仍未知，原 9/10 和历史消耗保持。原始记录见 [G12 最终窗口](../2026-09-11-g12-final-window/README.md)。

本轮第一条原权限题正常拒绝，后两条诊断版复现失败。最后一条取得匹配当前 thread/turn、不可原生重试的 `cyberPolicy` 错误，零工具调用。这证明新复现为原生服务策略拒绝，不证明历史失败必定同因。

修复复用现有原生调用和失败终态：白名单记录代码位置、固定失败类别、结构化上游类型和合法 HTTP 状态；不记录错误原文、提示词、凭据或协议包。明确策略拒绝返回非重试 `CODEX_REQUEST_REJECTED`，仍持久化为 failed，不生成助手回答。Web 提示请求未通过安全检查，消息已保留。没有新增搜索工具、通用 fallback、绕过策略或自动模型重试。

本地 128 项联合后端相关测试、31 项 Chat 状态测试、typecheck 与单项 H5 构建通过；最终最新源码 Web 构建由联合发布核对。故障注入覆盖 EOF、RPC 错误、错误身份、失败终态、空最终答复、上游枚举及日志保密；策略拒绝覆盖通知和失败终态，业务覆盖不重试及幂等回放。

本轮已消耗 3/8 模型、0/4 云端 SimC，原失败和脚本执行错误全部保留。计划每环境仅一条原权限题，并复用联合通用业务验收。正常拒绝与明确非重试策略拒绝均可满足权限边界，但“failed”不会改记成模型回答成功。

完整脱敏记录见 [result.json](result.json)。私有问题只在既有受限证据与任务私有目录使用；原始证据七天到期时间不延长。

## 联合 Candidate 暴露的持久化重放缺陷

`9e92bc124` 的原权限题已真实返回 `CODEX_REQUEST_REJECTED`（`cyberPolicy`、零工具、无助手记录），但订阅层把所有 failed 统一标记为可重试。该轮验收保持失败。修复 `22c681f99` 让持久化订阅使用与直接交付一致的错误码白名单；策略拒绝、输出无效和未知错误都不再标记为可重试。

新增测试先复现三种错误的错误标记，再通过修复。104 项相关测试在云端既有 runtime 的独立目录通过；本机完整 delivery 测试缺少 FastAPI，未记为通过、未安装依赖。本任务累计 4/8 模型、0/4 SimC。下一步只读回原持久化运行补验，不再运行模型；线上验收仍待新冻结版本。

零模型补验已通过，见 [candidate-replay.json](candidate-replay.json)：源码 `22c681f99`，原拒绝记录保持 failed / CODEX_REQUEST_REJECTED，重放 retryable=false，无工具及助手记录，owner 隔离且原数据不变。原先失败没有覆盖。
