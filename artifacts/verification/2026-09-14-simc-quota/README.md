# 取消 Chat SimC 次数上限

2026-09-14 用户授权取消次数限制，随后授权提交、合入、发布。

移除单轮网关及跨轮研究的 4 次提交上限；状态中的 simulations 上限/剩余为 null（不限次数）。保留历史提交记录、相同研究/快照/方案的幂等、研究结束门禁及账号隔离。核心规则和 MCP 说明同步更新；不改变来源采样、队列、单任务参数与执行时间保护。

本地相关 135 项后端测试及 63 项控制面通过。回归覆盖第 5 次提交、历史记录续跑、失败后重试和幂等；数据库集成与真实云端验收待本批执行。

发布范围为 4 份运行文件；基底 cdc7d262（已包含 G16），Web 保持 chat-return-0f8b3ce9。只发布本批 reviewed overlay，完整服务端源码与实际生产基底逐文件核对。deploy.py 沿用全任务空闲门禁和共享发布锁，固定 manifest 和恢复环境哈希。

有界验收：Candidate、线上各 1 次 Chat 模型请求，各 1 个 61 秒/100 iterations 云端模拟；专用新会话预置 4 条明确标注的历史额度测试记录，验证第 5 次真实提交、正 DPS/来源、幂等重放和第二账号隔离。预置记录为测试夹具，不冒称此前真实运行了 4 次模拟。保留失败和成本。线上 45 秒观察、10 次 readiness、零新增 error 日志；单次 readiness 超时 5 秒。恢复材料核验，不执行生产回切演练。

状态：本地验证通过，Candidate/线上待执行。
