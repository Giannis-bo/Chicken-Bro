# 通用研究证据与语义优化发布

状态：已合入、推送并发布；运行源码 `b406fc9155a03edba9e95912713606736fb2a1bd`，API/Worker同时切换。用户已授权五项优化及发布，未声明用户手工验收。

## 交付

- 通用历史证据按对象、视图、窗口与过滤条件区分，复用治疗、伤害、施法、资源统计及有界事件样本；属性优先、partial与截断显式保留。
- 早期用户目标和修正保留可追溯原文，相邻助手选项仅用于理解指代；研究切换隔离旧消息/图片，遗漏不冒充完整上下文。
- MCP说明统一每研究累计4个模拟；状态提供范围已用/剩余和单run执行额度。
- owner-scoped真实SimC返回值进入最终回答的有限声明检查及原有一次修复；仅识别明确模拟完成、DPS/HPS和百分比，不能证明完整自然语言、对象指代或因果正确。条件/否定/一般日志指标不误拒。
- 固定20题（含4组多轮）与离线trace评审入口已建立。默认只校验集合，不调用模型；旧研究执行器仍默认原3题，拒绝误跑多轮题。

## 验证

- 测试先红后绿；独立审查发现并修复条件语句误拒、最近20条边界指代丢失及大装备表挤掉属性风险。
- 788项后端在已有云端Python环境通过，13项研究数据库测试在独立Candidate库通过；63项控制面、主工作区44项定向通过。本地完整后端缺FastAPI等依赖未通过，未安装新依赖；复用云端运行环境执行相同源码。另一任务未提交文档令其主工作区控制面检查不通过，未将其改动纳入本批；隔离的本批源码控制面63项通过。
- 8条Candidate：5条固定集合问题（基础问答、能力介绍、Top100拒绝、空结果、相关性），另3条专项覆盖长对话修正、双资源窗口和真实已完成SimC结果。固定集合其余15题未运行，不称全量语义准确率。
- 两条线上：能力/执行意图边界、真实已完成任务结果读取。Candidate与线上DPS分别159166.78869662393和160820.55569451672，回答舍入一致。
- QQ授权URL、CSRF、退役Bearer/入口、图片上传读取及第二账号隔离、Chat历史与幂等重放通过。没有本轮人工QQ登录或新的引擎模拟；共10轮模型，0新增SimC、0直接WCL查询，两个实际工具调用均为读取已完成任务。
- 两个测试脚本准备错误保留：创建会话遗漏幂等头；历史fixture遗漏executions父行。均发生在相应模型调用前，前6条模型未重跑；仅精确结束未调度的fixture运行后安全停止Candidate。

## 发布与恢复

精确9文件overlay与完整139份server源码核对通过；Web未改，原13份公网文件SHA全部匹配。正式库及所有运行配置保持现有合同，无migration、依赖或引擎升级。空闲门禁同时切API/Worker。

root/0700私有包：`/var/lib/chickenbro-generalization-release-20260911/`，包含manifest、deploy.py、overlay、测试脚本和私有会话证据。恢复快照root/0600已校验；旧后端 `badcase-e61595bf20385b881e5023eab232e1cca23227bd` 与Web保留。在同一空闲门禁下执行本批 `deploy.py manifest.json rollback`，恢复后再核对运行身份及业务，不回滚数据库覆盖新写入。未进行生产回切演练或连续监控。

另一个任务在main的Badcase文档WIP已通过精确路径暂存、合入、恢复并逐字核对，未进入本批源码。

[运行清单](runtime-manifest.json) · [发布与用量](release-summary.json) · [逐条语义评审](semantic-review.json)
