# 有限研究完整解答：发布证据

当前状态：本地验证完成，Candidate执行中；尚未切换生产。运行源码候选 `e61595bf20385b881e5023eab232e1cca23227bd`。

## 问题与产品策略

原两日志、两战斗、三奶骑比较属于合理有限研究。原持久账本48/48来源子查询、8,600事件单位、2场战斗、4个身份槽（其中一人跨来源重复）、0新模拟。70条工具记录有18条失败；已通过请求哈希确认14条属于本地不支持的overview+Healing。原属性证据其实已经取得，跨轮未传给模型。历史token未知，不从2.34MB结果体积估算。

改动：研究范围跨轮保留10玩家/3战斗/3比较组/4新模拟；48来源子查询/20,000事件单位/360秒改为服务端单run执行保护，历史成本保留，正常追问可继续。无新增每日个人研究配额。原生网页搜索与后备网关的计量边界不变。

本地无效参数先拒绝，不预留上游；同owner/conversation的近期成功概览或治疗结果可精确复用，仍检查研究范围。历史包保留属性、装备、天赋、治疗和来源，按48KB事实上限裁剪，不传原始事件；显式新研究不自动登记旧对象。治疗专用查询返回配对有效/原始总量，负数调整与过量分开，不能把事件样本当整场。新增0010迁移记录每run工作量、主回答/修复token usage及来源请求元信息；缺失仍为unknown。

## 已有验证与失败

- 独立PostgreSQL：32项研究/持久执行用例通过；同run重建不能刷新、正常新轮48后继续、范围/owner隔离和新研究历史隔离均通过。
- 定向源码审查完成；负数调整和历史摘要修复也复核通过。
- 三名真实玩家直接治疗聚合全部verified；共5次直接WCL验证请求（含前期2次合同/响应查询），0新SimC。
- 第一Candidate `33221e83-73fb-46a8-b5e8-89f17278db60` 返回回答但治疗配对不完整，不通过；393,221 total tokens，336,256 cached input，16来源子查询。
- 第二Candidate `beacb229-75d9-4dcb-a70f-98fba5ab6551` 取得完整治疗后，第二个final_answer触发现有协议拒绝，不通过；533,403 total tokens，457,216 cached input，18来源子查询。未通过改变协议或删除失败制造验收。
- 私有原始、会话和失败记录位于本机 `~/.codex/badcase/chickenbro/research-completion-20260911/` 与云端root/0700 `/var/lib/chickenbro-research-completion-release-20260911/`；不提交用户会话、会话凭据、环境密钥。

## 发布与恢复

17个运行文件的精确overlay基于原后端 `badcase-22c681f99f457329f1b25570343b7f9be401201b`；完整受跟踪server目录与候选源码哈希一致。Web未改，保留原13文件。0010是向后兼容增量，不回滚业务数据。生产切换须空闲门禁同时切API/Worker并核对业务、公网产物和运行身份。恢复由本批manifest/deploy.py在同一空闲门禁下执行，旧后端/Web与环境快照保留，不声称回切演练或人工QQ验收。

第三Candidate：原题 `88cc31e4-95f4-44ab-aed7-de019e3002f9` 成功回答三人总量、过量、属性及有覆盖范围的施法窗口，17来源子查询/2,400事件单位，632,399 total tokens；属性追问 `e0ac6960-e958-4ee9-8fdd-fa8ee82ac630` 回答正确，但1次新overview+2次healing缓存，180,059 total tokens，未满足零新增上游效率门槛。根据实证补齐属性优先和URL规范化，保留该不足。最终本地744项后端通过（URL规范化前），最终缓存/历史/持久执行30项新增定向通过。
