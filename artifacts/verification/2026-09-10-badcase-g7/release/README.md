# G7 施法归因修复与发布

2026-09-10 用户明确授权 G7 必要修复、验证、提交合入与立即发布。运行源码 `f1f0f0a0d32c5682cae6493766b686f2aba824ac` 已上线，正式业务验收通过；后续文档提交不改变这两个运行文件。

## 问题与通用改动

既有榜单发现已覆盖 G7“找不到高分报告”。完整诊断另发现：从日志 cast 频次和同步事件推断主动按键、浪费 GCD 或使用习惯，再给减少施放/拆宏建议，缺少来源证据。

WCL 所有成功/部分结果增加统一解释限制，保持原始计数和事件；Chat 规则要求核对主动/触发来源，不从同毫秒或反复同步认定按键习惯、宏绑定或自动触发。不实现特定技能分类器，不匹配用户、角色、会话、报告 ID 或问题文本。原始数据不去重、不改写。来源参考为 [WCL 事件文档](https://www.warcraftlogs.com/help/pins) 和 [CastEvent 合同](https://de.warcraftlogs.com/scripting-api-docs/warcraft/interfaces/RpgLogs.CastEvent.html)；不能据此断定原题具体天赋机制。

## 有界验证及失败记录

- 预先固定20次模型调用、累计模型/回放3000秒、来源调用80次上限及停止条件。实际使用16次短题、首次失败Candidate1次、最终Candidate2次、正式1次，共20次，不再扩样。
- 四类短题（原始证据、同机制变体、独立留出、正常计数）前后各两次均通过，权限的未签发/撤销能力前后各两次均拒绝且不访问来源。基线短题也全部通过，不宣称准确率提升。记录保留各自源码/答案hash，未冒称全部使用最终提示词。
- 首次完整Candidate178.57秒虽不再建议少补火震，但把同步事件归为习惯并建议独立按键，语义失败，未发布该版本。随后仅补充规律同步不能证明按键习惯/宏绑定，使用原预算两次完整同题回归；没有重新生成留出或掩盖失败。
- 最终两次Candidate158.95秒、163.00秒均完成真实榜单/报告/施法分析，保留主动来源及收益边界，owner/幂等/历史/断线通过。旧短题和权限证据按未变条件及影响范围复用；最后提示词增量由这两次完整链路验证，不宣称旧短题已在最后版本重跑。
- 正式公网172.36秒通过同类原请求，实际读取同Boss、难度、专精第1/3名，明确日志频次不等于按键，不能从同步认定绑宏；owner、幂等、忙碌互斥、历史读取与会话撤销通过。
- 完整后端668项通过；最后提示词补充后82项受影响测试通过；控制面63项通过。控制面起初因隔离工作树缺主目录已有历史附件失败，复制只读附件后通过；该历史文件不进入本提交。

详细证据：[短题逐次评审](model-review.json)、[首次失败](candidate-initial-review.json)、[最终Candidate](candidate.json)、[正式验收](production.json)。本轮不重新验证全部游戏机制或每句建议的所有前提，不声称总体稳定性、准确率或用户真机验收。

## 发布与恢复

本批重新生成精确manifest，两个文件覆盖当前 `badcase-f95f6bbe...` 基底；整套后端库存哈希、两服务有效配置和运行cwd核对。执行器持有发布锁，有界排空Chat/SimC后切换；没有迁移、依赖、客户端或SimC引擎变更。14个公网Web文件与原制品一致。

旧后端 `/opt/chickenbro-releases/badcase-f95f6bbe234a2da8a67ca86466e96c6c29c6b794` 保留，恢复环境快照由执行器root/0600保存且绑定manifest；回退使用本批 `deploy.py manifest.json rollback`，仍须安全排空及恢复业务核验。此次未触发实际回退，不宣称本轮恢复演练通过。

本次为用户直接授权的人工发布收尾，不是18点自动批次，也未伪造CLI通用批次布尔门禁。G7本地分组保留本报告的approved授权，实际完成证据以本目录为准；历史G6批次未复用，原始resolved字段不改。
