# 剩余用例有限验证：查询修复已合入，发布失败后恢复

用户批准下一阶段最多8次模型、6次云端SimC、20次来源查询、60分钟；窗口2026-09-11 11:35:55–12:35:55北京时间。历史96模型保留。本轮没有重跑先前被跳过的CODEX_OUTPUT_INVALID用例。

## 当前结果

- G8 原题在已上线955源码回放通过：真实四件套、完整单体循环，300秒/10000次，两组只改变目标技能时序条件。先风暴守护者组161017.47DPS，升腾内组160247.65DPS，相差0.478%，保守误差合计173.14DPS。三次采样窗口核对通过；不代表所有迭代。第三次模拟是依据动作证据修正了首版内开组的后续冷却对齐，原中间结果保留。
- G9 两条原始预算解释回放通过：均撤回未经证实的“服务端上限”判断，无工具调用或虚构模拟结果。G8/G9不需要再造工具或重复发布；本次原题回放不是新的五类随机模型验收。
- G10 有限变体验证仍未完成四饰品组合：物品俗称和目标升级档位未核齐，正确保留缺口，0次模拟。发现其下属查询缺陷：已有快照中的完整物品名称不在固定名称表时查不到。
- G13 将该确定性查询缺陷单独修复：`af244e22bea1b894745f2ec0fd2467843375061a` 已合入并推送。仅修改现有options查询，复用已授权快照名称，不增设别名库、不猜升级档位。63相关测试、97发布契约、主工作区63控制面测试通过；独立冻结的确定性原例/数字ID变体/不同槽位留出/正常/权限矩阵由2/5变为5/5，未调用模型或模拟。隔离通用图片Chat、真实SimC、owner与幂等验收通过。

## 发布与恢复

批次 `de5ed88f44b3a6adcd649ba53d2241d0d344480dea797eaa340e90e135855820` 发布未通过。线上通用业务成功，第一条武器名称查询也返回正确ID237847；但复制的旧验收脚本要求必须出现原生搜索日志，即使问题明确“不联网”。没有搜索回执被误判，第二条查询未执行，整个批次不能记为通过。

发布器于12:03左右自动恢复955源码，随后真实图片Chat、SimC（DPS158652.53）、账号隔离、幂等与来源验收通过，状态`recovery_verified`。回执中的`enchant_smoke`是复用执行器的旧阶段标签，本次实际执行的是装备查询。

原失败执行器/批次保持不变。另存脚本修复，复用隔离harness已有的可选回执处理；无回执和单回执通过，重复回执拒绝，语法检查通过。修复后的完整线上流程没有重跑，不据此追认发布成功。无日志仅表示没有观察到完成的原生查询，不证明供应商零尝试。

发布包另有两个前置问题：上传文件owner与macOS元数据sidecar不符合精确清单；切流前已修正owner并隔离确切sidecar，原失败保留。工作树控制面62/63的缺失历史本地证据位于主工作区；主工作区原文件未改，运行同一代码的63项通过，未伪造历史文件。

## 停止点

本阶段8/8模型、6/6云端SimC、14/20已观测业务来源操作；含恢复验收，历史累计104模型。没有开启新一轮发布。生产仍为955，仓库main为af244e22b加本记录的文档提交，不能声称源码/生产已完全一致。G10完整组合、食物心脏阶段收益、原暂缓模型失败及G13重新发布均保留。私有回放副本按原七日到期时间归档，不重置保留期限。
