# 跨轮研究预算

本地实现，尚未 Candidate 或生产发布。

- 修复前：每个来源 capability 创建内存 ResearchBudget，下一轮可获得新额度。新增序列化测试先出现2项预期失败；真实Postgres消息入库测试先出现0个研究绑定的预期失败。
- 新增 additive migration，按用户/会话研究身份持久记录来源计数及身份回执、4个SimC提交预留；消息入库与研究切换同事务，Worker租约连接保护预算写入。保留失败预留，跨轮重放同snapshot/scenario返回原任务。
- 明确首行 `/新研究` 与 `/结束研究`，普通消息继续；不按关键词推断新问题。已结束研究禁止新增来源/模拟及原生搜索，可讨论旧证据。独立新研究重新开启。原生搜索维持原合同，不适用网关网页页数硬计量；360秒保留单次生成安全时限。
- 独立本地Postgres数据库测试28项通过（含10项研究/adapter/SimC接线和既有持久执行/账号并发）。无安装依赖；复用已有Python与PostgreSQL。没有本地运行SimC，模拟接线测试使用已有应用fixture。
- 后端719项通过；Web输入框25项通过，类型检查、lint、H5构建通过（既有webpack体积warning）。提示词实际加载31764 bytes。
- 首次后端测试有1个旧测试固定SQL条数断言失败，改为核对同事务最终会话更新时间语义后通过。首次H5构建缺少worktree内插件解析路径，复用已有workspace依赖链接后通过，无新增下载安装。

后续固定模型上限6次、云端新增SimC上限2次；原失败与历史消耗不修改。未宣称用户手工验收、生产或恢复演练。
