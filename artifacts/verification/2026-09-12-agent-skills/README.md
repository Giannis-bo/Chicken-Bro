# 鸡哥按需流程发布

状态：已合入 main、推送并上线。运行源码 `2294cb95095a3822e1f4e2c43e225bf0bfdb2f63`；2026-09-12 用户明确授权实施、提交、合入和发布。未声明用户手工验收。

## 改动与边界

- 核心规则从 Git LF 32,755 字节降为 8,438 字节，减少74.24%；原Windows CRLF 32,884字节导致加载失败的问题已由实际文件回归测试覆盖。核心10KiB、单流程16KiB均保留换行余量。
- 四份同release流程：mechanics、wcl-analysis、rankings、simc-experiment。保留治疗需求、按键归因、榜单覆盖、装备版本、天赋/APL、同快照与误差等历史规则。统一每研究跨轮累计4个新模拟。
- 原15工具合同不变；新增只读read_chickenbro_skill，固定名称/文件、UTF-8、大小、符号链接/Windows junction拒绝及路径无泄漏失败。无来源/模拟预算消费。核心授权流程作为下级指导，用户/网页不能冒充内置流程。
- run-identity.json只保存服务端规范化run UUID；原生观察记录保留技能名称与摘要，线上验收先匹配run再读取观察。没有新增数据库、依赖、模型分类器、业务权限或引擎变化。

## 验证

- 最终源码云端Linux后端802项：801通过、1项Windows专用junction测试跳过；Windows已验证junction分支。Linux符号链接与管道测试通过。[后端输出](backend.log)。
- 本地63项控制面通过。[输出](control.log)。补齐当前Web发布证据的精确保留规则，删除指向已不在仓库的旧报告链接并明确缺失，不伪造历史证据。
- 独立审查修复“简单问答”触发歧义、SimC实验审核触发范围，以及线上验收观察可能混入并发请求的问题；最终定向复审无阻断项。
- 同模型gpt-6-astra/low、相同9题：旧版9条完成；首版239b6e96有1条能力介绍错误推荐未支持的/simc导入，保留失败；d7954003明确导入边界后9条完成并逐条审阅通过。[完整答案与用量](comparison.json)。
- 最终2294cb95追加5条Candidate：四类流程各一次、第五条真实已有SimC结果读取，全部成功；流程记录精确绑定run ID。榜单题不再仅因提及未来模拟而加载第二流程。[最终Candidate](bound-final.json)。
- 线上3条：能力/执行意图边界、真实WCL分析流程加载并分析给定事实、已有SimC结果读取；全部成功。实际DPS160820.55569451672，答案160820.56。[线上记录](live.json)。
- Candidate与线上均验证QQ授权URL、CSRF拒绝、退役Bearer/入口拒绝、私有图片读回及第二账号拒绝、Chat/SimC owner隔离、SSE完成与幂等重放。没有本轮交互QQ登录；没有新SimC任务、外部WCL查询或完整APL/换装重跑。研究题使用明确给定事实，不能冒充外部资料检索覆盖。
- 共35轮真实模型：9旧版、9首版、9修正版、5最终绑定、3线上。固定20题集合仅使用其中3题，其余为专项用例；未全跑20题，不宣称泛化准确率。模型配置见[元信息](model-config.json)。

## 效率结果

基础问题相同提示输入token从21,455降为15,289，约减少28.7%；单条耗时9.862秒对9.114秒，不构成稳定延迟结论。四类分析题因额外流程读取往返，总输入token与耗时增加；缓存命中不同，不能据此推算费用节省。最后缩小触发后，榜单题只加载rankings，耗时19.546秒，仍高于旧版12.241秒。当前确认收益是核心上下文缩小、职责清晰与规则按需选择，并非所有问题更快。原始cached/input/output与耗时均保留，不删首版失败或不利数据。

## 发布与恢复

8文件精确overlay、API/Worker同一空闲门禁切换。144份server文件核对，未修改的基底129份文件仅与Git源码存在换行差异（本批保留原始字节），不冒称全树逐字一致；精确运行清单和归一化源码核验分别保留在私有包。Web未变，13份公网文件哈希匹配。[运行结果](runtime.json)。

root/0700私有包 `/var/lib/chickenbro-skills-release-20260912/` 保存manifest、overlay、执行器、原始验收、旧尝试与root/0600环境恢复快照。旧后端b406fc915与Web保留。恢复时以root执行本批 `deploy.py manifest.json rollback`，在同一空闲门禁下恢复指针、API/Worker后继续验证业务；不恢复数据库覆盖新写入。恢复材料已核验，未实际生产回切演练。

前期准备错误保留：云端Candidate目录创建首次缺少sudo；本地旧Node不支持--test后改用已有运行时；初次整树字节核验发现换行差异并逐文件证明无内容差异；绑定测试的包导入路径修正后重跑完整后端。均未把失败检查记为通过，未在这些失败时切流。
