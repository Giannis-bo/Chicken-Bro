# Chat 原生搜索恢复

状态：隔离检索验证通过；通用业务补验与发布正在推进。

用户在独立 Agent 查到资料后，要求重新处理搜索逻辑。修复 `c6b992ee8`，发布源码包含远端文档更新的 `031c1b2f4c5e1097b2be99997369fec1faef965b`。

- Chat 默认启用原生实时搜索，格式修正流程仍禁用搜索；自建网页工具是可选后备。
- 默认参考 3–5 个相关来源，允许补查必要详情；不再用固定 5 页限制结束未完成的调查。
- 网关计数只约束对应工具。玩家/WCL/SimC 业务范围不允许换工具绕过。无需新供应商、密钥、依赖或迁移。
- 同时包含此前已验证的公共网页代理修复；仅三项运行文件变化。

## 真实对照

| 类型 | 旧逻辑 | 最终逻辑 |
| --- | --- | --- |
| 原始戒指附魔问题 | 2 次未取得清单（复用已保留原始诊断） | 2 次取得 9 种身份、品质数值和来源 |
| 武器附魔变体 | 0/2，读取失败或无关搜索后停止 | 2/2，取得效果、数值和来源 |
| 未用于设计的法师宝石问题 | 0/2，无法核实效果和版本 | 2/2，取得效果、版本，保留指南分歧 |
| 暴击概念正常问答 | 2/2 | 2/2，无工具调用 |

最终 8 次均验证终态、历史、第二账号隔离、幂等与断连续跑；记录到 37 个原生网页完成事件。该计数只覆盖最终 8 次，不冒充先前探索调用的完整计量。一条回答中文译名存在差异，英文和物品链接可唯一对应；不宣称官方中文名稳定性已解决。

初次恢复入口的两遍只能列出种类，缺少完整品质数值，保留为部分失败。调整必要详情查询后两遍及最终两遍均取得明确品质的数值。原始无效上传测试单独保留，不能算原生搜索证据。

针对性后端 149 项通过。隔离 worktree 控制面 62/63：历史 runbook 引用的 cloud-codex-cleanup/report.json 未被 Git 跟踪；该文件存在于主 worktree，本次未修改该链接或覆盖其文件。

完整原始输入/回答仅存七日私有证据包，继承最早保留时间。元数据和独立评审位于本机 `/Users/boyuan/.codex/badcase/chickenbro/native-search-20260910/`。未关闭 G8/G10 模拟问题，也未声称 G9 原始 DPS 对比已完成。
