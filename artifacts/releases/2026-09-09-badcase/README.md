# G1 / G2 / G3 / G5 后端发布（2026-09-09）

用户明确授权发布、提交、合入。本次运行源码提交 `913d901c7ed3e839cbe611b6037ed71f0848de8f`；后续提交只补充验证脚本和发布记录，线上十个运行文件与最终 Git 源码逐项校验。G4 图片输入为独立产品任务。

## 发布内容

- G1：WCL 裸报告先发现战斗/角色范围，兼容 type/view 展示参数，保留部分 GraphQL 数据与错误。
- G2：客户端断开仅取消订阅；有界后台生成继续保存，复用账号互斥、幂等和 owner 隔离。
- G3：资源溢出与有效治疗收益分开，证据不足时不认定操作失误或固定治疗损失。
- G5：按显式角色/服务器/地区发现近期 WCL 报告，修复 Raider.IO 显示名/服务器别名误拒，保留身份保护。

无数据库迁移、客户端构建或小程序上传。SimC 新导入政策不变。

## 实际验证

- 隔离工作树：444 项后端、62 项控制面、9 项临时 PostgreSQL 测试通过；临时数据库已停止。
- Candidate：真实 HTTP started 后断开，生成成功落入 PostgreSQL，Web 会话读回同一回答；另一用户 404、忙碌账号 409、幂等重放无重复。WCL 角色发现/原样展示 URL 查询成功，G3 明确拒绝将 9 点溢出直接等同三次有效治疗。整个业务验收 45.21 秒，见 [Candidate](candidate-smoke.jsonl)。
- 正式公网 API：同样业务链路全部通过，整个验收 49.75 秒；真实鸡哥返回 Fusionbolt 的萨满/元素资料与来源，G3 保留证据边界。见 [线上业务验收](production-smoke.jsonl)。此结果为自动化业务验收，不冒充用户真机断网验收。
- 正式进程 cwd 指向新版本；十个文件 SHA256、Web 指针、数据库迁移集合、Codex 配置哈希及两项服务状态已核对，见 [运行身份](live-identity.json)。
- Candidate 启动最初两次因验证脚本错误使用 create_app 工厂失败；改为与正式服务一致的 server.app.main:app 后重跑通过。这两次未切换生产，也未创建测试用户。
- 测试使用独立合成用户与临时会话；结束后撤销会话、禁用测试用户并归档测试对话，未导出真实用户聊天记录。

## 版本与回退

[Manifest](manifest.json) 定义十个文件的精确覆盖；目标复制原后端版本再覆盖，179 个文件整体验证通过。Manifest SHA256：`605c2a1823d03980f4318cdd3d5ffc9f6b3db54216caa9f82b0d0dfebf0a88bb`。

- 正式后端：`/opt/chickenbro-releases/badcase-913d901c7ed3e839cbe611b6037ed71f0848de8f`。
- 保留上一后端：`/opt/chickenbro-releases/raider-4315455479566aab7ead4488c52842f3b83c55b1`。
- Web：仍为 `credit-189d711648c6846df73f07d9aeb2ae7f9766ff15`。

发布先使用 PostgreSQL admission fence 确认无 streaming Chat 或 queued/running SimC/队列任务，再停服务、切换软链、恢复服务与检查。失败路径保留旧目录；切换后的健康失败会尝试在同样空闲条件下回退。此次没有触发或演练实际回退，因此只记录回退目标与执行逻辑，不宣称恢复已验证。单独 rollback 参数未实现，手工回退须核对当前指针、使用相同空闲保护再切换旧目录。

## 保留边界

G2 不支持进程重启后自动续跑，长查询耗时也未解决。G3 五组基线和候选均通过，不宣称统计准确率提高。G1 部分数据/排名缺失仍须明确限定结论；G5 受公开日志、上游可用性与分页上限约束。定时增量扫描及自动发布完整工作流仍是后续独立交付。
