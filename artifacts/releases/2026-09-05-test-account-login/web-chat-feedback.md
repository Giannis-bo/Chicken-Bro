# Web 输入区与等待反馈验证

日期：2026-09-05。最终实现/测试 H5 构建：`54e9562b647377201406b7f7c9beb6aa8f816596`。用户已批准方案；体验验收与 main 合入待确认。

- 输入区桌面高度从 58px 增至 72px，强化边框、阴影、留白和主题色聚焦状态。浏览器修正前实际输入字体为 `monospace`，正文为 `Microsoft YaHei / PingFang SC / Hiragino Sans GB / Arial / sans-serif`；修正后两处实际字体一致，同为 14px、1.75 行高（计算行高约 24.5px）。
- 发送后显示圆点动画和“咕咕正在处理…”，10 秒起显示实际等待秒数；正文、失败或断线后移除等待组件并清理定时器。占位挂载时一次滚入可视区域，计时刷新不重复滚动。减少动画偏好关闭动画，耗时节点对读屏隐藏；未进行真实读屏设备验收。
- TDD 首轮新用例 3 项因缺失等待提示失败，实现后通过；补充一次滚动定位断言先失败后通过。最终前端 151 项、控制面 62 项、类型检查、lint 和 diff 检查通过。独立代码审查未发现重要问题并独立重跑 9 项 composer 测试通过；补充定位代码由主审复核。
- 从 clean commit 构建测试 H5，通过；保留既有图片/入口体积两类警告。系统默认 Node 为微信开发者工具旧版，无法启动当前 Vitest；切换机器已安装 Node 23.9.0 后运行，无安装或下载依赖。
- 登录态 Edge 已验证桌面与 390×844 窄屏输入区、聚焦及等待占位无重叠。测试会话中两次真实请求均呈现等待后恢复发送图标；第一条界面问候触发助手领域拒答，第二条“打断和驱散的区别”正常返回两句话。这里验证的是交互状态，不将拒答写为内容验收。10 秒计时与失败路径通过 DOM/假时钟回归，未人为中断云端请求或伪造进度。
- 最终 `54e9562b` 构建在同一测试会话第三轮发送净化术问题：390px 窄屏已有多轮消息时，等待占位立即滚入可视区域，随后返回真实简答并恢复发送图标。窄屏沿用原页面整体滚动布局。验证结束已重置 viewport、返回用户原会话，输入框为空且没有遗留等待状态。
- 只发布 `/test/` Web，22 个文件本地/远端/公网 SHA 全部一致；96 个非 Web 文件逐项校验保留，包含现有后端及热更新规则。测试服务未重启，数据库、凭据和正式客户端未修改；readiness 为 `ready`。精确身份和逐文件清单见 [部署证据](web-chat-feedback-deployment.json)。
- 当前测试 release 为 `/opt/chickenbro-test/releases/54e9562b647377201406b7f7c9beb6aa8f816596`。回滚将 `/opt/chickenbro-test/current` 原子切回前一版 `4d3f3168394c52204db3a2e2ad6f65a39edd387a`；完整撤回本次输入/等待优化则切回 `c4b6d4b0f24c40ba4e67ffe3ffbf778a48f9ade5`。旧 release 均保留，回滚不删除数据或重启后端。
