# FAQ 独立页面（2026-09-07）

- 用户纠正：FAQ 使用独立页面承载，取消 FAQ 弹窗；更新日志保留弹窗。
- 页面在现有 Web shell 的主内容区，按认识鸡哥、账号与记录、SIMC 模拟分组，保留顶栏及返回对话/模拟入口。
- Web 使用 `?view=faq` 作为可直接访问的页面地址；保留原 Taro hash。对话/模拟/FAQ 导航支持浏览器历史，Chat/SimC 保持挂载，未新增 Mini 路由或 API。
- 先修改回归用例确认 2 项失败；实现后 21 项聊天/导航测试通过，包含直接链接、历史事件以及往返保留回复和草稿。类型检查、改动文件 ESLint 与 H5 测试构建通过，构建仍有两项资源体积提示。
- 源码基线 `29e90700576ba4bbb82f7bfeef259ba2ececc4b5`，含此前侧栏与账号入口未提交改动；构建身份与逐文件源码/产物 SHA 见 [清单](web-faq-page-manifest.json)。
- 测试 Web 构建：`1a2934d8007bfff9dc2ebf4ce72fc71f22917fc3c189e42ff6f51423f0f18dcd`。发布前后新旧静态目录全量 SHA 核对通过。
- Chrome 实测：直接 URL 打开完整 FAQ，进入模拟输入临时文本再进入 FAQ，浏览器返回后临时文本保留；随后已清空。390×844 页面文档宽度 390、dialog 数为 0，已恢复默认视口。没有读取角色、发消息、提交模拟或退出账号。
- 仅更新测试 Web；后端 release 保持 `d94646b6e97a4c84a3ac2af859d095d5c23cc007`，无生产部署、数据库写入或服务重启。
- 回退目录：`/opt/chickenbro-test/web-updates/1a2934d8007b/web` 保存上一版完整静态页面；同目录保留新旧 manifest 和源码快照，核对 current 与 SHA 后可反向原子交换。
- 状态：测试页已发布并验证，用户体验验收待完成；源码未提交、推送或合入 main。
