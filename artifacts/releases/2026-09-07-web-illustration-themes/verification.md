# Web 插画主题本地验证

日期：2026-09-07；分支：codex/web-illustration-themes；等级：local_verified，工作区未提交。

## 实现与资源

用户确认六张线稿上色；默认部落主题加六个新主题，共七项，无伊利丹。头像菜单入口、立即切换、浏览器保存与跨标签存储事件恢复、异常存储提示、未知值回退均局限 Web。原图位置：apps/mini-taro/src/web/assets/themes/；提示词见同目录 README。

## 验证

- 先添加两项交互回归，因缺少修改主题入口失败；实现后通过。
- 267 项前端回归通过（含主题与存储异常、流式聊天不中断）；类型与 lint 通过；H5 构建通过。
- 浏览器隔离外观预览复用 WebShell/WebHeaderActions/WebThemeDialog 与实际样式，以 DOM 组件适配替代 Taro 宿主、空列表和头像替代远端数据；不是实际账号登录验收。
- 桌面 879×708、窄屏 390×844：主题卡片完整、弹窗内容可滚动，七张图片加载成功；全部主题选择对应正确 id；刷新保留联盟主题；完成后焦点返回头像。
- 实测发现原生 dialog 关闭时覆盖提前设置的焦点，已改为关闭后的清理阶段恢复焦点，并重新实测。
- 本地源码审查：固定主题白名单、无任意 URL/HTML 输入、仅外观 localStorage、未卸载业务组件、未改 Mini 或服务端、未触及生产。

## 边界

未提交、推送、部署；未宣称真实扫码或用户验收完成。完整本地登录页受 HTTPS 证书条件限制，未绕过浏览器证书检查。外观预览在 http://127.0.0.1:4180/，只能用于查看外观，不用于 Chat/SimC 业务验收。

## 2026-09-07 布局修正

- 逐项检查发现全工作台背景被侧栏和输入框遮挡，居中欢迎标题与角色面部、斧头重叠。插画移入消息区域独立容器，随输入区高度自动留出边界。
- 联盟/虚空文案居左，亡灵/剧毒文案居右；部落/玛格汉/死亡之翼标题置顶、场景居中。窄屏统一上下排布，保持原图比例；上下边缘渐隐，已开始对话时降低背景浓度。
- 隔离外观预览逐一截图检查全部七主题：1280×720、1512×982、390×844；使用真实源码与样式，仍非真实登录或生产验收。
- 本轮新运行 typecheck、lint、18 项主题/流式聊天/自动滚动回归、H5 构建、git diff --check 均通过。H5 仍有资源/入口体积两项警告。

## 2026-09-08 六主题轻动效

- 格罗姆内置 image_gen 两轮重绘：第一轮放大堡垒，第二轮删减密集细节；`maghar-citadel.png` 已接入主题与缩略图，旧稿保留。新增 `storm-clouds.png`，sips 确认为 alpha PNG；提示词见素材 README。
- WebThemeArt 使用同一 SVG 坐标系显示原图、局部风动与光效。风动只在云、发辫/发梢、旗帜、披风软遮罩内改变采样，外侧使用中性位移。死亡之翼原图静止、叠加胸口裂纹描绘和熔光、远景陨石；未给乌拉特克新增动效。
- 新增 3 项行为测试：实时减少动态效果、隐藏标签/切换业务页停止、乌拉特克静态。全前端 39 文件 270 项通过；typecheck、lint、H5 构建、diff 检查通过。H5 仍有资源与入口体积警告（入口682 KiB）。
- 浏览器本地隔离预览逐项检查六个主题；1280×720 桌面及390×844的希尔瓦娜斯/格罗姆窄屏复核，脸、武器、文案未新增遮挡。实读云层 computed transform 为 matrix(1,0,0,1,25.1724,0)；切 FAQ 后 data-moving=false，animate 节点为0。
- 页面视觉复核和动画 DOM/CSS 状态不等同真实账号验收或跨浏览器性能基准。未提交/推送/发布，尚待用户对动效幅度的实际体验反馈。

### 动效可感知性修正

用户确认在本地预览看不出动效。排查本地 data-moving=true，非系统关闭；原动画经画面缩放后位移过小，且格罗姆初次闪电需等待约12秒。提高局部风动幅度、将周期由14秒改为6秒；云层范围40扩至90源像素，周期缩短；闪电/陨石在切换后首几秒出现，光效对比提高。脸和身体遮罩边界未扩大。

浏览器同主题两次实读云层位移 -24.5956 → -9.78563，配合截图检查；15项相关回归、类型、H5构建和diff检查通过，构建仍有两项体积警告。本地预览更新，未发布。

### 实际像素与连贯摆动复核

用户再次反馈变化不明显。使用已有 gstack browse 浏览器对真实本地页面做确定性时间采样：暂停 CSS 装饰层，只把 SVG 时间设为0与1.5秒，旧风动约3256个像素超过2级色差，肉眼表现仍为细碎扰动。将噪声滚动替换为软遮罩内连贯往复位移（5秒周期），云层平移范围提高；保持相同身体/面部保护边界。

新版0/2.5秒截图：部落约12854像素变化；联盟旗帜区域5723像素变化，抽查面部与护甲均0；希尔瓦娜斯披风6399、抽查脸/身体均0；萨拉塔斯发梢2650、脸部抽查0。以上是选定区域的像素比较，不是全身每像素保证。截图及20帧采样GIF见 motion-proof/，旗帜约20屏幕像素的极值位移，5秒循环；GIF暂停了云层CSS以隔离旗帜效果，局部GIF仅裁切截图，未放大动作。

16项动效/主题/业务切换回归、类型、lint与diff检查通过。首次组合测试遇到同时新增的短URL回归尚未接线，未干预路由改动；接线更新后新鲜复跑16项全通过。本轮实际页面由Vite编译并截图，未新增生产部署。

## 最终状态：用户取消全部插画动效

静态背景已恢复；动效组件简化为背景图展示，动画CSS、SVG滤镜和叠加特效移除。新版格罗姆与主题选择保留。typecheck、15项相关测试、diff检查通过。浏览器实际读取 document.getAnimations().length=0，animate与data-motion-layer节点=0。前述动态证据仅为历史，不代表当前交付。未发布线上。

## 2026-09-08 合入发布前复核

用户已明确接受并授权合入、发布。合并 origin/main 后保留账号并发、会话删除与短地址；与 origin/main 比较，features/packages/server 无差异。289 前端、62 控制面、类型、lint、正式 H5 构建通过。控制面新增主题计划/证据保留规则。

源码审查覆盖主题 storage 回退及失败提示、切换不重建模型、原生 dialog 焦点恢复、七资源映射、动效删除与平台边界。CodeRabbit 未安装，沿用用户“不需要再安装CodeRabbit”的既有决定，以本地源码审查和 fresh checks 为本次门禁。

正式配置预览修复了 Taro 将 button CSS 标签改写为 taro-button-core 导致完成按钮失去样式的问题：改用独立 done class 和显式 focus 类。重跑289前端/类型/lint与两个正式配置构建通过；生成 CSS 不含被改写的主题按钮选择器，未打包弃用 maghar/storm-clouds。

最终构建源提交 4a39d1eed6386de69c781c1267aee8af1cf1fc99。正式域名 /previews/themes-final-20260908/ 使用现有登录态与生产 API，只读历史；这不是隔离数据库 Candidate 或新的扫码验收。七插画加载、七名称、完成按钮右对齐、删除底部提示、主题切换/刷新、桌面及390px检查通过。未发送消息或创建模拟。回滚目标与逐文件 SHA 见 final/manifest.json。
