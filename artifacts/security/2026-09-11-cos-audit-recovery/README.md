# COS 历史调查与审计恢复交付（2026-09-11）

状态：本轮只读调查、当前资源完整性及本地恢复验证完成；历史匿名写入结论仍未知，目标账号配置核验与供应商追溯待外部输入。没有新增服务、费用承诺、桶迁移、云端写入或应用部署。

## 范围与证据

基线源码 `6e7037fbe2f2178da6ab36162e7fe01615731b8f`，桶 `zhajiduizhang-1257807175`、上海，当前资源路径 `wow-assets/releases/2026-09-08-web-themes-v1/`。保留 [2026-09-08 原记录](../2026-09-08-cos-write-hardening/report.md) 与原失败/消耗，不覆盖历史时间。

预算冻结为当前七张 PNG、一个清单、一个既有标记；一次 CDN 完整核验，一次源站完整核验，一次匿名 ACL 只读查询，一次本地恢复。实际 15 次 CDN GET/HEAD、9 次源站 GET、1 次 ACL GET；图片两路总计 20,223,450 字节，另有小型清单和响应头；没有保存公网响应图片。没有全桶扫描、模型调用、SimC 任务、安装或新下载依赖。官方页面瞬时读取失败仅有限重试；浏览器能力初始化失败一次、重试恢复。

| 核验 | 实际结果 | 边界 |
| --- | --- | --- |
| [CDN](cdn.json) | 七图 SHA256/字节匹配，PNG 类型、一年 immutable 缓存通过，清单匹配 | 仅此次路径与时点 |
| [源站](origin.json) | 七图 SHA256/字节及清单匹配，原安全标记仍为零字节 | 不证明没有曾覆盖后恢复 |
| [控制面](control-plane.json) | 匿名 GET Bucket ACL 为 403 AccessDenied | 不证明当前写权限或日志已关闭 |
| [本地恢复](local-restore.json) | 固定 Git 提交经 git archive 解包，复用 prepare-web-theme-cdn.cjs 重建七图与清单，全部匹配 | 同一本地 Git 源，非独立备份介质；未执行云端恢复 |

2026-09-08 的匿名新建与覆盖 403 是已保存的历史证据，本轮不把它改写为新鲜写入测试。当前公网 GET 成功只能支持公开可读和指定资源完整性，不能据此确认完整 ACL。

## 当前工具与账号边界

已搜索本会话可调用工具：未找到专用腾讯云 COS/Lighthouse 控制面或数据面工具；本地 PATH 未找到 tccli、coscli、lighthouse。复用现有 Node/curl 清单工具完成读取，无需重造上传引擎。该结论仅限本会话工具和 PATH，不推断腾讯云没有 API。

鉴权 Chrome 控制台实际打开 Lighthouse 桶列表，但当前账号展示的桶 APPID 为 `1251810746`，目标为 `1257807175`。未访问或修改其他桶，也未把另一账号套餐/权限作为目标证据。目标桶的 ACL、日志、版本和对象锁状态均未取得新鲜鉴权读回；需要切换到目标桶所属账号或已有授权的只读身份。不要在任务对话中提供密钥。

## 官方能力核对（2026-09-11）

- [Lighthouse 与 COS 对比](https://cloud.tencent.com/document/product/1207/108904)：轻量桶仅提供基础桶操作；版本控制、生命周期、清单等列在标准 COS 高级能力，轻量桶不支持桶级 SDK 操作。不能直接调用标准桶配置 API 并称轻量桶已获支持。
- [日志存储](https://cloud.tencent.com/document/product/436/16920)：标准 COS 上海地域支持，目标须同地域，需 CLS 服务授权且有费用；不应投递到公开源桶。只覆盖文档列明的 XML API 路径，且不保证日志 100% 准确。没有开启前的日志不能靠现在开启补回。配置入口缺失还可能由登录身份造成，不能单凭 UI 缺入口证明产品不支持。
- [版本控制](https://cloud.tencent.com/document/product/436/19884)：标准 COS 可保存及恢复版本；历史版本计费，开启后只能暂停、不能回到初始未启用状态。不能恢复启用前已被覆盖且没有备份的数据；不启用默认历史版本清理或任何自动删除。

对象锁定不在本次已核实的轻量基础能力中，实际是否支持、开通条件和限制需要供应商书面确认；本轮不启用，不把版本控制等同对象锁。

## 可执行恢复方案

现有七图共 10,111,725 字节，发布源文件和确定性生成器已纳入 Git。此次已验证从固定提交重建七图与清单，足以提供当前主题图的已知良好恢复来源。它不覆盖旧版 135 个图片/矢量对象、全桶未知对象或私有数据；不声称独立灾备完成。

如发现指定资源不匹配：保留异常对象 key、请求时间、request ID、哈希及原始证据到私有介质；禁止先覆盖或删除异常对象。用上述固定提交准备本地八文件，复核 SHA 后，经目标账号已有鉴权上传到**新的版本目录**，使用禁止覆盖请求头；没有签名能力就停止，禁止恢复公有写。对八文件执行源站与 CDN 回读，再按当前应用发布程序变更 CDN 根路径并验证用户可见图。回滚只切回经核对的良好版本目录，保留异常目录和旧目录。若同 URL 已缓存异常内容，不能只修源站后宣称恢复，优先换版本路径；缓存刷新另记录实际结果。

本轮无需上传、改 ACL 或重新部署：当前八文件匹配，未发现需改动的运行资源。后续发布继续复用现有 prepare/verify 入口，并保存源站证据。

## 日志方案与成本边界（未启用）

优先让腾讯云确认该轻量桶能否在原桶开启对象日志；若可行，提议上海独立**私有**日志目标，前缀按源桶与日期区分，先按 30 天保留窗口估算费用。保存时间、对象路径、操作、状态、requester、request ID，区分匿名成功写入和被拒绝尝试。原始日志只授予审计身份；共享报告去除 query 签名、KeyId、IP 等敏感字段。日志里的 requestUri 可能含签名，不能提交原始日志到仓库。CDN 边缘访问记录不能替代直接源站写日志。

若轻量桶不支持，不在本轮迁移；标准 COS 方案另审批源桶、目标桶、CDN 回源、版本保留、CLS 角色、迁移清单和回滚。现有套餐不推定可抵扣标准 COS/CLS。

成本拆分：

- 本轮新增订购费用为 0；现有服务 GET/HEAD 与流量可能依既有账单计费，未取目标账号账单，不能宣称总账单零增长。
- 当前图集约 0.00942 GiB。按官网上海标准存储参考价 0.118 元/GB/月，保留一份约 0.0011 元/月，十份约 0.0111 元/月，**仅图片存储估算**，另加清单、请求、流量、日志、版本等。不是迁移或总费用报价。[COS 定价](https://buy.cloud.tencent.com/price/cos)
- 日志应按实际日增日志量 D、保留天数 R、索引比例和投递量计算：稳定保留量约 D×R，月写入约 30×D；再叠加 CLS 写入/索引/存储/请求与 COS 日志对象存储/请求。目标桶请求量、实际日志大小、折扣和 CLS 计费模式未取得，因此不能负责任地填写固定总价。[CLS 定价](https://cloud.tencent.com/document/product/614/45803)
- 原桶历史存量没有全量盘点；旧清单的 135 个文件不等于当前全桶字节数。迁移费须取得准确清单后算，不能把当前 10 MB 图集当整桶。

启用前需批准具体月费用上限与服务授权。建议先取得报价再批准 30 天试运行；告警阈值不是计费硬上限。停用日志也不自动撤销 CLS 投递角色；回退须核对其他桶是否共享角色，只撤销本次新增权限，保留已有日志，不删除证据。版本控制暂停仍有历史版本存储费，不能称完全可逆或停止所有费用。

## 后续唯一必要输入

1. 目标桶账号的已登录只读访问，以核实当前 ACL 与已有日志/版本配置；若已存在日志，优先复用，不新增服务。
2. 批准向腾讯云发送 [工单草案](support-ticket-draft.md)，只请求历史范围与功能/报价确认，不授权云端变更或收费调查。未发送。
3. 供应商确认能力与费用后，才审批日志启用/标准桶迁移/版本保留；本轮不要求预先批准未知金额。

完成这些之前，该事项必须保持“历史调查受阻 / 审计启用待决策”，不能标记历史安全已查清或持续审计已上线。
