# COS 匿名写入整改（2026-09-08）

生产桶 `zhajiduizhang-1257807175`（上海，Lighthouse 版）已通过鉴权控制台从公有读写改为公有读、私有写。详细结果见 [evidence.json](evidence.json)。

## 已验证

- 未签名 PUT 在根目录、`data/`、当前插画发布目录创建新对象均返回 `403 AccessDenied`。
- 鉴权控制台创建独立零字节目录标记后，未签名 PUT 覆盖该已存在对象也返回 403；请求未带禁止覆盖头。标记再次读取仍为空，没有覆盖生产图片。
- 权限变更后，COS 源站和 CDN 的七张 PNG、发布清单全部匹配本地源码；CDN 图片类型和一年 immutable 缓存检查通过。
- 鉴权控制台根目录原有 `data/`、`wow-assets/`；`data/` 显示为空。发布目录包含 2026-07-18、2026-09-08 两版。旧清单列出 69 PNG、66 SVG，当前版为七张 PNG 和一个 JSON 清单；没有完整核验历史对象字节。
- 零字节校验标记 `wow-assets/security-verification-20260908-9452e079/` 保留为证据；被拒绝的新对象没有创建。

## 审计与功能限制

云审计近七天 COS 写事件筛选结果为零，跟踪集为零；未取得对象请求级日志。这不能证明没有历史匿名写入，也不能排除曾被覆盖后恢复。当前哈希只证明核验时内容与预期一致。

[腾讯云 Lighthouse 版功能对比](https://cloud.tencent.com/document/product/1207/108904)仅列基础桶操作，版本控制属于标准 COS 高级功能，且 Lighthouse 不支持桶级 SDK 操作。当前控制台未暴露对象锁定和访问日志设置；匿名配置查询被拒绝，不能据此断定已关闭或开启。本轮未启用版本控制、对象锁定或付费日志服务。

## 后续操作约束与待办

1. 上传仅使用鉴权控制台或最小权限签名身份，保留新版本目录和禁止覆盖请求头；签名不可用时停止上传，不能恢复公有写。凭据不得写入仓库、日志或前端。
2. 发布继续执行本地 SHA 清单与源站/CDN 回读校验。公开桶仅存公开静态资源；私有数据应使用独立私有桶。
3. 历史调查需要腾讯云协助。工单草案：请核查该桶截至 2026-09-08 14:07（北京时间）的历史匿名 PUT/DELETE、对象覆盖和 ACL 变更记录，确认日志可追溯范围，区分本次已知部署写入；重点为桶根、`data/` 和 `wow-assets/`。工单尚未发送。
4. 持续对象审计、版本恢复或对象锁定需要先确认 Lighthouse 支持情况；如不支持，再评估标准 COS 迁移及日志保留、存储成本。迁移和保留期限待决策。

匿名写入入口已收紧，历史调查仍受日志可用性限制。记录随 1.0 文档收尾纳入仓库；上述检查时间保持原值，本轮未重放云端权限操作。
